# Weyver 模組設計文件索引(按產品功能發布藍圖分類)

> **分類依據**|對齊 `docs/23 產品功能發布藍圖`(R1 完整 Ragic → R2 ERP+計算層 → R3 MES+ISO → R4 對標+AI)+ Foundation(Phase -1 基礎/橫切)。sprint 代號沿用 `docs/13`(P0-x=Phase 1、P1-x=Phase 2,歷史代號)。
> **檔案組織**|各模組 design doc 依所屬 release 置於子資料夾 `docs/modules/<R1|R2|R3|R4|foundation>/`;`_template.md`(骨架)與本索引留在根層。**新模組動工前先寫 M0 design doc,依 release 放對資料夾,並在本表登錄。**
> **流程**(每模組)|M0 DRAFT → OQ 裁定 APPROVED → M1..MN 實作(每 milestone 一 commit)→ FMEA 收尾 SHIPPED。詳見 `_template.md` + `memory/rule_module_design_flow`。
> 🔴 **動工前先查 [`_research-index.md`](_research-index.md)** —— 那是「這個問題**已經有人查過了**」的索引,含**反向索引**(哪一份 Ragic 官方文件已被誰逐字抄過)。它是**產生檔**,由 `packages/docs-check` 從模組 doc 掃出並在 CI 比對,不會漂。⚠️ 重複查證最貴的不是時間,是**兩次查出不同結論卻沒人發現**。
> **[P0] 研究即寫入 doc**|競品 / 規範 / 實測研究**當下就寫進模組 doc §0 並附來源連結**,不得只留在 task 或 commit message —— 2026-07-28 稽核發現六個模組的研究成果全散落在 repo 外,**來源 URL 差點永久遺失**。追溯補寫用 `## 0-bis`。
> ⚠️ **「附連結」不是形式要求**|2026-07-29 複核 `print-merge` 時,其只記文件編號未附 URL 的引用**被查出兩處歸屬錯誤**(把「紙張委派瀏覽器」記在 `doc/149` 名下,實際出處是 `doc/4`)。**編號無法自我驗證,錯了也看不出來**;連上去一看即知。凡引用外部文件一律附可點 URL。
> **[P0] 收尾必回填 `docs/25`**|模組 SHIPPED 時,除本表標 ✅ 外**必須**同步更新 `docs/25 功能完整對照清單` 之對應子功能列(✅ / 🟡 並註明界線)並重算覆蓋率 —— docs/25 是「仿 Ragic 進度」唯一權威,不回填則該數字失真。
> 🔴 **這條規則已經漂了四次,所以 2026-08-05 起有機器擋**|`packages/docs-check` 會在 SHIPPED 的模組**沒有出現在 docs/25** 時讓測試紅。回填時把 **sprint 代號或 design doc 檔名**寫進那一列(它是機器對得上的鍵);真的沒有 parity 面就加進該檔的 `EXEMPT` 並寫明理由。
> ⚠️ **機器只擋得住「從沒提過」**,擋不住「提過了但該翻的 ⬜ 沒翻」—— 後者靠 docs/25 §1 的**逐段校對日期** + 「選一段動工前先重驗那一段」。
> **狀態圖例**|✅ SHIPPED · 🚧 進行中(APPROVED / 實作)· 📝 M0 DRAFT · ⬜ 未起(規劃中)

---

## Foundation(Phase -1|基礎 / 橫切,gate R1 對外上線)

| 模組 | Sprint | 狀態 | 文件 |
|---|---|---|---|
| **訂閱計費(地基預留,非實作)** | F-8 | ✅ **SHIPPED v1.0(2026-07-28;M1–M3 + FMEA B1–B8)**|應決策方「先規劃預留,避免最後商業化有問題」。**核心主張:分開「計費自動化」(可延 —— 17 家客戶手工開帳單完全可行,docs/04 A7 已採此路)與「計費地基」(不可延)**,分界線為「**成本是否隨時間上升**」。**⚠️ 查出文件不一致**:docs/04 A7 說 **Phase 2**、docs/13 說 **Phase 4**、docs/23 說 **R4** —— 排期差兩階段 → OQ-SB-1(建議統一為 docs/04 原文之觸發條件「客戶數 > 10 家」,而非硬編階段)。**程式碼現況**:entitlement / 用量記錄 / 租戶生命週期**三者全缺**,但 `QuotaService` 單一檢查點 · `tenants.parent_tenant_id`(多廠區)· `TenantGuard`(停權攔截點)已是可用的縫。**不可延三項**:**用量歷史(晚做就永久失去,唯一不可回溯者)** · 生命週期狀態(要在 `TenantGuard` **每請求熱路徑**攔)· entitlement 檢查點(成本隨模組數**線性成長**)。**預留為零行為變化**:`tenants` 三個 nullable 欄 + append-only `tenant_usage_daily` + **P0 一律放行**的 `EntitlementService`。**FMEA 要點**:計費須 **fail-open**,不可與 authz 的 deny-by-default 混用(B2);停權採唯讀而非封鎖,否則客戶拿不回自己的資料(OQ-SB-5)。**落地**:`tenants` 三欄 + `tenant_usage_daily`(RLS;**app 車道只授 SELECT** —— append-only 由**權限**保證不靠自律,整合測以 `SET ROLE weyver_app` 斷言 UPDATE/DELETE 皆 permission denied)+ `EntitlementService`(全放行)+ `QuotaService` 四層 fallback + `TenantGuard` 停權唯讀(**GET 不查 DB**,不為每個讀取加 round-trip)+ `UsageService` 每日快照(席位 / MAU / 表單數 / 儲存位元組,advisory lock + 冪等可補算)。**零行為變化**(全租戶 active / plan NULL)。**實作期修正**:`records` 指標宣告未採集 → 移除並說明(逐張動態表 count 成本不成比例);`UsageModule` 自 @Global 拆出(相依 DDL_KNEX,否則污染所有測試模組圖)。**殘留 B6**:停權唯讀已保證,但 **docs/04 I「匯出所有資料」⬜ 未起 → 停權功能啟用前必須先具備匯出**,否則承諾是空的。**未做**:金流 / 帳單 / 發票 / 催繳 / 方案內容 / 管理端 UI / 停權自動觸發。api 360 綠(+21)。migration 0017。commit `1eaeb1f`(M1)/ `6af1de4`(M2)。≈0.16 mo | [foundation/subscription-billing.md](foundation/subscription-billing.md) |
| Monorepo + NestJS + PG 骨架 | F-1 | ✅ 隨 form-engine-core M2 落地(`apps/api`)| —(見 R1/form-engine-core §9-bis)|
| **Auth + 租戶 context + 使用者身分**(Better Auth + Argon2id)| F-2 | ✅ **SHIPPED v1.0**(M0–M5:引擎+DI · 對映表+IdentityService · AuthGuard session→tenant + 剝 client header + TenantGuard 環境分派 + 跨租戶隔離 e2e · `/api/auth/*` handler + 前端登入/註冊/登出 + 受保護 layout〔強制登入僅 prod〕· rateLimit/安全標頭/throttler 硬化 · auth.spec 固化 · FMEA P0 全緩解;§6-bis 登入分層+治理;org→tenant 走 afterCreateOrganization hook;**dev header→真實認證,R1 上 prod 硬前提達成**。三層 RBAC=P0-4;SSO/MFA=後續)| [foundation/auth.md](foundation/auth.md) |
| UI shell + design tokens + deploy | F-3 | ✅ 前端 v2.1(`packages/ui` + `/app`)| —(見 docs/14)|
| **二步驟驗證(MFA / TOTP)** | F-4 | ✅ **SHIPPED v1.0**(M0–M4:twoFactor plugin + 4 整合測 · 帳號設定啟用/停用 UI〔QR + backup codes〕· 登入二步 challenge〔/login/2fa,TOTP + 備用碼〕· verify 端點 rateLimit · mfa.spec 固化 · FMEA P0 全緩解;承 F-2;scope out email/SMS OTP · passkey · org 強制 · trustDevice)| [foundation/mfa.md](foundation/mfa.md) |
| **平台可靠性工程(冪等 / 配額 / metadata RLS 兜底 / 清理 job)** | F-6 | ✅ **SHIPPED v1.0(2026-07-28;M1–M5 + FMEA L1–L7)**|**不是新功能,是收斂跨模組 P1 殘留**:form-engine-core R7(mutation 無 idempotency key,AGENTS ⚙️ [P0] 鐵則缺口)· C5(建表 DDL DoS 無 per-tenant 配額)· T4(metadata 表已有 RLS FORCE 但 Drizzle 走特權連線 → superuser 繞過,現為 app 層單防線;F-2 已 SHIPPED 故治本條件成立)· C2(孤兒 pending form 清理)· R8(app 車道 statement_timeout)· file-storage S6(孤兒檔實體回收)· actions-approval(簽核完成與 onComplete 非同一 tx)。**明確排除並附理由**:outbox / 欄位級加密 / feature flag / circuit breaker(無外部相依,不為不存在的需求建設施)。**收斂之殘留全清**(core R7/C5/T4/C2/R8 · file-storage S6 · actions-approval)。關鍵實作:`TenantDb.withTenant` 為租戶 metadata 唯一入口(不曝露裸 db,漏設 GUC 於編譯期即不可能);簽核改「先副作用後定案」+ 冪等 key(跨車道無法同一 tx,新序嚴格優於原設計);清理 job 交易範圍 advisory lock。api 305 + e2e 25 全綠(20 新測)。人月出自 docs/04 v2.4 已編列之 I 平台可靠性工程 8 人月之 P0 子集。migration 0015/0016。**後續批次**:outbox / 不變量對帳 job / 欄位級加密 / feature flag / circuit breaker。≈0.36 mo | [foundation/reliability.md](foundation/reliability.md) |
| **影像處理(EXIF 剝除 / 縮圖 / HEIC)** | F-7 | ✅ **SHIPPED v1.0(2026-07-28;M1–M4 + FMEA P1–P10)**|收斂 image-signature-fields S3(無縮圖)/ S4(EXIF 隱私)/ HEIC 不支援 + file-storage 縮圖殘留,**三處殘留全清**。**設計由實測與競品證據推翻三項原前提**:(a) HEIC **不是加相依就能解** —— sharp 0.34.5 預建版無 HEVC 解碼器、維護者表態永不內含,且**專利不可解**(HEVC 池明文對雲端服務按用戶計費)→ 改走**前端轉檔**(`accept` 排除 HEIC 觸發 iOS Safari 自動轉 JPEG);(b) 縮圖**不需背景 job**(24MP→240px 僅 28ms)→ 同步做,省掉整套佇列與狀態機;(c) **重編碼剝 EXIF 是錯的** —— 世代性失真且 q95 反而**大 14%** → 改採 **JPEG marker 層無損切段**(APP1/APP13),已驗證壓縮資料位元組完全相同,兼顧 Airtable「不改原檔」精神與消除 GPS(**Partiful 2025-10 GPS 外洩事故**為警示先例)。另實測 1.6MB 檔案可宣告 149.8MP、sharp 預設 pixel 上限擋不住 → 顯式 `limitInputPixels = 50MP`。落地:`ImageProcessor`(無損切段 → orientation>1 才 `.rotate()` 重編碼 → 320px webp 縮圖**永不放大**,承 Ragic)+ `{key}.thumb.webp` 衍生 key(零 migration)+ `?variant=thumb`(**取不到縮圖回原檔,前端永不破圖**,省掉 Teable 式重生機制)+ 孤兒回收涵蓋縮圖。**實作期兩發現**:假 PNG fixture 觸發 500 → 補齊設計中已列但未實作的 422 `IMAGE_UNREADABLE`;`Content-Length` 誤用 `file_object.size`(已含縮圖之配額)致下載截斷 → 改取 `storage.stat()`。api 9 單元 + 4 整合 + web 3 e2e 綠(41 e2e 全綠)。commit `76ff412`(後端)/ `eec7509`(修正)/ `40140f3`(前端)。**殘留**:P9 iOS 自動轉檔前提**僅社群證據、桌面 harness 無法驗**(降級路徑已備:錯誤訊息明確指引)/ P6 `sharp.concurrency`·`MALLOC_ARENA_MAX` 與 P8 `supportedArchitectures` 歸部署硬化 / P7 既有影像不回溯 / OQ-IP-8 WASM 轉檔 P1。≈0.17 mo | [foundation/image-processing.md](foundation/image-processing.md) |
| **檔案儲存基礎設施(上傳/下載/附件欄)** | F-5 | ✅ **SHIPPED v1.0(2026-07-27;M1–M5 + FMEA S1–S11)**|**解三模組共同硬阻塞**(OQ-FTP-6 image·signature·attachment 上傳 / OQ-PM-1 合併列印範本上傳·PDF 寫回·浮水印)。P0:`StorageDriver` 抽象(local dev/on-prem + S3-compatible 涵蓋 R2·S3·GCS·MinIO,承 docs/11 §3.6 R2 決策 + §16 避 lock-in)+ 伺服器代理上傳(**magic bytes 驗型別**/大小/白名單/**生成檔名**)+ API 代理下載(表單 view + **欄位 hidden 檢查**、`Content-Disposition: attachment`+nosniff)+ `file_object`(0014,RLS 車道)+ 兩階段綁定(pending→bound)+ 租戶配額 + attachment 欄完成(欄型契約不動)。docs/22 上傳鐵則逐條對照。**已解鎖**:attachment 欄可上傳/下載/移除(STUB 移除),OQ-FTP-6 之 image·signature 與 OQ-PM-1 之範本上傳阻塞已除,由各模組續作。api 283 + web 45 + e2e 25 全綠(`file-storage.spec` 3 測)。commit 346f67d(M1)/ bdd8c88(M2)/ ee066d5(M3)/ 2a78956(M4 前端)。**v1.1(同日)**:清 P1 殘留 S7(記錄刪除後附件不可讀)+ S10(開機 schema 檢核)。**P1 殘留**:病毒掃描 ClamAV / presigned 直傳直下 / 縮圖 Sharp / image·signature 欄 / 範本上傳 / 孤兒實體回收 job(歸平台可靠性工程)。migration 0014。≈0.37 mo | [foundation/file-storage.md](foundation/file-storage.md) |
| **框架升版(NestJS 11 + Fastify 5)** | F-9 | ✅ **SHIPPED v1.0(2026-07-29;M1→M4,OQ-FU-1..7 全採建議)**|**程式碼零改動**完成 NestJS 10→11 + Fastify 4→5(4 升 1 移 1 加);`pnpm audit` **47→25**、**fastify 鏈 16→0**、critical 歸零。實走驗證 `/api/auth/*` 萬用字元路由、multipart 上傳/下載串流、錯誤信封無 `FST_ERR` 外洩。**🔴 實測推翻 M0 的頭號風險預測**:cron ×3 在 `@nestjs/schedule@6.1.3` + NestJS 11.1.28 下**不成立**(orchestrator 為靜態 provider 且以物件屬性賦值註冊,同名覆蓋不累積)——M1 改動保留但價值定位誠實下修為「消除對框架去重行為的隱性依賴」。釐清 **`fastify` 必須釘死與 adapter 同版**(11.1.28 釘 5.10.0),宣告 `^` 範圍會在上游發 5.11 時裂成兩份、重演 F-2 期型別重複。**殘留**:prod 需設 Better Auth `trustedProxies`(否則速率限制退化為全域共用桶)/ SIGTERM 時序未實測 / `fast-uri`·`find-my-way` overrides 待上游跟進後移除。以下為 M0 研究紀錄 ——|承 #102 部署前置。**盤查推翻原始範圍認知**:`@nestjs/platform-fastify` 把 `fastify` 當**直接相依且釘死**(10.x→4.28.1 / 11.x→5.2.1)→「升 Fastify 5」實質等於「升整個 NestJS 到 11」;這也回頭解釋 F-2 期「型別重複」踩點為何必須釘 4.28.1。**§0.1 更正 file-storage 的 CVE 歸屬錯誤**:CVE-2026-33806 不影響 4.28.1(其範圍為 5.3.2–5.8.4),真正命中的是 **CVE-2026-25223**(`< 5.7.2`,high)與 **CVE-2026-3635**;結論不變但論據更換,並補判準「引用 CVE 須連同版本範圍一起記」。**§0.3 最痛的 breaking change 暴露為零** —— path-to-regexp v8 只打 Express,本專案無 middleware / 無 setGlobalPrefix / 無 enableCors。**§4.1 揪出最大風險**:`ScheduleModule.forRoot()` 被呼叫 **3 次**,Nest 10 以 deep-hash 去重故現況正常,**Nest 11 改用物件參考 → 每個 cron 跑三次**(每分鐘通知派送 = 重複寄信、每日用量統計 = 計費數字錯),而 **561 個測試結構上抓不到**(單元測試不跑 cron)。ghostfolio 真實升級案的維護者原話:「Automated tests don't cover much in this case」。`pnpm audit` 現況 50 條,其中 **16 條在本模組射程** | [foundation/framework-upgrade.md](foundation/framework-upgrade.md) |

---

## 🟢 R1 · 完整 Ragic 平台(既有客戶遷移 land)

| 模組 | Sprint | 狀態 | 文件 |
|---|---|---|---|
| **動態權限(記錄範圍 + 指派)** | E-1 | ✅ **SHIPPED v1.0(2026-07-29)**|「業務只看自己客戶」已可用:角色 × 表單 × 動作可設 `own` 範圍,`member` 欄勾「指派即授權」即資料驅動指派。**強制點在 `AS RESTRICTIVE` RLS policy**(隨建表 provision,漏注入也不外洩)+ `assignees bigint[]`/GIN + 存取預覽面板。**實作期揪出兩個 P0**:(a) member 的數值 actor id 在填單送出邊界被字串分支**靜默丟掉**;(b) **`APP_DATABASE_URL` 未設時 app 車道回落到 migration 特權角色 → RLS 全面不執法**(租戶隔離一起失效),已補 prod fail-fast + 開機自檢 + dev `x-dev-real-authz` 逃生口。**殘留**:D3 lookup 顯示值不受來源表範圍管(併入 #113)。以下為 M0 研究紀錄 ——|docs/25 覆蓋率彙總後之下一順位:「業務只看自己客戶」是 Ragic 客戶普遍在用的能力,而 **Weyver 目前完全沒有**(表單可見 = 該表所有記錄可見;既有 owner 短路是**表單層**非記錄層)。**§0.1 推翻本模組原本的假設**:docs/04 記為「依欄位值判斷」暗示 Salesforce 式規則引擎,但 **Ragic 根本沒有條件規則引擎** —— 而是「**有序存取層級**(問卷式 / 佈告欄式等,內建『自己新增及被指派的』述詞)+ **member 欄位值驅動的指派**(欄位上一個勾選,且明載仍受表單級層級所限)」兩機制組合。**§0.2 企業級對照**:Salesforce criteria-based **上限 50 條且禁 lookup/公式/衍生欄位**、預計算 `__Share` 表非同步重算(>200 萬列需開 Defer);Odoo `ir.rule` 查詢時注入且 **global AND / group OR**(加第一條 group rule 反而收緊);**Airtable 完全無此能力**(Interface 過濾**非安全邊界**);**Notion 只綁 Person 型屬性** —— 與 Ragic 獨立收斂到同一受限模型。**§0.4**:RLS policy 內子查詢**每列執行一次**、非 LEAKPROOF 破壞 pushdown → **維持只做租戶兜底,動態述詞走應用層注入**。**§0.5 Krebs/Varonis 揭露之 Salesforce Community 外洩**根因為「規則正確但管理員理解錯」且產品無法預覽 → **預覽模擬器列 P0**。**v0.2 三路補研究(含本機 PG 16.13 / 30 萬列實測)推翻 v0.1 核心架構**:(1) **強制點由「應用層注入 WHERE」翻為 `AS RESTRICTIVE` RLS policy** —— 實測兩者**執行計畫完全相同**(BitmapOr + GIN,0.169 vs 0.16 ms)**零效能代價**,但 RESTRICTIVE **語意恆為 AND**,使用者篩選的 OR **語法上不可能逃出**,且**應用層漏注入也不外洩**;實測反例:少一層括號即 **103 倍外洩**(309 列 vs 3 列)。v0.1 據以否決 RLS 的推論對複雜 policy 成立、對簡單述詞不成立,已標 SUPERSEDED。(2) **指派存成固定系統欄 `assignees bigint[]` + GIN**:實測 0.16 ms vs junction + `OR EXISTS` 的 **265 ms**(planner 產生 hashed SubPlan 退化成掃全租戶);jsonb 計畫相同但多一層轉換。更關鍵是它讓**所有動態表共用同一份靜態 policy**(建表 provision 時一次寫入,消除「新表忘了套」),規則變更是**資料變更而非 DDL**。(3) **預覽採唯讀試算不做 impersonation**(「Login as user」觀測性不足易濫用,Varonis);承業界三分類 effective access / simulator / report-only 分階段。**⚠️ 授權警訊**:**NocoDB 與 Directus 2026 起已非 OSS**、Baserow `enterprise/` 專有、Teable `apps/*` AGPL → 一律只讀公開文件不看實作。**另記一個 pre-existing bug**:`record.service.ts:425-434` keyset 分頁在非 id 排序時會跳列/重複(已對碼確認,另立小項)。**相依**:`member` 欄前端未完成(與通知模組撞同一殘留)。≈0.28 mo | [R1/dynamic-permissions.md](R1/dynamic-permissions.md) |
| **記錄修改紀錄** | R1·H-4 | ✅ **SHIPPED v1.2(2026-08-05)**|**v1.2 完成 H-4**:① **資料庫設計變更頁**(Ragic 官方 `doc/81` 逐字「頁面下方,可以看到資料庫設計變更」—— 同一頁的下半部;資料本來就在 `ddl_audit`,🔴 **`executed_sql` 一律不回**:那欄含物理識別字 `t123`/`f456` 與完整 DDL,攤在畫面上等於奉送一份攻擊面地圖,而動態 identifier 注入是本專案第一大威脅)② **批次還原**(官方逐字「點擊該筆修改或匯入紀錄旁的還原符號」;官方截圖把整批折成一列「修改了 4 筆資料 (大量修改) ↺」,版面照它)。批次 id 走**交易範圍 GUC**(OQ-RV-7)—— 批次是交易的性質,與 `tenant_id`/`actor_id` 同類,改走參數就要動三支簽章與六條呼叫路徑。**🔴 一處刻意超出 parity**:官方限制 3 只寫「不建議還原很久以前的大量修改」而不擋 —— 照還原會**銷毀別人後來的工作**,故逐欄比對現值,動過的跳過並回報(不靜默、不整批拒絕,與貼上計算欄 OQ-GP-4 同形)。⚠️ 落地踩到兩個**只有把值從歷史餵回寫入端才會出現**的落差:(a) `numeric` 寫進去是 `50`、讀回來是 `"50.0000000000"` → 比較誤判成「別人改過」而靜默少還原一格 (b) 同一個字串被寫入端的型別驗證擋下 —— **它擋的是它自己剛剛寫進去的值**。以下為 v1.1 ——|**v1.1 收尾**:全庫「資料修改紀錄」頁(Ragic 官方 `doc/81` 明載於「資料庫管理」下,可依表單篩選;**只列動了哪些欄不列值** —— 橫跨數十張表不做逐欄遮罩,用途是找線索,追下去就是那一筆的記錄頁)+ 保留期清理 365 天(**掛進既有的 `trash.purge`** 不另開排程 —— 同一維運動作兩套做法本身就是缺陷)。⚠️ **刻意沒做時段路由(勿擾)**:查證後 Ragic 沒有這個功能,它不是 parity 而是我方自己加的舒適功能,不該用「H 還有一列沒收」當理由去做。以下為 v1.0 ——|Ragic 使用者每天在看的「這筆單子被誰改了什麼」,我方原本**完全沒有** —— 只知道 `updated_by` / `updated_at`。🔴 **一手依據推翻了一個預設**:我原本要做「單筆還原」,而 Ragic 官方 `doc/81` 明文**只給大量修改與匯入還原**(六條限制裡有三條在講單筆還原會與公式 / 連結載入 / workflow 重算打架)→ P0 收斂為**看得到**,不做單筆還原。**寫在唯一咽喉**(`insertOne` / `updateOne`)而非各呼叫端:這一輪已經數過,橫切關注點掛在單筆路徑上就會被繞過(索引漏三次、事件漏兩次),驗收就是**列舉所有寫入路徑**的測試。只存差異不存快照(Ragic 逐字「列出**詳細的修改內容**」= 差異視圖;快照成本隨欄數 × 筆數相乘)。**逐欄遮罩**:隱藏欄的歷史值就是隱藏欄的值 —— 這一輪修過三次同型(公式污染閉包 / 連結標題 / 通知內容),**值只要有第二個出口就會漏**,而歷史正是最容易忘的那個。與 `ddl_audit` 不同**開 RLS**(那兩張存動作,這張存值)且 app 車道只給 SELECT/INSERT。⚠️ 落地踩到:`drizzle-kit generate` 把全部表重建了 —— 本專案 migration 是**手寫**的,meta 停在 0030。**殘留**:保留期清理(P1,**不是可選項**,高頻表一年可達數百萬列)· 全庫管理頁 · 批次還原。 | [R1/record-revisions.md](R1/record-revisions.md) |
| **通知系統(訂閱 / 提醒 / 通道)** | H-1 | ✅ **SHIPPED v1.0(2026-07-28;M1–M5 + FMEA N1–N17)**|**解當前最大功能斷裂** —— 簽核流程已 SHIPPED 卻無任何機制告知下一關的人。**緣由**:docs/25 v1.5 覆蓋率彙總指出 H 段僅 12%,且**簽核已 SHIPPED 卻無機制告知下一關簽核人** —— 做完的功能因缺另一功能而不可用,為當前最大功能斷裂。**§0 Ragic 證據**:通知三層結構(全域開關 / 逐表單四開關 / **事件×通道矩陣**)全由終端使用者自設;「跟我相關」官方定義 = 新增 + 指派 + 回應;共通篩選訂閱有**進入 / 離開**兩種觸發;**批次匯入與大量修改刻意不通知**(風暴防護,明載);提醒為宣告式(日期欄 + N 天前後 + **收件人來自欄位值** + 每日排程 + 同天同收件人**預設合併**)。**競品分歧**:Airtable 把通知做成 automation 的 action 且 **Editors 只能檢視設定** → 與本專案自助化命門衝突,故採 Ragic 訂閱模型。**§0.3 實查出改 scope 的相依**:「跟我相關」三要素中 Weyver 僅 `createdBy` 成立 —— **member 欄型無前端渲染、註解功能完全不存在** → OQ-NT-2。P0 = 站內通知中心 + Email + 三層訂閱設定 + 簽核接通 + 風暴防護;**提醒 / LINE 等通道 / 註解 明確排除並附理由**(提醒需 cron 地基,C 段整項未起)。**v0.2 補 §4 外部通道設計**(v0.1 僅一行「通道抽象」不足以決策),並**推翻 v0.1 自身判斷**:原估「加 LINE +0.1 mo 小批」錯誤 —— 成本在**綁定流程 + inbound webhook + 租戶憑證**而非送訊息 API;結論(P0 不做 LINE)不變但理由改為「**它大到應自成模組**」而非不重要。Ragic 綁定流程:加官方帳號好友 → LINE 內傳 `/validate|信箱` → **寄驗證信** → 15 分鐘內點連結完成(**驗證刻意繞回 Email 防冒用**)。**Weyver 無計費載體(訂閱計費 Phase 2)→ 只能走租戶自帶憑證**,確認 docs/04「通道連接設定 UI 1 人月」為 LINE 硬前提。**多租戶陷阱**:LINE userId 為 **Provider 作用域** ⇒ 綁定鍵必須含 `tenant_id`,否則跨租戶洩漏。§4.5 立五項抽象約束(收件人存 user_id 非位址 / 綁定獨立 / 憑證屬租戶 / 內容結構化 / **inbound 為一等公民**)。**v0.3 補 §4.6 個人 vs 群組**(決策方 review 揪出 v0.2 缺漏 —— **docs/04 §H 早已明載「個人 1:1 + 群組」且註明「台灣 SMB 日常 LINE 群組通知」**,v0.2 只設計個人綁定):兩者**非同一功能的兩種位址而是兩種功能**(收件人 / 訂閱者 / 權限模型 / 退訂 / 審計語意五面皆異)→ (a) 群組屬「事件廣播」不屬「通知訂閱」,設定入口在表單·租戶層,歸 docs/04 通知路由引擎;(b) **OQ-NT-9 對群組升級為不可協商**(群組廣播無任何權限模型可依靠);(c) **§4.5 第 1 條修正為多型 recipient** —— v0.2 原寫 `recipient_user_id` **是錯的**,群組通知沒有收件使用者。新增 OQ-NT-13/14 + FMEA N12(群組廣播洩漏 P0)· N13。**UI 裁定**:LINE 欄 P0 不顯示(無 driver 亦無連接頁 → 死控件)· 全部通知只做面板 50 則 · 設定放個人設定 · **簽核逾期豁免總開關但須明示**。**v0.4 §0.4「站在巨人的肩膀上」四路研究,推翻自身四項決定**:(1) **訂閱模型改單一有序 enum** —— GitHub/GitLab/Discourse/Zulip/Notion/Slack/Teams/Linear **無一例外**,且查不到任何系統從 enum 退回獨立開關(Zulip 反由二元→四檔);Ragic 4 開關 = 16 組含「別人建的通知、自己建的不通知」等無意義組合;**「與我相關」在成熟系統是自動訂閱地板非開關**;(2) **通知與寄送必須分兩張表** —— Discourse/GitLab/Novu 三家皆分,生命週期 / 寫入模式(dead tuple)/ 扇出 / 保留策略四者衝突;PostgreSQL-only 仍正確(pilot < 50 msg/s 遠低於實測 800 jobs/s 死亡螺旋);(3) **同記錄去抖動必做**(Jira 3min idle / 10min max + 關鍵事件 bypass),否則連續編輯 10 次 = 10 封信;跨記錄 digest 才可不做;(4) **自架 SMTP 在雲主機基本不可行**(IP 段被封 / port 25 封鎖 / 低量養不熱 IP)→ relay 共用 pool,**與 OSS-only 不衝突**(買 IP 信譽非軟體授權)。**OQ-NT-9 理由改寫**:真論據為「**欄位級權限使業界主流的收件人過濾失效**」(Jira/GitLab 無欄位級權限),佐以 CVE-2019-11544 / CVE-2021-39119 / CONFSERVER-52560。**🔴 FMEA N14(P0,已對碼驗證)**:`titleOf()` 取 `fields[0]` = 使用者自建任意首欄 → **通知標題本身即可能洩漏金額 / 身分證號**。N17:**PgBouncer tx mode 下 LISTEN/NOTIFY 不可用 + RLS FORCE 擋 worker 跨租戶掃描**,M1 第一件事確認。**落地**:四表(notification / delivery / pref / setting,皆 RLS FORCE)· 有序 enum 層級 + **沿用既有分類資源軸繼承**(不新建基礎設施)· 顯式 emit 風暴防護 · `safeTitle()` 型別上不接受欄位值 · 站內鈴鐺(Discourse 式部分索引算未讀)· Email 通道(relay / **抑制清單 P0** / 退避重試 / **同記錄去抖動** / `References` threading)· 三軸設定頁。**實作期揪出五個缺陷**:指名收件人被角色過濾而**丟掉無角色的送簽者** · `scope_id` 為 NULL 使唯一索引失效致**偏好每次修改都新增一列** · 應用時鐘與 DB 時鐘不一致使**去抖動永不到期** · 通道偏好誤做成整批共用 · `engineFetch` 無 body 的 POST 仍宣告 content-type 致 500(共用層,順修)。**殘留**:N8 送達率部署設定(SPF/DKIM/DMARC/VERP bounce 管線,**pilot 前必做,否則通知等於沒送出**)· N9 保留策略 · 提醒(待 cron 地基)· LINE 等通道(自成模組)· 註解 @提及。api 391 綠 / e2e 44 綠。migration 0018–0020。UI 稿 [notification-flow.html](../mockups/notification-flow.html)。≈0.33 mo | [R1/notifications.md](R1/notifications.md) |
| **匯入既有表單(upsert + 撤銷)** | #106 | ✅ **SHIPPED v1.0-M3(2026-07-29)**|**§4.2 決策表與 §4.3 schema 已全數補完**:命中多筆改為擋(原本 `Map.set` 直接覆蓋 = 文件明文否決的 Airtable 式靜默全部更新;**unique 約束擋不住**,因比對用的是正規化後的值)· 正規化命中警告 · 大量影響二次確認 · clear 打字確認**後端也驗**(前端有對話框但沒送後端,API 可繞過)· 撤銷保留期 30 天 · Idempotency-Key。**第三次「整合測綠、瀏覽器才爆」**:`approval_instance` 走 app 車道但該表刻意無 grant → 改走特權車道(反向注入 ActionsRepository 會造成模組循環)。以下為 M2 ——|四政策 upsert / 決策表 / plan 是 dry-run / commit 帶 planHash 防「看的是 A 檔送的是 B 檔」/ 撤銷為補償批次(before-after diff + per-field compare-and-set)。**M2 補**:未知選項 `create` 真正生效(原本 schema 收但不實作 → 使用者選了會整批失敗)· 合併儲存格填滿並回報(舊 Excel 單頭欄必然合併,原本靜默變空白)· 錯誤列 CSV 下載含注入防護 · **撤銷 UI**(原本有 revert 端點但沒有清單 → 使用者無從得知 batchId,功能等於不存在)。**子表語意維持 untouched**,append/replace 待真實客戶檔案樣本再設計。以下為 M0 研究紀錄 ——|**R1「既有客戶遷移」定位的硬破口**:Ragic 官方的匯入主入口是「既有 sheet → Tools → Import Data From File」,遷移後客戶**每天在做的是匯入既有表**,而本平台只做了「Excel → 建**新**表單」。**§0 研究推翻兩個原本判斷**:(a) 最會靜默毀資料的**不是**「檔案中沒有的欄位」—— NetSuite / Shopify / Airtable / Baserow **一致保留未映射欄位**;真正的戰場是**「有映射但儲存格空白」**,而業界在此分裂:Salesforce / NetSuite **預設保留**(安全),**Airtable 預設覆蓋、Shopify 覆蓋且無開關**(官方逐字「overwritten as blank」)。(b) 原本的 `import_batch_id` + soft delete 撤銷設計有 **5 個實質缺口**,最大兩個是「**更新型變更無 before-image 撤不回來**」(而遷移場景絕大多數是更新)與「**batch_id 掛在記錄上會被第二批覆蓋**」→ 改為側表 `import_batch_rows` 存 before/after **diff** + per-field **compare-and-set** 偵測「匯入後又被人改過」。**其他關鍵發現**:業界**無一家做原子交易**(Salesforce/NetSuite/Odoo/Shopify 全部分提交)→ 護欄是 dry-run + 可撤銷,不是 rollback;**檔案內 key 重複會讓 PG `ON CONFLICT` 直接爆錯**(cardinality violation),應用層必須先擋;**Airtable 擴充在既有命中多筆時「全部更新」且靜默**(官方逐字),**絕不採用**;**key 必須有 unique 約束**且由 DB 強制(NocoDB #3438 正是真實表架構下未強制而讓主鍵重複);**業界無一家做 no-op 偵測**(值沒變仍寫入,污染 `updated_at` 與稽核)→ 低成本差異化。**競爭切入點**:🔴 **Ragic 有 revert 但 revert 完資料是不一致的** —— 官方明載還原**無法復原且不會重新觸發**連結與載入 / 公式重算 / workflow。**Excel 本身是資料破壞源**:基因名事件 30.9% 論文受影響且五年未解;英國 PHE **15,841 筆確診因 XLS 列上限被靜默丟棄**致接觸者未匡列 → preflight 主動偵測前導零流失 / 科學記號 / 日期誤轉是業界共同破口上的差異化。**OQ-IMP-6 為推翻既有 OQ-GEI-3=A 之提案**(前端解析 → 後端;Airtable 的 25,000 列上限正是前端解析的代價,SheetJS 官方亦建議大檔在伺服器處理)。**前端解析的三處靜默吃錯資料已先修**(e068c6a:寫死首工作表 / 寫死第一列當標題 / 推斷只取前 50 列)| [R1/import-to-existing-form.md](R1/import-to-existing-form.md) |
| **簽核進階語意(動態簽核人 / 會簽擇辦 / 加簽 / 退回 / 不可竄改)** | R1·後續-1b | ✅ **SHIPPED v1.1(2026-08-04;前端落地缺口已補)**|🔴 **2026-08-04 補的是一個「已裁定、後端已出貨、但使用者碰不到」的缺口**(§3-ter)。§3-bis 的狀態欄逐字寫「✅ 已裁定 C 並落地(2026-08-03)」—— **那句話對後端成立,對整條路徑不成立**:(1) 設計器的簽核人規則清單**沒有 `fieldRef`**,只能打 API 建,而第一約束逐字說「有 API 可以做」不算解決 —— 偏偏 `fieldRef` 正是 Ragic 遷移的落點,R1 的整個目的;(2) 前端 `APPROVER_RULES` 鏡射也漏了它,後果比選不到更糟 —— 用 API 建過的租戶 `z.enum` 解不到 → 整個 def parse 失敗 → **簽核設定區塊整塊消失**,而同一個檔案上面的註解正好警告過這個形狀;(3) 三個 manager 規則**可選但恆解不出人** —— 它們靠角色樹的父子關係,而角色頁 UI **刻意是平的**(`authz` 0-bis),從畫面建角色的租戶永遠沒有父角色,選了要等**送簽當下**才炸 → 改為停用並在選項文字寫「(尚未建立角色階層)」。另修一句**指向不存在之物的宣稱**:doc 寫著繞過風險「已列為 SOP 的必填項」而當時**根本沒有 SOP 文件** → 警語移到**設計器裡選 `fieldRef` 的當下**(唯一擋得住繞過的地方是那裡,不是文件),並補 §3-ter-bis 遷移 SOP 三步驟。⚠️ **刻意沒做**:沒為了讓 manager 規則可用而開角色階層 UI —— 那條「刻意平掉」的裁定有自己的理由,不該為本模組單方面推翻。以下為 v1.0 紀錄 ——|**🔴 瀏覽器實走抓到兩個靜態層攔不住的 P0**:(a)**會簽的分母把送簽者算進去** → 角色 3 人其中一人是送簽者時那一關**永遠不可能通過**,畫面停在 2/3 不動且無任何錯誤訊息(整合測沒抓到,因為它的角色成員裡剛好沒有送簽者);(b)**駁回按鈕一直是壞的** —— UI 送 `reject` 不帶理由而後端自 #103 起強制必填,型別對、lint 過、後端整合測也綠。前端另修「退回/駁回」詞衝突與 zod 必填造成的整塊消失。**後端狀態機**:hash chain 偵測層(0048)· 動態簽核人(role tree 推導)· 會簽/擇辦 · 臨時加簽 · 退回指定關 · 顯式解鎖 + admin 改派。**🔴 五個 milestone 裡有四處推翻了 M0 自己的裁定**:(a) `decide()` 的 race 早就修好了、(b) append-only 的 DB 層防護早就做完了(0021)—— 這兩條是寫 M0 時採信 §0-bis 舊記載沒去對碼,「巨人的第一站是自家 repo」的第二次;(c) quorum「未填 = 全體同意」(Ragic 退化式)**一寫下去整合測全紅** —— Ragic 那個設計成立是因為它的簽核對象是刻意挑的**群組**,而我方是**角色**,50 人的角色預設全簽既荒謬也會讓既有流程一夜卡住,改為三態顯式;(d) 「admin 永遠可編輯」與同一題「解鎖必須留痕」**自相矛盾**,靜默 bypass 不留任何「有人在簽核中改了單」的紀錄,而 dev 車道人人 superadmin 等於把鎖整個關掉 → 改為顯式解鎖 + admin 改派。**退回後全部重簽**由「只算最後一次退回之後的核准」達成,不另存狀態 —— 少了它,退回第 1 關後第 2 關會因上一輪核准直接通過。api 1003 綠。以下為 M0 研究紀錄 ——|承 actions-approval §0-bis 之 P1 第 7–11 項(代理人已交付)。**🔴 對碼查證推翻一個隱含前提**:本專案**沒有任何「主管」關係**,只有用於權限繼承的 role tree —— 動態簽核人不是「接一個欄位」,而是「要不要引入第二份組織結構」的架構決定(兩份組織結構必然分岔)。**一手查證五題**:Ragic 三種動態解析為 **parity 基準線非進階**;**解析失敗業界一致硬失敗、無人做 fallback**,Salesforce 更把「離職」與「空值」同等對待且**在簽核人回應時才炸**;Ragic 的「**擇辦人數清空 = 會簽**」是最好的 N-of-M 模型(All 是 N 的退化值,結構上不可能矛盾);Ragic **加簽三分法**明載「向前加簽會暫停目前簽核動作」,而 **ServiceNow 社群明文警告**執行期插入關卡會破壞狀態機一致性;**退回後全部重簽是業界唯一預設**,Kissflow 自承是痛點;**簽核紀錄 hash chain 六家皆無**,Odoo 有但用在會計分錄且附**鏈完整性檢查報告**(讀取端可自證),為唯一可借用之 OSS 先例。≈0.5–0.7 mo | [R1/approval-advanced.md](R1/approval-advanced.md) |
| **表單範本庫** | R1·B-2 | ✅ **SHIPPED v1.0(2026-08-03;M1–M5)**|⚠️ 本欄一度停在「M0 DRAFT — OQ-TPL-1..8 待裁定」,而模組文件 §13 早已記出貨(audit-D §4)。🔴 **殘留**:OQ-TPL-6=C 裁定的 `template_key` / `template_version` 溯源欄位**零落地**(drizzle 0001–0051 無此欄、全 repo 命中 0),而該裁定的理由逐字是「**現在不記以後補不回來**」—— 每套用一次範本就少一筆溯源。以下為 M0 紀錄 ——|`docs/04` B 列了 2 人月但**全庫無實作**;對不會寫程式的使用者,空白畫布與「訂便當系統」範本是完全不同的兩件事。**🔴 四家競品一致(強證據):範本的單位是「容器」不是「表」** —— Ragic「大多模組包含多張表單,且彼此的連結關係已經建立好」· Teable `create-from-template` 以 base 為單位 · Baserow「one or more applications」· Airtable template = 整個 base。這**推翻本模組原本「單表範本 + 套用後重指」的直覺**,改為範本包 + 包內相對代號,而**容器邊界正好解掉 link 重指的難題**(我方 `targetFormId` / `pushTo` / `approverRoleId` / `view_def` 全存真實 id,重映射才是主要工程量)。**🔴「各家都不處理範本更新」的假設只對一半**:Airtable 企業版明說會做,但代價是套用後配置**被鎖**(「its configuration is locked」)→ 承重結論「**要能更新就必須先鎖**」,而鎖與我方「客戶自己建自己改」的定位相衝 → 採「先脫鉤但記錄來源與版本」(現在不記以後補不回來)。範例資料採 Teable 的 `withRecords` 布林 —— Airtable 一律帶且清除入口**藏在一次性側欄**,官方自己得補 workaround。≈0.4–0.6 mo | [R1/form-templates.md](R1/form-templates.md) |
| **表單引擎動態 schema 核心** | P0-1 | ✅ **SHIPPED v1.0**(2026-07-19;M0–M7,59 tests,FMEA P0 全清)| [R1/form-engine-core.md](R1/form-engine-core.md) |
| **表單設計器 + 填單 接引擎 API** | P0-1·UI | ✅ **SHIPPED v1.0**(2026-07-19;M0–M5,Playwright golden path 固化;**Gate P0-1 全數達成**)| [R1/form-designer-ui.md](R1/form-designer-ui.md) |
| Grid 主檢視 + Excel 建表 onboarding | P0-2 | ✅ **SHIPPED v1.0**(2026-07-19;M0–M4;Glide 網格改格 + xlsx 推斷建表 bulk 灌資料;e2e 固化 + FMEA P0 全清)| [R1/grid-and-excel-import.md](R1/grid-and-excel-import.md) |
| **網格貼上 Excel 區塊** | P0-2 殘留 | ✅ **SHIPPED v1.0(M0–M5)**|客戶離開 Excel 的第一理由。**🔴 §0 一開始整節沒寫,被 review 點出後補,而補的時候發現最大的巨人是腳下的套件**:Glide 逐字「`onPaste` 為 true 時 split the data by tabs and newlines」「`onCellsEdited` provides all edits inbound as **a single batch**」「**will not attempt to add additional rows**」→ 解析與批次都是免費的,**加列才是真正要手工做的那塊**。**競品研究把猜的數字換成有出處的**,而 M5 又把它換成**自家實測**:2.2–3.5 ms/列近線性,500 列外推 1.1–1.8 s —— 外部數字會變,量測不會。**最大反面教材**:超量整批不做且不出聲,四家四種形態,共同點是「使用者看到成功、系統其實少做了事」;本模組每一段 UI 都在回答「少做的事有沒有講出來」。**e2e 抓到三個型別檢查抓不到的 bug**:undo 把 PG 回的 `numeric` 字串原封寫回(**同形狀第三次**)· `query.q !== ""` 恆真導致永遠不給加列 · Glide 走 `navigator.clipboard.read()` 故測試不授權就整條路徑不執行。**殘留**:加列與更新跨兩個 tx(偏離 OQ-GP-1;順序選「先建列」因失敗態看得見)| [R1/grid-paste.md](R1/grid-paste.md) |
| **欄位輸入的無障礙名稱** | R1·A11Y | ✅ **SHIPPED v1.0(2026-08-04)**|填單與記錄頁的欄位輸入在無障礙樹上**沒有名字** —— 視覺欄名是**旁邊那一格的 div**,與 `<input>` 無任何程式關聯,螢幕閱讀器只唸「編輯文字」(WCAG 4.1.2)。**起因是 e2e 找不到穩定錨點** —— 👉 測試找不到,通常代表使用者也找不到。🔴 **自家量測推翻了原本要採用的方案**:我因「`display:contents` 會把元素移出無障礙樹」的舊印象準備排除 `<label>`,實測在現行 Chromium **完全可用,而且多給了「點欄名聚焦輸入框」** —— `aria-labelledby` 沒有這個好處且要為每欄產 id。⚠️ **印象裡的瀏覽器 bug 有保鮮期。**站① 關鍵:label 格與值格本來就在**同一個共用原件**(`field-grid.tsx` 的 `Cells`)→ **單一落點**,不必改 27 處;且填單與設計畫布**只共用格子不共用容器**,故不會動到 12 欄座標系統。站③ W3C 名稱計算逐字:`placeholder` 落在第 ⑤ 級(Tooltip,「只有其餘都沒有結果時才用」)→ **任何正確修法都會蓋掉 placeholder,波及面不可避免**(已量:8 支 spec 靠 placeholder 或整頁 `.first()` 錨定;直接加 `aria-label` 實測造成 27 條紅)。⚠️ **只量了 Chromium**,Firefox/Safari 未量,已列 FMEA A4。🔴 **落地後 §5-bis 回填 4 條 M0 沒預見的失效,3 條實際發生**:(A6,P0)**巢狀 `<label>`** —— 附件 / 圖片的輸入元件自己就有 `<label>` 包 `input[type=file]`,外層再包是無效 HTML,實測**上傳整個失效**,4 支 spec 同時紅 → 加 `noLabelWrap` 逃生口;(A8)`wysiwyg-parity` 兩處用 `firstElementChild` 取標籤格,`display: contents` **不佔版面但是一個 DOM 節點** → 量到 0 個欄位格 / 寬度 0,而錯誤訊息寫「選取器可能失效」指不到原因;(A9)一時把說明鈕也 `aria-hidden` 掉,違反 M0 §1.2 自己寫的範圍,說明文字變不可及。**共同形狀:站①只查了「在哪裡渲染」,沒查「誰在依賴這段 DOM 的形狀」。**順帶修掉兩條無關的既有缺陷:`data-export` 因後端每日上限 10 + 清單上限 20 而**一天只能跑十次**、`approval-advanced` 的狀態章與提示訊息**文字一字不差**導致 strict mode 無規律撞兩個元素(**文字是給人看的,不是識別碼**)。e2e 143 綠 / web 243 綠 / lint 4-4 綠。≈0.05 mo | [R1/field-label-a11y.md](R1/field-label-a11y.md) |
| **連結欄選記錄 + Load 帶入** | R1·LNK | 🟡 **M1 SHIPPED(2026-08-04)· M2 Load 未做**|承 `_audit/giants-shoulders-audit-C.md` §2.2。🔴 **連結欄在前端本來就是 stub** —— `STUB_TYPES` 逐字 `["link"]`,填單畫面顯示「此型別即將推出,暫不可填」,而 `formula-and-linkload` 檔頭同時寫著「Link&Load SHIPPED」。**UI 一直說實話,是模組文件在過度宣稱。**M0 的關鍵題「Load 在前端還後端」由 **Ragic 官方 `doc/14` 逐字直接回答**:「在**未儲存**的情況下變更或清除任一連結欄位的值……」—— 講到未儲存狀態,那只可能是前端,故現有 `RelationService.load()`(伺服器端組值)形狀不對,**不接上主路徑**。M1 交付候選端點(🔴 **跨表單權限必查** —— 來源表單的權限不蘊含目標表單的權限,候選清單天生是「把整張目標表念出來」的端點,漏檢就是掃描器;沿用 `WidgetService` 已驗證的範式)+ `LinkInput`(沿用 `MemberInput` 形狀)+ label 對隱藏標題欄回 `#id` 而非空白。🔴 **實走揪出送出邊界靜默丟值**:`toSubmitValue` 沒列 `link` → 畫面上選了供應商、存進去是 null 且無錯誤;`member` 欄踩過同一個坑(#96)且註解逐字寫著,**而規則只寫在註解裡就是會漏**。⚠️ 試過補齊欄位輸入的 `aria-label`(WCAG 4.1.2,它們在無障礙樹上沒有名字),**量到波及 27 條 e2e** → 判定為獨立的 a11y 工作,已還原不夾帶。api 1054 + web 243 + e2e 全綠。 | [R1/link-picker-and-load.md](R1/link-picker-and-load.md) |
| 公式引擎 + Link&Load | P0-3 | 🟡 **公式側 SHIPPED v1.0 · Link&Load 的 Load 半邊未接線**(2026-08-04 更正)|🔴 **原記「M0–M6」是過度宣稱**:`_audit/giants-shoulders-audit-C.md` §2.2 查出 `RelationService` **零個生產注入點** ——`load()` 唯一呼叫者是 `lookup()` 自呼,而 lookup 的生產實作在 `record.service.ts:336` 與它無關;`field-input.tsx` 也沒有 `case "link"`(link 欄落到 default,要使用者自己打 id)。**這是「沒有 reader 的欄位」的上一層形態:沒有 caller 的 service** —— 更難發現,因為它有整合測試而且會綠。👉 `SHIPPED` 的判準不能是「方法寫了 + 測試綠」,要能回答「從使用者的哪一個動作走得到這裡」。✅ **確實在跑的**:公式 parser(fork Teable MIT)+ Tarjan SCC 依賴圖 + 讀時算注入 + Rollup(N+1 safe)+ 設計器啟用 + 即時預覽 + e2e。⚠️ Load 的落地**先要一個 M0**:Ragic 語意是「選到記錄的當下把值帶進表單、存檔前還能改」= 前端行為,而現有 `RelationService.load()`(伺服器端組值)形狀就不對。舊述 ——| [R1/formula-and-linkload.md](R1/formula-and-linkload.md) |
| **三層權限(授權層)** | P0-4a | ✅ **SHIPPED(M1–M7 + FMEA + 管理 UI)**|表單級 **動作集**(view/create/edit/delete/approve/export/design,M7 由 4 級升級,Guard 執法)+ 欄位級遮罩/寫白名單 + **role tree 部門繼承**〔OQ-1=C〕+ owner→admin 對映 + 權限管理 API + **管理 UI `/app/settings/permissions`**(角色樹+動作矩陣+欄位可見性,瀏覽器驗證往返);deny-by-default;縱深(RLS 仍為跨租戶最後防線);後端 163 tests 綠、§12 FMEA P0 全清。記錄級延 P1-I | [R1/authz.md](R1/authz.md) |
| **↳ 資源軸繼承(分類授權 + owner + 敏感旗標)** | P0-4a·uplift | ✅ **SHIPPED v1.0(2026-07-24;M1–M4 + FMEA)**|解逐表列舉維護膨脹(`O(表單)→O(分類)`):既有 `form_permissions` 重定位為**覆寫層**,其下補**分類授權層** `category_permissions` + owner 短路(`form_def.created_by`,**得資料動作 design 除外**)+ 敏感旗標(不吃繼承,admin-only)+ 租戶預設 profile(Salesforce OWD 式)+ 無權表三態(readable/鎖定 stub/敏感隱藏)。migration 0008 純加法惰性零回歸;分層解析 owner→覆寫→分類繼承→預設;管理 UI(FormMatrix 分類分組 + ResourceSettings + 工作台鎖定 stub);後端 40+ tests + 6 web e2e 綠(`permissions.spec` 固化)。**向上設計研究**(Notion/Salesforce/Drive/Purview,§10-bis)錨定 OQ-ARI-1..8。commit `4c9b76b`(後端)| [R1/authz-resource-inheritance.md](R1/authz-resource-inheritance.md) |
| **↳ 記錄工作台收斂(app-shell + 集合視圖 → Object Page)** | R1·workbench-uplift | ✅ **SHIPPED v1.0(2026-07-28;M1–M4 + FMEA E1–E3/U1–U4)**|**A0/A1 已由 workspace-ia 與 views-list 交付**(v0.5 對照程式碼查證重整;OQ-RWB-1 nested 路由被 views-list 之 nuqs mode/rid 取代)。本輪補完 A2–A5:`/api/users/lookup`(只回同租戶 role_members 之 {id,name},不回 email)+ `/records/:id/relations` 反向關聯(**來源改由 `field_def.options->>targetFormId` 推導** —— `relation_def` 僅於 registerRelation 時寫入會漏)+ 狀態章(首個 singleSelect;tone 只認明確設定之語意色,不臆測)+ 金額彙總 + 真實使用者名 + 關聯 rail 正反雙向 + **inline 檢視↔編輯**(不再跳設計器)+ 清單列 status/money enrich。api 311 + web 45 + e2e 28 全綠(`record-workbench.spec` 3 測)。commit 03e3175(後端)+ fb2104a(前端)。殘留:U3 狀態欄治本(OQ-3=B 設計器指定)/ E3 反向關聯單一 UNION / 選項顏色設定 UI(field-types-parity P1)|承 form-designer-ui;現況 Object Page 已誠實建好 ~70%(master-detail + 錨點 + label/value + 子表 rollup + 稽核)。補 R1 缺口:**A0 app-shell 密度**(全域 status bar + 首頁卡牆改工作面,回應「頁面單薄」;M1 先做)+ **① 集合視圖**(browse 網格)+ 狀態章 / 金額彙總 / 關聯 rail(正+反向,需小後端)/ user 名 / inline 編輯;OQ-6=C 單域 rail+status bar;R2/工作流(GL/簽核/批號)不放。UI 依 `docs/mockups/weyver-integrated-list-to-object.html` | [R1/record-workbench-ui.md](R1/record-workbench-ui.md) |
| **前端重構(視覺 / 心智模型 / 操作體驗 / UI / UX)** | R1·UX-1 | ✅ **SHIPPED v1.0(2026-07-31;M1–M12 + FMEA)**|**橫切品質模組**,非新 surface。**五輪研究錨定,證據強度逐條標注**。⚠️ **推翻我方兩處既有依據**:「專業 vs 玩具」**無權威可操作拆解**(Linear/Superhuman/Height/Sigma 皆無公開設計系統)· 「近乎無動效」**查無主流系統明文宣告**。✅ **反佐證**:全框線 vs 斑馬紋(A List Apart n=2,276)。**照抄的正典**:W3C ARIA APG 鍵盤 · Carbon 列高/動效 token · Cloudscape 密度規則 · WCAG 1.4.12 可執行驗收。**交付**|M1 docs/14 升 v5.0(校正**三處會誤導施工的過期內容**,含 §2.4 與 §0.1 v3 正面矛盾)· M2 導覽 13→7 項可展開標籤 + S22 設定中心(**同批把六項全加進 ⌘K**,否則會同時斷掉點擊與鍵盤兩條路)· M3 `builder/_components` 33 檔→6 folder + **8 檔跨 surface 共用原語移出私有區** · M4 首頁「最近使用」(安全性由建構保證)· M5 子表 **ARIA APG grid** + 清單 **listbox** · M6 密度誤套 34 處 + **WCAG 1.4.12 進 CI** · M7 載入改保留內容 + 延遲細進度條 · M8 **Carbon productive motion token** · M9 對比三條 + **對比進 CI** · M10 **字階 17 種 551 處→6 階** + 字級白名單進 CI · M11/M12 FMEA + VisAWI-S 量測協定。**🔴 六次自我更正**(rail 定位誤判 / 動效數字憑印象 / grep 漏 packages / **研究方向寫反** / 「待我簽核」早已實作 / M5 目標指到唯讀表),共通型=**規格層的詞與程式碼的實際形狀不是同一套詞彙**,只有實作前對照才會浮現。**FMEA 實際發生 4 條**(U1 部分/U4/U7/U11/U12)+ **設計階段未預見 3 條**(批次 rename 撞名 · **token 靜默失效**(Tailwind 4 命名空間錯 → class 不產出而 type-check/lint/build 全過)· Hooks 規則違反)。**U11 為 P0 且真的發生**:字階收斂使可見列數 14→13,依 Fiori「密度靠間距不靠縮字」以 padding 回收 → **15 列**(比原本更密),並轉為常設守衛。**web 126 單元 · 全 e2e 71 綠**(4 失敗為既有基線,零新增)。⚠️ **docs/25 覆蓋率誠實不變**(仍 46%)—— 品質模組不新增功能覆蓋,不灌水 | [R1/frontend-uplift.md](R1/frontend-uplift.md) |
| **設定中心(租戶 / 個人 / 成員 / 帳號安全 / 通知通道)** | R1·A-1 | ✅ **SHIPPED v1.0(2026-08-01;M1–M6 + FMEA)**|docs/04 v2.5/v2.6 兩次審計補列的 S22 —— 該節時間戳寫得很直接:**「『設定/管理』surface 從未列項」**。**交付**|M1 租戶 / 個人設定(**兩軸時區**:業務日界屬租戶不可覆寫、顯示時區屬個人;動態繼承)· M2 使用者管理(**管理員建帳號 + 系統產生 15 字一次性初始密碼**,ASVS §V6.4.6 反對管理員知道使用者密碼;停權**擋登入**〔刻意不照 Ragic〕)· M3 帳號安全(裝置清單 / 強制登出**連帶撤 API 金鑰** / 認證稽核留 6 個月 / 密碼 63B-4 單因子 15 字 / 外洩·情境字比對)· M4 通道連接(**應用層信封加密** KEK 包 DEK、憑證只回布林旗標、固定 host 走 allow-list)· M5 **租戶級事件廣播**(群組 ≠ 個人訂閱:一事件一則、內容不含欄位值)· M6 FMEA 逐條回查程式碼。**🔴 查出並修掉五個既有缺口**:登入限流可被 `x-forwarded-for` 繞過(實測輪換假 IP 打 12 次 **0 次被擋**)· 初始密碼可永久使用(`used_at` 從未寫入)· 安全機制只在 prod 生效(dev 走另一條 guard,e2e 實測新人直接進工作區)· 暴力防護主詞從 account 誤植為 IP · **帳號鎖定固定時長本身即 DoS 工具**(OWASP 明文點名,改指數退避)。**LINE Notify 已於 2025-03-31 終止服務** → 走 Messaging API,照舊資料寫會一上線就死。api 876 · e2e 97 綠。殘留十項見 §6,**只有 R1(log redact)標為建議先補**。≈0.5 mo | [R1/settings-center.md](R1/settings-center.md) |
| **工作區 IA(分類目錄首頁 + app-shell)** | R1·UP-1 | ✅ **SHIPPED v1.1(2026-08-04;IA 第二階段)**|**第二階段**(§14,承 docs/33 OQ-IA-3=A):公開表單與通知的**面板本身**搬進表單層,不再深連設定中心。🔴 **一手研究翻掉了原本要做的一半** —— Ragic 使用手冊 doc-user/12 逐字「表單個別通知 —— 在表單的**工具**選單中」,而同一份文件另記個人設定裡的「頁籤個別設定」:**同一筆設定的兩個入口,不是兩份各存一份**。OQ-IA-3 否決雙入口的理由(改哪個才算數)只在後者成立 → 設定中心那一半改為「下拉 → 逐表單清單」而非刪掉(§2.1 抱怨的一直是下拉;一次調整多張表單的訂閱是清單才做得到的事)。🔴 **`docs/33` P4 推導出的兩個閘門都錯**:公開表單應為 **admin**(後端逐字「租戶級的安全決定,不是表單級功能」,用 design 會顯示按下去 403 的入口)· 此表單的通知**不該有閘門**(`notification_pref` 帶 actor_id 是個人訂閱,擋在 design 後等於一般使用者管不了自己的通知)。**錯在假設「原本在 admin 頁面裡」= 「該用設計權」**,而判準應是這件事的粒度與擁有者。實走另補:欄位挑選器改吃後端可公開型別(前端不維護第二份清單)· 逐表單清單依分類分組 + 篩選(數十張表時捲不完)· 補 `DELETE /notifications/prefs`(原本設了回不到繼承,而畫面寫著「未設定則繼承上層」)。api 1040 + web 227 + e2e 136 全綠。以下為 v1.0 紀錄 ——|docs/27 §6 順序 1(OQ-UP 全裁定後首發):首頁卡牆改**分類目錄**(復用 form_categories,三態可見性,空分類隱藏)+ 左 icon rail / 底部 status bar 信任訊號(OQ-RWB-6=C 落地)+ ⌘K command palette 導航搜尋(client-side 零洩漏)+ Object Page 動作列(複製/刪除/友善列印 + print CSS);**誠實不上**通知鈴鐺(無事件源)/工作項目(獨立模組)/右欄佔位。小後端:`GET /api/categories`(非 admin,tenant-scoped)+ forms DTO +categoryId/updatedAt。api 200 + web 7 e2e(`workspace.spec` 固化)綠。commit c15913a(後端)+ ec7504e(前端)| [R1/workspace-ia.md](R1/workspace-ia.md) |
| **視圖系統 + 集合(browse)視圖** | R1·UP-2 | ✅ **SHIPPED v1.0(2026-07-25;M1–M5 + FMEA V1–V12)**|docs/27 §6 順序 2(承 workspace-ia):落地 D2「Ragic 語意、Airtable 骨架」—— 每表恆有**集合 Glide 網格視圖**(套 view 選欄/篩選/排序、inline 編輯、前導「檢視」欄下鑽)+ 與 Object Page **雙模式**(nuqs mode/rid,列表為進表預設)+ `view_def`(0009,authz Tier-1 車道非 RLS)**儲存檢視三態**(個人/共通/預設,admin-gating + 預設唯一)+ facet 篩選(型別感知 operator、單層 AND\|OR)+ 多鍵排序 + 快速搜尋(textual ILIKE)+ 批次刪除 + client-side 匯出。records query 擴充單層 combinator(OR 包 group 不洩 tenant/deleted_at 邊界)+ 快速搜尋。**OQ-VL-2 修正 docs/27 §3**:forcedFilter 非 view 屬性 → 移出歸 authz 軸(Airtable 明文 view filter 非安全邊界 / Teable Authority Matrix)。api 220 + web 12 e2e(views.spec)綠。commit f986477(後端)+ 3300561/9ccda81/9c7cdba(前端)+ ad2a4c2(spec)。殘留 P1:伺服器端大量匯出 / autoNumber·formula 篩選 / `/me` capabilities | [R1/views-list.md](R1/views-list.md) |
| **分組 / 看板 / 行事曆檢視** | F-1 | ✅ **SHIPPED v1.0(2026-07-30;M1→M5,OQ-VG-1..9 全採建議)**|**核心決斷成立**:分組作為排序變形,keyset 完整保留、不必退回 offset。Kanban 完整共用分組地基(stack = 單欄 group-by 的一階特例),Calendar 走獨立區間路徑。拖曳走既有 PATCH → 天然吃到樂觀鎖/欄位級/RLS/簽核鎖四層,優於業界普遍的 LWW;失敗給三類具名訊息(Jira 靜默彈回是反面教材)。FMEA G1–G10 全緩解,G1(小計洩漏)/ G2(隱藏欄分組)/ G6(時區差一天)已反向驗證。**🔴 實走揪出既有 P0**: 未跟上 #105 的選項物件化 → **填單單選下拉、篩選、看板分欄全部空清單**,型別為 unknown 故靜默通過所有測試。api 583 + web 87 + e2e 3 全綠。以下為 M0 研究紀錄 ——|承 docs/25 §226 優先序(通知 H-1 / 動態權限 E-1 已 SHIPPED,本批為下一項:「客戶第一週就會撞到通知與報表」)。**兩路深度研究,多條結論取自競品原始碼**(Baserow / NocoDB / Teable 皆 OSS)。**核心決斷:分組是「排序的變形」而非聚合查詢** → group key 前置進排序鍵、複合 cursor 擴充,**keyset 分頁完整保留**(AG Grid 官方明載 infinite row model 不支援 grouping —— 前提是把分組理解成「先聚合再展開」;改成排序變形即無此限制)。**Kanban 的 stack = 單欄 group-by 的一階特例,可完整共用地基;Calendar 不可共用**(是區間重疊查詢,一筆佔多格,而 group-by 假設一筆屬一組)。**§0.3 聚合 × 記錄級權限**:PG 官方明載 policy 先於 user query 的 conditions/functions 求值 → `COUNT`/`SUM` **天然只算通過 RLS 的列**,唯一洩漏路徑是特權連線 —— 而**本 session 已三度踩到該類**(RLS 假綠 / `action_audit` 缺 grant / `approval_instance` 走錯車道);**Ragic 官方自承「報表快照以系統管理員權限產生,可能包含檢視者無權存取的資料」是現成反面教材**。拖曳走既有 `PATCH record` 不開後門,天然吃到樂觀鎖/欄位級/RLS/簽核鎖四層,優於業界普遍的 LWW;失敗須具名三種訊息(Jira 拖曳彈回卻無提示是有紀錄的反面教材)。時區以 RFC 5545 為錨:`date` 欄無時區、`dateTime` 依租戶時區分桶(Airtable 依瀏覽器時區導致「差一天」為經典抱怨)。≈0.42 mo | [R1/views-group-kanban-calendar.md](R1/views-group-kanban-calendar.md) |
| **日期輸入與顯示格式** | R1·FMT | ✅ **SHIPPED v1.0(2026-08-04;M1–M4 全數)**|**M3 自製日期輸入已補**:可打字(`20151022` / `1022` 補今年 / `22` 補今年今月,逐列對齊 Ragic doc/51 官方例)· 解析失敗**保留使用者打的字 + 具名說明**不清空不猜(同 field-types-parity:409「即使 PG 猜得出來」也不猜)· APG 鍵盤日曆(焦點留容器 + aria-activedescendant)· 窄螢幕保留原生控件(**行動端未量測,誠實標注**)。⚠️ **一個容易寫錯的地方**已寫進註解與測試:Ragic 在 `dd-MM-yyyy` 下輸入 `1022` 得 `22-10-2015`,看似「依格式解析」實為解成 MM=10/dd=22 後**顯示**才重排 —— 誤做成依格式解析會**靜默存進錯的日期**。§9.1 追加四條 FMEA,其中 **D5**(寫死 `\u202f` 的正規化早已失效,現行 ICU 送 `\u2009`)與 **D6**(兩條測試在釘住 bug 本身,綠燈成了 bug 的護欄)是設計時完全沒預見的型;**D8 匯出 Excel 走 client-side 自己一套值轉換,與畫面是否一致尚未驗證** —— 列殘留。web 242 單元 + date-format.spec 4 條 e2e。以下為先前紀錄 ——|起點是 #155「原生日期輸入顯示 mm/dd/yyyy」,**研究把題目換掉了**:自家量測(同一份 HTML,只換 Chromium `--lang`)zh-TW→`2026/03/05`、en-US→`03/05/2026`、de-DE→`05.03.2026`,而 `value` 三者皆 `2026-03-05` ——**不是「我們沒本地化」,是「格式不在我們手上」**。意外收穫:只設 context `locale` 而未設 `--lang` 時,`navigator.language` 各自正確但控件**全畫成美式** → `navigator.language` 與控件格式可以不一致,**連偵測都不可靠**,這排除了「偵測後只在必要時自製」的折衷。競品一致把格式放在**欄位**上(Ragic 設計手冊 doc/51「設計模式 › 欄位設定 › 基本」;Airtable 欄位的 Date format,且明說預設跟瀏覽器走、要改**得去改瀏覽器設定**)。**順著查下去撞到更大的缺口**:列表網格印出 `128400.0000` 與原始 ISO —— **正是 `display-value.ts` 檔頭逐字說它要修的兩個症狀**,但列表走的是另一支格式化函式。根因是**兩支函式做同一件事**,故 M1 是**合併不是補丁**(同型漂移 pivot-and-charts §14.5 才記過)。另修一行早已失效的空白正規化(寫死 `\u202f`,現行 ICU 送 `\u2009` → 那行是 no-op 且不會有任何東西告訴你)。M2 補欄位級 `dateFormat`(白名單五檔,先窄後寬;民國年維持 P1 但模型留得下)+ 設計器入口。**兩條測試在釘住 bug 本身**(`2026-07-19 10:00:00` = UTC 去 T;`128400.0000` = numeric 原始表示),已更正並補「兩支函式同輸出」斷言。⚠️ §13.3 誠實記下一次誤判:看板鍵盤 e2e 中途反覆變紅,我判定是本模組迴歸並據此改了兩處程式碼,**量測基線後發現乾淨樹本來就 50% 不穩**,兩處改動已全部撤掉 —— 不留無法佐證理由的程式碼。api 1043 + web 229 全綠。≈0.15 mo(M3 未計) | [R1/date-and-display-format.md](R1/date-and-display-format.md) |
| **樞紐分析 + 圖表** | F-2 | ✅ **SHIPPED v1.1(2026-08-03;M1–M5 全數)**|**M4 小圖表已補**(後端 + 列表頁渲染 + 自助設定 UI):可見群組候選先過來源表單權限、**建立時後端再驗一次**(只在前端過濾等於沒做,直接打 API 就能繞)· 不可用時回**具名**理由而非空白圖(空白圖會被當成「沒資料」)· 設定 UI 是**定位缺口**不是體驗 —— 在此之前只能打 API 建立,而第一約束逐字「有 API 可以做不算解決」。**🔴 另修兩條「已裁定但沒做到」**:(a) 樞紐 / 圖表**不吃當下檢視的篩選** —— 列表篩成南區而圖仍畫全區,OQ-PC-10=A 的裁定理由逐字就寫著「那張圖在騙人」;修法是抽共用推導而非傳 query,**漂移正是複製造成的**。(b) form DTO **回傳全部欄位不過權限** —— 值有 maskRead 擋著但**欄位名稱**外洩,且使用者選得到一個必定失敗的軸。**這兩條比「沒想到」更值得記:裁定寫得很清楚,但沒有機制在實作偏離時發出訊號。**另修 `Chart` 的 `ariaLabel` 被 ECharts 靜默吃掉(每張圖的無障礙名稱都一樣)。以下為先前紀錄 ——|**核心決斷成立**:pivot 與 group-stats **共用引擎**,只改 grouping set 產生規則(前綴 rollup → 兩組前綴笛卡兒積),RLS/交易/filter/聚合/截斷全部不動。回長表前端轉置(以鍵查找非索引,稀疏格才不會位移)。ECharts 6 tree-shaken + 自寫 wrapper(不用 echarts-for-react,其 peer 未明列 React 19)。**🔴 實走揪出兩個研究未預見的 a11y 問題**:(1) ECharts 只內建**簡體 ZH**,其自動 aria 描述**直接覆寫容器 aria-label** → 繁中產品出現簡體,已註冊 zh-TW locale;(2) 自動描述在直角座標系下把**軸索引唸成資料值**(「中 為 0,300」),改帶 {name,value} 無效 → 改自訂描述。**研究說「a11y 是一行開關」對 decal 成立、對自動描述不成立** —— 文件層級的正確與實作層級的正確之間有落差。api 589 + web 87 + e2e 5 全綠。以下為 M0 研究紀錄 ——|承 docs/25 §F 之 pivot(5)+ 圖表(4)+ 儀表板(4),F 段最大宗。**兩路深度研究推翻原規劃兩個前提**:(a) 🔴 **Ragic 的資料儀表板根本不可拖拉** —— 官方逐字「各欄位統計數據會**依據表單中的位置,從左到右、從上到下依序排列**」,可拖曳的是**小圖表 widgets**(可插在表單頁/列表頁任意位置 + 可設可見群組),首頁則是**受限直欄版面**;→ **`docs/10` §131「拖拉排版」記載有誤,已更正**,且拖拉式儀表板非 parity 必要項(與 docs/24「儀表板為次要視圖」一致)。(b) **業界一致回長表**,Metabase / Superset / Cube 原始碼皆前端轉置,**沒有一家回動態寬表** —— PG result set **1,664 欄**是硬天花板,且寬表的 JSON key 會變成使用者資料。**最省的結論**:F-1 的 `group-stats` 只需把 grouping set 產生器從「前綴 rollup」改成「**兩組前綴的笛卡兒積**」,RLS / 同交易 / filter / 聚合 / 截斷**全部不動**。**§0.5 洩漏面的主角是維度值清單而非聚合值** —— 欄標頭/圖例/tooltip 會列舉 GROUP BY 的維度值;**CVE-2024-55951**(Metabase filter values 跨 sandbox 使用者快取共用)是直接可引的公開事件,故 R1 **不開快取這個面**。`crosstab` 排除(要求欄名寫死,動態化等於再開一條注入面);**不用 `CUBE`**(2ⁿ 組 vs 明列 12 組)。圖表庫維持 **ECharts**(Apache-2.0 含專利授權 + a11y 一行開關 + 甘特/地圖/雙 Y 軸一次補齊 parity),但**不用 `echarts-for-react`**(peer 未明列 React 19)。≈0.42 mo | [R1/pivot-and-charts.md](R1/pivot-and-charts.md) |
| **資料匯出(帶得走的完整副本)** | I-1 | ✅ **SHIPPED v1.0(2026-08-03;M1–M5 + FMEA E1–E11)**|設定中心「資料匯出」頁(請求 / 附件 opt-in / 進度輪詢 / 下載 / 到期倒數 / 剩餘次數)+ `use-exports` + `data-export.spec`。**🔴 M4 推翻 M3 的下載回應形狀**:簽名分支原本回 `302 + Location`,那對 curl 成立、對瀏覽器不成立 —— 端點是 POST(要帶密碼)故前端只能用 `fetch`,而 fetch **跟隨重導後最終回應仍須通過 CORS 檢查**,物件儲存桶預設不給;且該路徑在 dev 與所有測試裡**完全不執行**(local driver 無 `presign`),屬「測試永遠綠、只有 prod 會壞」——本模組已踩過同型兩次。改回 **200 JSON `{url}` + 前端導航**(導航不受 CORS 管),整合測臨時補 `presign` 覆蓋那一半。**再認證不預判環境**:直接送、後端回 `EXPORT_REAUTH_REQUIRED` 才顯示密碼欄(且在扣次數之前,不消耗額度)。**e2e 自己先紅一次**:按下建立到清單重抓之間有空窗,`.first()` 指的還是上一筆 → 斷言會**先假通過再失敗**,改為等列數增加;驗 zip 魔術數與位元組數(只驗「有觸發下載」的話錯誤頁也會通過)。**⚠️ 未解釋的觀察(E11)**:開發期出現一筆無人請求的 job 與一次無人按下的下載計數增加,控制重現與 60 秒閒置觀測皆正常,未能再現。殘留:on-prem 代理下載走記憶體 / 再認證未於真實 session 實走 / 每日 10 次額度被 e2e 消耗。以下為 M0 研究紀錄 ——|解 F-8 殘留 B6:停權唯讀的訊息逐字說「可檢視與**匯出**資料」,但全庫沒有租戶級匯出 —— 那句話目前是假的。一手查證 GDPR Art. 20(「structured, commonly used and machine-readable」)+ Salesforce Data Export(48 小時 / zip CSV / 附件 opt-in)+ Google Takeout(7 天 / 限 5 次 / **下載要再認證**)。**核心主張:既有「匯出 Excel」只含畫面已載入的列,是看的便利不是帶得走,不可做成它的放大版**;動態 schema 平台必須「資料 + metadata 一起出」,故 zip = 逐表 CSV + `manifest.json`。裁定:DB 佇列表 + `@nestjs/schedule`(零新相依,不為單一功能引入 Redis)/ 7 天 + 限 5 次 / 附件 opt-in / **依 `export` 權逐表**(否則是欄位級權限的第 17 條旁路)/ 下載再認證 / **唯讀閘門豁免匯出**(否則本模組上線後停權客戶依然拿不到資料)。⚠️ CSV 注入的方向與既有防護**相反**(既有是拒收上傳,匯出要的是產生時跳脫)。≈0.3–0.4 mo | [R1/data-export.md](R1/data-export.md) |
| **資源回收桶 + 保留期硬刪** | H-2 | ✅ **SHIPPED v1.0(2026-07-30)**|承 docs/25 §246 下一批優先序第一項。**盤查發現現況比「缺一個還原 UI」嚴重**:刪除全為 soft、物理層完整保留,而程式註解所稱的「清理 job 之後收」**那個 job 並不存在** → 同時是「誤刪不可救 + 合規上等於沒刪(個資法 §11 III / EDPB 2025 報告點名「無成文刪除程序」)+ 額度持續被佔」三個問題。交付 `trash_entry` 索引表(title 與表單名皆為**刪除當下的快照**)+ 還原 dry-run **三類阻擋**(父已刪 / 同名 / 違反後加約束)+ **到期分批硬刪**(真 `DELETE` / `DROP COLUMN` / `DROP TABLE`;簽核中不刪;直接掃 `deleted_at` 而非掃 entry,合規無死角)+ 回收桶頁 + 立即硬刪路徑。**🔴 §0.5 本機 PG 16 實測推翻本專案既有文件**:field-types-parity §B-1 原記「`VACUUM FULL` / `pg_repack` 重建整表才回收 attnum」——實測 300 次 add/drop 後 `VACUUM FULL`,dropped 仍 300、max_attnum 仍 301,**完全沒回收**;PG 核心開發者明言「We just never recycle attnums」。已更正該文件。**🔴 瀏覽器實走抓到四個測試沒抓到的缺陷**:唯一索引漏 `form_id` 導致跨表撞號時 entry 被 `ON CONFLICT DO NOTHING` **靜默吞掉**(正是本模組要防的「刪了但桶裡沒有」)/ 顯示欄位非快照 / 硬刪要求 `resolveForm` 致父已入桶時丟誤導 404 / **`approval_instance` 無 app 車道 grant**(本 session **第五次**踩到「安全機制被特權連線遮蔽」,順帶補上該表一直缺的 RLS)。migration 0031-0033。api 606 綠(trash 17)· web 87 · e2e 3。殘留 R5 還原背景化 / R7 attnum 軟上限 / R8 附件連帶硬刪。0.33 mo | [R1/recycle-bin.md](R1/recycle-bin.md) |
| **分頁級租戶上下文(修跨分頁污染)** | F-10 | ✅ **SHIPPED v1.0(2026-07-30)** — OQ-TC-1..6 全採建議(TC-6 實作時判定 dev 車道**結構上免疫**,刻意不加永遠不會觸發的分支,原因寫進註解)|承 task #107。租戶原本來自伺服器端 session 列的 `activeOrganizationId`,**所有分頁共用**;分頁 2 切公司 → 分頁 1 的下一次寫入落到錯的公司,且無任何訊號。**現有三道防線全部會通過** —— 不是漏了哪一道,是租戶的作用域選錯了。**先例**:Shopify 對非嵌入式 app 的同一問題官方承認**無 workaround**;Clerk 官方逐字警告 session cookie 是 **singleton (global) value**;Better Auth 官方已預期並明文把解法推給應用層(確認無 per-request org override)。作法:每請求帶 `x-weyver-org-intent`,伺服器**獨立驗成員資格**後採用;讀放行、寫回 409;不寫回 session。`org-intent.ts` 用整段講清它與 AGENTS 鐵則 3 剝除的 `x-tenant-id` 差在哪(選擇器 vs 授權結論)—— 差別細微而改錯的後果是 BOLA,**反向驗證確認拔掉成員驗證即轉紅**。前端 header 收斂成單一 `engineHeaders()` 出口(原本散在 6 處,漏一處等於沒修)。跨分頁用 BroadcastChannel 掛 sticky 橫幅,不強制重載、不自動切回 —— **此 UX 查無任何業界先例**。api 698 綠 | [foundation/tenant-context.md](foundation/tenant-context.md) |
| **上傳掃毒 + presigned 混合下載** | F-11 | ✅ **SHIPPED v1.0(2026-07-30)** — OQ-MS-1..8 全採建議 |**🔴 本模組三次「研究說 A、實機是 B」**:(1) OOXML 巨集只比對檔名**可繞** —— OPC 允許 part 任意命名,型別由 `[Content_Types].xml` 宣告;(2) PDF 關鍵字表憑印象列,對照 **PDFiD 原始碼**後補 `/JBIG2Decode` `/XFA`,並發現**原始位元組掃描看不進 `/ObjStm` 壓縮的物件** → 推翻「便宜措施投報率高於 ClamAV」的隱含結論,兩者**不重疊**;(3) **最重要:`AlertExceedsMax yes` 對 zip bomb 無效** —— 199KB 壓縮 / 200MB 解開的 zip,clamd 回 `clean`,而同一次實測中超過 `StreamMaxLength` 的 30MB 串流正確回 ERROR(證明設定有載入)→ 改由自己讀中央目錄的未壓縮大小算比例,確定性且不依賴掃描器,並把實測結論**釘成回歸測試**。**部署面五項實測**:官方映像**只發 amd64**(影響 prod 選 ARM 的假設)· `cap_drop: ALL` 讓容器起不來 · `/run` 掛 tmpfs 也失敗 · healthcheck 三種寫法皆不可用 → 改由應用層 `ping()` · 冷啟 >5 分鐘。交付:內容層檢查(OOXML 巨集 / PDF 主動內容 / polyglot / zip bomb)· `scan_status` + **下載閘 deny-by-default**(`pending` 也擋 —— 那是掃毒有沒有意義的分界)· clamd INSTREAM 客戶端(自寫,不用 `clamscan` npm 的 local-binary 模式:CVE-2020-7613)· 兩段式掃描 + 補掃 cron · presigned 混合下載(授權每次重算,302 到 60 秒 URL,簽章帶 disposition 覆寫)。**staging 洞**:裝上閘門後既有測試立刻紅 → 加 `MALWARE_SCAN_MODE`,但 prod 停用須 `MALWARE_SCAN_ACK_DISABLED=1` **顯式承認**,不讓「忘了設定」與「決定不掃」長得一樣。migration 0036。api **749 綠**(真 clamd 4 條)。公開表單附件**仍未解鎖**(#121:缺 per-share 配額 / 逾時清理 / 匿名限流)| [foundation/malware-scanning.md](foundation/malware-scanning.md) |
| **事件匯流排 + 出站 Webhook + API 金鑰** | G-1 | ✅ **SHIPPED v1.0(2026-07-30)** — OQ-WH-1..10 全採建議|**🔴 盤查推翻兩個既有文件記載**:(a) AGENTS.md ⚙️ 與 docs/20 §25 把 **BullMQ + DBOS 當既定選型**在寫,`package.json` **兩者皆未安裝** —— 真正在跑的是 `notification_delivery` + 每分鐘 cron 抽取的 outbox(已在 prod 驗證)→ 復用而非引進新依賴;研究另指出 **BullMQ 的 group 併發是 Pro 商業功能**,OSS-only 下引進也換不到順序保證。(b) **`record.created`/`record.updated` 是死路徑** —— 事件碼有宣告、單元測試覆蓋 `levelAllows`,但全專案只有 **2 個 `this.notify.*` 呼叫點且都在 approval**,`RecordService` 從未注入 → **通知設定頁預設檔位承諾的行為從未發生過**;`NotificationsModule` 早是 `@Global()` 且註解明文列 form-engine 為預期消費者,線拉好了沒接。本模組的事件發射點順手補好。**🔴 實測改變了選型**:研究建議 undici pin DNS,實測有效但 undici 8 型別與 Node 內建 `undici-types` 衝突;回頭實測 `node:https` 的 Agent `lookup` **一樣有效、零新依賴、預設不跟隨 3xx** → 最終**未加任何依賴**。**🔴 反向驗證「沒轉紅」挖出真 bug**:拔掉 IPv4-mapped 正規化測試仍全綠 —— 它們是靠 `split(":")[0]` 得空字串→NaN 的**意外分支**通過,與正規化無關;連帶暴露「任何以 `::` 開頭的 IPv6 都被誤擋」。改完整 hextet 展開 + 補正面測試,現在拔掉會 5 條轉紅。**🔴 瀏覽器實走**:SSRF 擋住了但錯誤落到 500「internal error」(`SsrfBlockedError` 未繼承 `DomainError`)—— 擋下來不等於做完了,改映射 422 帶出原因。交付 `event_outbox` 同 tx 發射 + 扇出 cron + SSRF 全鏈(40 條對抗性測試)+ Standard Webhooks 簽章(零停機輪替)+ Svix 退避與雙條件停用 + 啟用挑戰 + API 金鑰(只存 hash;不用 Argon2 因金鑰是 256-bit 隨機值,字典攻擊不成立)+ 整合設定頁。載荷目前 **thin-only** → 欄位洩漏面為零,fat 模式列後續。migration 0034。api 676 · web 87 · e2e 4。≈2.5 mo | [R1/webhook-and-events.md](R1/webhook-and-events.md) |
| **公開表單** | G-2 | ✅ **SHIPPED v1.0(2026-07-30)** — OQ-PF-1..8 全採建議|與 G-1 分開因**威脅模型相反**(前者防我們被誘導打別人,後者防別人灌我們 / 從我們撈資料)。**🔴 研究最大發現**:下拉 / 帶入欄是**已實證**的洩漏破口 —— Airtable 社群與支援一致確認 linked record 欄會讓填表者看到**來源表全部記錄的 primary field** 且可被爬取;**prefill 不是安全機制**(Airtable 官方明文 hide 參數可刪、Ragic `pfv` 明碼在 URL)。**🔴 對本專案的關鍵推論**:[authz.md §12.2 F4](R1/authz.md)(公式引用 hidden 欄間接洩漏)是**已接受的 P1 殘留**,內部靠管理員紀律可接受,但**觀眾換成匿名者後同一個洞就是 P0** → 既有風險評級綁定威脅模型,開新入口必須重評。交付:**opt-in 欄位白名單**(排除制在「日後有人加一個成本欄」那一刻就外洩)· 危險型別設計期閘門(link / autoNumber / member / 附件各有理由)· **匿名提交落待審收件匣不進動態表**(刻意不照抄業界 —— 各家問卷平台都不隔離,但 ERP 下匿名提交直接觸發簽核、吃單號、污染主檔不可接受;隔離同時解掉自動編號被消耗、唯一值變 existence oracle 兩件事)· 關閉條件(計數與提交同一 tx)· honeypot + 最短填寫時間 + throttler · IP 只存加鹽 hash · 回執不透明代碼 · `/f/[token]` 訪客頁(不用 `/app` layout,無任何內部 chrome)。**🔴 瀏覽器實走抓到所有靜態防線都攔不住的 bug**:控制器裸建構子參數 + tsconfig 未開 `emitDecoratorMetadata` → Nest 注入 undefined、路由 500;type-check 過、16 條整合測全綠(直接 new 服務繞過 DI)、lint 無話說 —— **只有真的跑起來打那條路由才會炸**。**與原設計差異**:link 改「一律不得公開」(比伺服器端搜尋更強);Altcha 與 magic link 列後續,理由見 doc §3。migration 0035。api 692 · web 87 · e2e 4。≈2 mo | [R1/public-form.md](R1/public-form.md) |
| **跨表全文搜尋(⌘K)** | H-3 | ✅ **SHIPPED v1.0(2026-07-31)** — OQ-FTS-1..6 全採建議|**🔴 選型由本機 200K 筆繁中實測決定,不是查文件**:PG 內建 `to_tsvector` 把整串中文當**單一 token**(搜「食品」找不到「大成食品股份有限公司」—— 那是等值比對不是搜尋);`pg_trgm` 有 **3 字門檻**,而**中文最常見的搜尋詞正好是兩個字**(食品 / 鋼板 / 螺栓)→ 退回全表掃描 ~50ms;`pg_bigm` 同樣查詢 **4.6ms 且 planner 自選索引**。Cloud SQL 官方支援 pg_bigm,pgroonga / zhparser / pg_jieba 皆不在支援清單。**不引入外部搜尋引擎**:上了引擎 RLS 就不再執法,租戶隔離由「DB 層已驗證的防線」降級為「應用層記得簽對 token」—— 用最高風險換 Baserow 自陳 <1% 的請求量。**不做逐欄 tsvector**:Baserow 官方 issue 第一手承認該做法「已撞到 1600 欄上限」「proven fragile」「can become out of sync」,改集中式 `search_doc`。交付:自建 pg_bigm dev image(建置期驗證擴充存在)+ `search_doc`(RLS FORCE)+ **同 tx 索引寫入**(建/改/刪/還原四路徑)+ 權限 **pre-filter 進 WHERE**(事後過濾的話**結果筆數本身就是洩漏**,可二分逼近;此點**優於 Ragic 的「隱藏欄乾脆不索引」**)+ ⌘K 跨表記錄搜尋。**🔴 三個「不報錯但完全不動」由瀏覽器實走與端到端測試抓出**:(1) `SearchService` 未進租戶交易 → RLS 濾光、API 一律回空,而**既有整合測試直接對表下 SQL 繞過了 service**(反向驗證:拿掉交易正好新增 4 條轉紅、原 13 條全綠);(2) 可搜尋型別清單手寫了 `textarea` / `richText` **兩個不存在的型別**,真正的長文字叫 `longText` → 備註欄從未進索引,型別參數是 `string` 故編譯期抓不到 → 改由 `FIELD_TYPE_REGISTRY` 推導;(3) controller 路徑多一層 `engine`(web rewrite 已映射過)。順帶補 `removeFormInTx` —— 定義了卻沒人呼叫的半成品,接上回收桶硬刪(保留期內刻意不清,還原免重建)。**釐清 RLS 三層語意**:`ENABLE` 管非 owner / `FORCE` 連 owner 也管 / superuser·BYPASSRLS **兩者皆繞過** —— testcontainer 預設使用者實測為 superuser,故隔離測試一律 `SET LOCAL ROLE weyver_app`。migration 0037。api **770 綠**(search 21 條)· e2e 4。殘留:既有資料 backfill(pilot 前必做)/ statement_timeout / GIN 寫入延遲量測 / 批次匯入索引效能。0.5 mo | [R1/full-text-search.md](R1/full-text-search.md) |
| **2D 表單設計器(form-designer-ui uplift)** | R1·UP-3 | ✅ **SHIPPED v1.0(2026-07-25;M1–M5 + FMEA F1–F10)**|docs/27 §6 順序 3(承 views-list):落地 D1「2D 格線畫布=填單畫面」—— 既有線性設計模式 uplift 成 Excel 式 **2D 格線畫布**(row/col/span 擺位、拖曳、合併)+ **靜態敘述/圖片元素** + **表單分段**(列範圍)+ **欄位設定核心**(預設值 17 變數 / 唯讀 / 隱藏 / placeholder / 說明)+ **設計草稿模型**(批次 Save + Ctrl+Z)。**核心洞見**:版面皆 layout metadata、與資料正交 → `form_def.layout` JSONB(migration 0010),form-engine-core DDL/DML 鏈**不動**(低風險)。OQ-FD2-2 定調:layout 走草稿、結構性 DDL 維持即時(Ragic 排除清單);靜態=layout 元素非 field_def(OQ-3);分段=列範圍(OQ-4);畫布 CSS grid + dnd-kit(OQ-5)。create-time 預設值變數($DATE/$USERID 等)。api 226 + web 15 e2e(designer.spec,拖曳分步 mouse)綠。commit e073659(後端)+ cd7dc3a/4a2ee56/2445a5f(前端)+ a473a6a(spec)。殘留 P1:填單頁 Markdown sanitize / 格式 mask·民國年 / 條件式格式 / 全延遲結構性變更集。≈0.47 mo(四模組最大)| [R1/form-designer-2d.md](R1/form-designer-2d.md) |
| **欄位型別 parity(form-engine-core 增量)** | R1·UP-4 | ✅ **SHIPPED v1.1(2026-07-29 快照帶入補完)**|**帶入欄的 live/snapshot 顯式化**:`syncMode` 預設 live(既有零遷移)、snapshot 走物理欄 + lazy backfill、**重整先給 diff 再套用且逐筆稽核**(Ragic 對應功能是無差別覆蓋、無 diff、無記錄)、設計器文案不出現術語、帶入三層權限閘。**瀏覽器實走抓到整合測遮住的缺陷**:`action_audit` 對 app 車道缺 grant(測試當時走 superuser 車道 → 假綠;已補 migration 0029 並把測試改走 app 車道)。以下為 v1.0 —— |v1.0(2026-07-25;M1–M5 + FMEA T1–T10)|docs/27 §6 順序 4(承 form-designer-2d):落地 §2 欄位型別 P0 —— **系統欄 4 型**(投影 audit 零儲存)+ **rollup**(複用已完整 RollupService,接讀時 field type)+ **lookup**(複用 RelationService,讀時注入)+ **link 補完**(啟用/驗證/顯示標籤/link&load/reverse)+ **autoNumber pattern**(token 文法 + date + reset,counter table)+ **選項顏色 + 連動選項**(options 加法擴充,valueSchema 不變零遷移)+ **條碼生成**(前端 OSS lib 渲染,零 DB)+ **格式遮罩**(displayMask 顯示)。核心洞見:RollupService 已完整、formula withFormulas 讀時注入為 lookup/rollup/系統欄範本、link 部分已建。**OQ-FTP-6**:image/signature/attachment 上傳依賴檔案儲存基礎設施 → 排除 P0(自成 P1)。migration 0011(autonumber_counter)。核心洞見:RollupService 已完整、formula withFormulas 讀時注入範本、系統欄投影 audit 零儲存、virtual 欄(no-op buildColumn/baseQuery 排除)。M4 設計器進階 palette + 設定編輯器。api 236 + web 17 e2e(field-types.spec)綠。commit 0972cf7/d98ffa4/7d212f0(後端)+ e469aa1/ad8c9e7(前端)+ 58e6f1d(spec)。殘留:⚠️ lookup 欄位級權限 / 顯示層 QR·顏色 chip·連動過濾 / image·signature(file-storage)/ member·rich-text·reverse-query 皆 P1。≈0.45 mo | [R1/field-types-parity.md](R1/field-types-parity.md) |
| **設計檢視 = 表單本身(設計器心智模型補完)** | R1·UP-3c | 🟡 **M0 APPROVED(2026-08-02)· M3 之子表入口已落地(2026-08-04)**|🔴 **task #154 已解**:「＋加子表」原本擠在設計器**標題列右上角**,與「v1 · 2 欄」這類中繼資料並排 —— 而「兩層以上的資料結構」正是本產品對 Google Sheets 的核心差異點,**核心賣點被擺在 metadata 的版位**。判準很簡單:加欄位與加子表都是「**往這張表加東西**」,所以該在同一個地方、長同一個樣子 → 移進欄位面板,與欄位型別**共用同一個 `PaletteRow` 元件**(不是靠兩份 class 字串碰巧一致);子表不可再有子表 → 上層不給 callback 時整組不渲染,**不是給了再 disable**(那會讓人以為「現在不行,等一下可以」)。⚠️ **e2e 的負向斷言第一版是空的**:寫成「標題列不得含『子表(明細)』」,而舊版按鈕叫「＋ 加子表」**字面不同,那條規則本來就不會紅** → 改為比對名稱樣式 `/子表/`,並**實際把按鈕放回去驗過它會紅**。header 三帶收一帶仍未動(牽涉 `docs/29` S5 不變量與整體視覺方向)。以下為 M0 紀錄 ——|走查發現 `form-designer-2d` 已 SHIPPED 的 **D1 裁定「2D 格線畫布 = 填單畫面本身」只實作了前半** —— 座標系統做了(實測畫布為 `52px × 12` CSS grid),視覺對齊沒做(格子渲染成**欄位卡片**而非**欄位列**)。故本模組是**補完已裁定事項**,非提新設計。四項:① 畫布共用 `FormSection`/`FieldGrid` 渲染欄位列 + 示例值 ② 就地新增(表尾 ghost row) ③ 型別常駐欄改 popover ④ 屬性收進右側單一抽屜(accordion)。**零 migration / 零新端點 / 不動 layout 資料模型與草稿模型**。量測基準:header 帶 3→1、輔助面板 4 欄→1、主體 21%→≥60%(docs/29 S3/S4/S5 三條現全破)。⚠️ 教訓已記:**裁定句子裡的每個子句都要有對應驗收** —— 「2D 格線畫布」有 designer.spec 測拖曳,「= 填單畫面本身」無任何測試,於是安靜地沒被實作。走查順帶發現 builder 頁 **hydration mismatch** 與 401(OQ-FDW-6 建議另立)。⚠️ **2026-08-01 撤回一項**:我曾把 Ragic 的產品文案誤讀為 Weyver 定位變更(「取代 ERP / ERP 外掛 / 個人使用」三說並存),並列為擋路的前置問題;用戶澄清那是**包含關係不是矛盾**,基本方向仍是替代 ERP。企業 chrome / 租戶 / 簽核 / R2 計算層優先序全部不變 | [R1/form-designer-wysiwyg.md](R1/form-designer-wysiwyg.md) |
| **條件式格式(form-designer-2d P1 解鎖)** | R1·UP-3b | ✅ **SHIPPED v1.3(2026-08-04;C-2 + C-3 全數)**|🔴 **本模組最值得記的是一次自我更正**:v0.1 逐字寫著「條件式行為(顯示/隱藏/唯讀/必填)性質不同 → 另立模組」(OQ-CF-8=A),查一手文件後發現**問錯了問題** —— Ragic 所稱的「條件式格式」是**一個功能、一個入口、一份由上而下求值的規則清單**共 **10 類效果**,設定顏色只是其一;上游 `docs/27` 把五項寫成同一項是對的,是本模組把它拆成兩件事才是誤判。**代價不是少一個功能,是同一份設定在新系統會得到不同結果** —— 官方用一整節〈問題排除〉解釋「同一欄位被多條規則涵蓋時後者覆蓋」,拆成兩份清單後跨清單順序**在結構上無從定義**。§10-bis 重裁為選項 C 分層漸進。**C-2 全數落地(2026-08-04)**:分段採**目標選擇器**而非新效果類(OQ-CF-9,官方逐字「無需逐一針對各欄位進行設定」= 批次捷徑;維持單一仲裁空間,後者覆蓋跨分段級/欄位級自動一致)· 「上鎖」= readonly,不另立效果 · 訊息插值改用**欄名**而非 Ragic 的欄位編號(我方 conditions/targets 一律欄名,不讓使用者學兩套),分隔符改冒號且**只切第一個**(欄名可含底線,Ragic 用 id 才沒這個歧義)。**兩條 P0**:`{{fieldValue}}` 是**欄位遮罩的旁路** —— 與 2026-08-04 修的公式污染閉包**同一形狀**,故從第一版就吃已遮罩的 values 並回具名「(無權檢視)」;訊息一律純文字,e2e 打 `<b>` 進去斷言元素數為 0。站① 另抓到 `fieldLayoutSchema.sectionId` **零 reader 零 writer**(同 `a66f110` 移掉的 colWidths),本批移除。**C-3 同日落地(§11-bis)—— 本模組第一次進入伺服器強制**:必填 / 動作按鈕 / 開始簽核。§10-bis 逐字說這三項有「風險與工期跳級」的顧慮,共同點是**只在前端做就等於沒做**。🔴 **求值器要在伺服器跑,而答案本來就在自家 repo**:`@weyver/formula` 是前後端共用的套件,`formula-preview.ts` 檔頭逐字寫著「與後端用同一套引擎」—— 照辦,抽出 **`@weyver/rules`**,前端降為 re-export。照抄一份到後端會漂移,而漂移的症狀不是樣式不一致,是「**畫面說可以存、伺服器說不行**」。⚠️ **站②當場又踩一次**:`packages/rules` 內部 import 寫 `./evaluate.js`(NodeNext 慣例),而隔壁 `packages/formula` 用**無副檔名** → Next 解不到,整個 `/app` 500。**先看隔壁怎麼寫,不要照通則寫。**四條 P0:(G13)打 API 繞過必填 —— 整合測試完全不經畫面斷言 422;(G14)**部分更新讓條件憑空不成立** —— `PATCH` 只送改動欄位,只拿 patch 求值必填會靜靜消失,故同交易內讀回該列再合併;(G15)前後端漂移 —— e2e 同時斷言畫面星號與伺服器 422;(G16)**閘門必須用未遮罩的值** —— 用遮罩值求值的話,看不到「金額」的人條件自動不成立,「金額超過一萬才需簽核」對他就失效。裁定:必填是**聯集不是覆蓋**(官方只對唯讀說「條件式優先」,對必填說的是設計期排除)· 按鈕與簽核是**目標軸**不是新效果類(官方對三者用同一組動詞)· 客製提醒訊息沿用同一條規則上的 `message`。⚠️ 抓到一條**假綠**:整合測試漏傳 `expectedVersion` 得到 400,看起來與「必填擋下」一模一樣。另有一條測試因 C-3 把 `required` 變成合法 kind 而**靜靜不再測任何東西**。rules 50 + api 1072 + web 216 + e2e 147 全綠。以下為 v1.0 紀錄 ——|form-designer-2d 之 P1 殘留。**§0 clean-room 證據:Ragic 與 Airtable 的模型相反,不可各取一半** —— Ragic=**表單級 + 欄位級著色 + 後者覆蓋 + 自由選色**,且表單頁與列表頁分開設定;Airtable=視圖級 + 整列著色 + 前者優先。Ragic-parity-first → 整套採 Ragic(OQ-CF-1/3/5/7)。**OQ-CF-2 需裁定**:Ragic 用自由選色,與 docs/14 §0.2 才剛立的「受控色盤非自由選色」衝突(建議沿用 12 tone,誠實代價=遷移色需映射)。**OQ-CF-8 明確排除條件式行為**(顯示/隱藏/唯讀/必填):條件式隱藏**不是安全邊界**、條件式必填只在前端即為裝飾 → 另立模組。P0 複用既有 filter 模型 + 純前端求值 + 存 form_def.layout → **零 migration 零新端點**。落地:求值器 16 單元測(含「反轉順序→結果反轉」證明覆蓋序)· 設計器面板(兩面獨立/覆蓋序常駐提示/12 色盤/即時預覽)· 記錄頁標題與值著色 + Glide themeOverride(每列快取)。**色值三處皆白名單查表**(後端 enum / chipToneTextClass / grid-tone)。api 321 + web 61 + e2e 38 全綠。UI 稿 `docs/mockups/conditional-format-flow.html`。commit 20aa281 + daa0c63。殘留:G6 未壓測 / 條件式行為(另立)/ 整列著色。≈0.16 mo | [R1/conditional-format.md](R1/conditional-format.md) |
| **選項顏色設定 UI(field-types-parity P1 解鎖)** | R1·UP-4c | ✅ **SHIPPED v1.0(2026-07-28;M1–M4 + FMEA C1–C6)**|`options.colors` 後端於 field-types-parity 已建、**前端從未有入口**(死參數);record-workbench-ui 狀態章已預備讀取。**§0 clean-room 證據推翻預設前提**:**Ragic 無 per-option 顏色**(其對應功能為**條件式格式**),per-option 色為 Airtable/Teable 範式 → 本模組是**增強而非遷移 parity**;兩競品實際色盤色數**文件未載**,任何色數皆為推測。**§0.2 攤開與 docs/14「裝飾配色一票否決」之正面衝突** → OQ-OC-1 交裁定(建議 A 只開 4 語意 tone)。另 OQ-OC-4 誠實指出:若求遷移 parity,**條件式格式(form-designer-2d P1 殘留)優先度應更高**。**裁定 OQ-OC-1=B 完整色盤**(用戶明示可推翻 docs/14;**解禁嚴格限「裝飾配色」一項**,漸層/pill/陰影/大留白仍一票否決,已寫回 docs/14 §0.2)。8 組類別色 token(去飽和、避開綠/琥珀/紅)+ StatusChip 12 tone(toneClass 兼安全白名單)+ 後端 enum 收斂與 colors↔choices 交叉驗證 + ChoicesEditor(逐項/即時預覽/自動配色)。**順帶**:edit-form-panel 422→299 行;**修 C6 潛在缺陷**(呈現用設定壞掉會擋住整張表的記錄寫入)。api 321 + web 45 + e2e 34 全綠。commit 47cfa4c + c0a8af7。殘留:條件式格式(OQ-OC-4)/ 整列著色(OQ-OC-3)。≈0.13 mo | [R1/option-colors.md](R1/option-colors.md) |
| **圖片欄 + 簽名欄(field-types-parity P1 解鎖)** | R1·UP-4b | ✅ **SHIPPED v1.0(2026-07-28;M1–M4 + FMEA S1–S8)**|field-types-parity 之 OQ-FTP-6 子件,阻塞由 F-5 file-storage v1.1 解除。**§0 clean-room 競品證據**:Ragic 明載「圖片上傳」與「簽名」皆為**獨立欄型**(圖片有縮圖/多張/排列;簽名為滑鼠觸控手寫存成圖片),且**明確區分簽名(欄位)與簽核(流程)**;Airtable/Teable 統一 attachment、無簽名欄。P0 = 兩欄型(jsonb,沿用 `[{key,name}]` 契約 → 零 migration 零新端點)+ **上傳 MIME 收斂為僅影像** + 多張上傳/縮圖預覽 + canvas 簽名板(自建 Pointer Events,零相依)+ 顯示面。**明確排除並附理由**:伺服器縮圖 Sharp / EXIF 剝除 / HEIC / Ragic 之簽名背景圖與移除邊界空白 / **合規電子簽章 TWCA(docs/23 v6.1 C2 已裁定 R2)**。**順帶修既有隱性缺陷**:`toDbValue` 原硬編 attachment 才 JSON 序列化 → 改以 `dbFieldType==='jsonb'` 判定。api 317 + web 45 + e2e 31 全綠(`image-signature.spec` 3 測)。commit a057008(後端)+ 0247c68(前端)。殘留:EXIF 剝除 / 伺服器縮圖 / HEIC。≈0.17 mo | [R1/image-signature-fields.md](R1/image-signature-fields.md) |
| **自訂按鈕 + 簽核流程(workflow UX)** | R1·後續-1 | ✅ **SHIPPED v1.0(2026-07-25;M1–M5 + FMEA A1–A10)**|docs/27 §6 後續-1(承四大 P0 模組):落地 §4 P1「按鈕與簽核=workflow UX 面」—— **自訂按鈕動作框架**(updateSelf 更新本表 / pushTo 資料拋轉 / openUrl;確定性 allowlist + 權限 gate + 冪等 + audit,docs/22 不變量)+ **簽核 state machine**(階層順序多步 + ZEN 金額路由 + 人核准 gate + 記錄鎖 + 簽核完自動執行)。**核心洞見:簽核=DB 狀態機(pending step 由 approve 推進)不需 DBOS durable execution;ZEN 只算路由決策**(OQ-AA-1/4)。裝 GoRules ZEN(MIT in-process);DBOS/並簽/Email·SMS 動作/留言@提及 → P1。migration 0012(button_def/action_audit/approval_def/instance/step_log)。記錄鎖用全域 interceptor(guard 早於 TenantGuard 拿不到租戶)。api 250 + web 19 e2e(actions-approval.spec)綠。commit d346451/6c352aa(後端)+ a4d48cd/ead550c(前端)+ 56adb24(spec)。殘留:⚠️ 完成狀態與 onComplete 非同一 tx(冪等兜底)/ 空角色送簽檢查 / 並簽 / Email·SMS 動作 / 留言@提及 皆 P1。≈0.45 mo | [R1/actions-approval.md](R1/actions-approval.md) |
| **標籤/QR 產生器 + 列印增強** | R1·後續-2 | ✅ **SHIPPED v1.0(2026-07-27;M1–M5 + FMEA P1–P10)**|docs/27 §6 後續-2(§4 P1/P2)。**範圍洞見(Ragic 證據)**:列印是三件事 —— ①標籤 maker **完全 in-app 設定**(選欄+樣式+標籤尺寸+平舖/一頁一標籤+數量參照欄,doc/40)、②友善列印**紙張/邊界/方向委派瀏覽器**(doc/149)、③合併列印/客製報表才需**上傳** .xlsx/.docx(Carbone,doc/42·138)。→ **P0 取 ①②(零新依賴、零 infra):barcode 欄 QR 渲染(複用已裝 qrcode.react)+ `label_def` 標籤產生器 + A4 平舖列印頁 + 批次 + `layout.print` 頁首頁尾/換頁**;**③範本上傳合併歸 P1,待 file-storage**(與 OQ-FTP-6 同一阻塞)。migration 0013。**M2 補 field-types-parity 之條碼顯示層殘留**(barcode 欄實際渲染 QR)。api 258 + web 22 e2e(print-merge.spec)綠。commit 4620642(後端)+ 7b3f138/3778afb/58b3539/de4d46d(前端)+ 674deac(spec)。殘留 P1:頁首頁尾未每頁重複(需 @page margin box 或伺服器端 PDF)/ 合併列印範本上傳(待 file-storage)/ 伺服器端 PDF 密碼·浮水印 / Code128。≈0.34 mo | [R1/print-merge.md](R1/print-merge.md) |
| 通知(通訊平台 LINE/Slack/…)+ 事件推送偏好 | P0-4b | ⬜(拆自 P0-4,與授權無耦合)| 待建 |
| Ops(觀測 / 健康檢查 / 部署硬化)| P0-4c | ⬜(拆自 P0-4)| 待建 |
| 每表單自動 API + Public Form + webhook | P0-5 | ⬜(zod-openapi;M6 deviation 於此落地)| 待建 |
| Ragic A–I 補齊(BI/報表/樞紐/mobile PWA/電子簽章基本/i18n/系統管理）| P1-I | ⬜ | 待建 |
| **介面語言 + 租戶自助翻譯** | R1·H-5 | 🚧 **M0 APPROVED — OQ-I18N-1..6 全採建議(2026-08-05)**;**重估 2 → ~5 人月**(未 cascade 進 docs/04)|🔴 **parity 的形狀不是「我方出貨三種語言」,是「租戶自己把介面翻成他要的語言」** —— Ragic 官方 `doc/84 介面翻譯` 逐字:內建**六種語言**,且租戶在「帳號設定 → 介面翻譯」挑語言 + 挑**介面分類**,**藍字是 key 不可編輯、綠字填譯文**,存檔即生效。`docs/25` 記的「中/英/日 2 人月」**低估了標的**。**站②大發現**:`next-intl@4.13.2`(MIT,已讀 LICENSE 本文)**早就裝在 `apps/web/package.json:33` 而全 repo 零 import**,且其 `getRequestConfig` 為**每請求非同步**、`messages` 型別本身就是 `DeepPartial`、另有 `getMessageFallback` / `onError` —— 租戶覆寫所需的三件事**套件原生全支援**,不必自建 resolver;`./extractor` + SWC plugin 可搬 ~5,000 條硬寫中文。**伺服器端訊息(`errors.ts`)也回中文,只做前端等於沒做**。OQ-I18N-1..6 待裁定 | [R1/i18n.md](R1/i18n.md) |
| **白牌:自訂網域 + 品牌客製** | R1·A-2 | 🚧 **M0 APPROVED — OQ-WL-1..7 全採建議(2026-08-05)**;**重估 2 → ~4.5 人月**(未 cascade)|三個 H 缺口裡**唯一對客戶明文承諾**的(`docs/23` R1「平台」列逐字「白牌自訂網域」),而它**不是 Ragic parity** → 必須自己過〈向上設計三條〉,三條逐條有一手依據。🔴 **條件①的逐字證據**:Ragic `doc/56` 自述其「公司網域」**「只是會進行轉址的動作……並不是資料庫帳號網址會改變」** —— 使用者最後仍站在 `ragic.com/youraccount`。🔴 **不得低估它的品牌客製**:登入頁五項(橫幅 / 商標換掉 Ragic logo / 歡迎詞 ×2 / 隱藏註冊鈕)+ 尺寸建議表 + **CMYK 色域擋錯**(踩過坑才有的細節)。🔴 **但它的「自訂載入頁面」要使用者寫 HTML/CSS**(官方還提示「增加 margin 屬性」去閃避進度條),且部分設定要**寄信給 Ragic Support** 才開 —— 兩者都是第一約束的反例。**真正的地基工作**:租戶解析目前完全由 session `activeOrganizationId` 決定(`auth-guard.ts:44-50`),自訂網域要求它**能吃 host** → `docs/25` 記的 1 人月明顯低估。OQ-WL-1..7 待裁定 | [R1/white-label.md](R1/white-label.md) |
| **伺服器端 PDF + 列印範本** | R1·後續-2b | 🚧 **M1 SHIPPED(2026-08-05)** —— 送出 → 渲染 → 存檔 → 下載可用(記錄頁「下載 PDF」按鈕);OQ-PDF-1..8 全採建議。**刻意偏離 OQ-PDF-2**:落在同一行程而非獨立服務,因為 Playwright 本來就把 Chromium 跑在另一個 OS 行程,「當機不連坐」是免費的,獨立服務買到的是映像體積與擴縮(部署層);`PDF_RENDERER` token 即該縫。**M1 兩個發現**:(a) 票只在 `running` 期間有效,第一版測試事後核銷 → 第一次就 404,而且**一條否定斷言因此空過**(404 的 body 自然不含票);(b) `window.location.href` 下載會 **401** —— 導覽不帶 `engineHeaders()`,**單元與整合測試都看不到**,真瀏覽器實走才指得出。**M1.1 清掉三項殘留**:產出物到期清理(`pdf.expire` 每小時,刪物件、列標 expired)· 子表明細(依 `parent_form_id`,子表單另過一次 `canRead`)· 多筆合併實測。🔴 **差點出貨一個 bug**:`UPDATE ... RETURNING` 回的是**新值**,照直覺寫 `SET object_key=NULL RETURNING object_key` 會拿到 NULL、物件永遠刪不掉,而列已標 expired **看起來一切正常** —— 改用 CTE,且**測試第一版對這個 bug 空過**(只斷言狀態與 NULL,兩種寫法都會過),已補 `storage.stat` 並實測會紅。**殘留**:A2 版面設計器 / 列印頁仍是第二份排版 / 浮水印 + 附件 PDF 合併。該列**重估 4 → ~6 人月**(未 cascade)|現況產物是 HTML + `window.print()`,**「寄一份 PDF 給客戶」做不到**。🔴 **Ragic 的實作是 Node + Puppeteer 獨立服務**(官方 KB `doc-kb/284` 逐字含版本 `puppeteer@14.4.1` 與啟動指令 `node .../nodeJs.js 8888` + `PDF_SERVER_URL` 設定)→ HTML→headless→PDF 這條路**有實證**,且它也是獨立行程。🔴 **它的「範本編輯器」其實是在 Word 裡寫一套參數語言**(`{d.1002578:html(inline)}` / `{c.now:formatD(...)}` / 子表要手寫 `[i]` 與 `[i+1]` / 只有 SYSAdmin 能建 / 附 10 條限制含「不可用全形引號」)—— 第一約束的位置,**我方不照抄**,改瀏覽器內版面設計器並復用 print-merge。⚠️ **不得宣稱「它什麼都沒給」**:它有**欄位語法產生器**。⚠️ 站②誠實記帳:Playwright 只是 **devDependency**,升為 runtime 會把 e2e 與 prod PDF 的升級節奏綁在一起 —— **「已經裝了」不等於免費**。🔴 FMEA P0 含「PDF 是值的又一個出口」(本輪已修過四次同型)與渲染器 SSRF / `file://`。OQ-PDF-1..8 待裁定 | [R1/server-pdf.md](R1/server-pdf.md) |

---

## 🔵 R2 · ERP 核心 + 計算層(補「算」取代 ERP)

| 模組 | Sprint | 狀態 | 文件 |
|---|---|---|---|
| **⭐ 語意計算綁定層(自由表單 ↔ 算,自助化)** | P0-6(命門地基）| ✅ **M0 APPROVED — OQ-CBL-1..8 全採建議**(R2 design-ahead;M1–M7 待 R2 計算層啟動;與 J/K/L/N 共用「接法」)| [R2/calc-binding-layer.md](R2/calc-binding-layer.md) |
| 財會 GL / AP / AR core | P0-6 | ⬜ | 待建 |
| 進銷 K + 生產 L basics | P0-7 | ⬜ | 待建 |
| 台灣電子發票 + 批號追蹤 | P0-8 | ⬜ | 待建 |
| 計算層(過帳期結 / 成本 / 估值 / MRP / FX;docs/18)| P0-6~ | ⬜ | 待建 |
| Q 多 ERP 對帳 bridge | P1-A | ⬜ | 待建 |
| ERP 進階(J/K/L+N/P)+ HR 完整 + 合規電子簽章 TWCA | P1-B~F | ⬜ | 待建 |

---

## 🟣 R3 · MES 現場 + ISO 品保(完整 MVP)

| 模組 | Sprint | 狀態 | 文件 |
|---|---|---|---|
| MES 現場 UI + Cloud/Edge Gateway + OEE/Andon | P0-9(T)+P1-G | ⬜ | 待建 |
| ISO 通用工具(文管/稽核/NCR/CAPA/客訴/供應商評鑑)| P0-9(U)+P1-H | ⬜ | 待建 |

---

## 🟠 R4 · 對標 + AI 智慧營運(持續演進)

| 模組 | Sprint | 狀態 | 文件 |
|---|---|---|---|
| 財會 / 生產 / 品質對標(Multi-book / MRP II / SPC / 進階 WMS 等)| Phase 4 | ⬜ | 待建 |
| AI 進階(Copilot / 智慧對帳 / 需求預測 / 異常偵測)| Phase 4 | ⬜ | 待建 |
| HR/CRM 對標 · 完整行動 App · 訂閱計費 | Phase 4 | ⬜ | 待建 |

---

## 下一步候選(依 docs/13 dependency)

> **✅ Gate P0-1 + P0-2 達成**(form-engine-core + form-designer-ui + grid-and-excel-import 皆 SHIPPED):瀏覽器可建表/加欄/填單/子表/檢視 + Excel-like 網格改格 + xlsx 推斷建表灌資料,引擎生成真實資料表 + RLS 隔離。

1. **F-2 Auth**|Better Auth + JWT + nestjs-cls;三模組對外上線硬前提(治 dev tenant header 殘留)
2. **P0-4 三層權限 + 通知 + Ops**|三層權限(表單/欄位/記錄)+ 通訊平台通知 + 監控
3. **P0-5 每表單自動 API + Public Form + webhook**;**P1-I Ragic A–I 補齊**(BI/報表/樞紐/mobile PWA/i18n)
