# Weyver 模組設計文件索引(按產品功能發布藍圖分類)

> **分類依據**|對齊 `docs/23 產品功能發布藍圖`(R1 完整 Ragic → R2 ERP+計算層 → R3 MES+ISO → R4 對標+AI)+ Foundation(Phase -1 基礎/橫切)。sprint 代號沿用 `docs/13`(P0-x=Phase 1、P1-x=Phase 2,歷史代號)。
> **檔案組織**|各模組 design doc 依所屬 release 置於子資料夾 `docs/modules/<R1|R2|R3|R4|foundation>/`;`_template.md`(骨架)與本索引留在根層。**新模組動工前先寫 M0 design doc,依 release 放對資料夾,並在本表登錄。**
> **流程**(每模組)|M0 DRAFT → OQ 裁定 APPROVED → M1..MN 實作(每 milestone 一 commit)→ FMEA 收尾 SHIPPED。詳見 `_template.md` + `memory/rule_module_design_flow`。
> **[P0] 研究即寫入 doc**|競品 / 規範 / 實測研究**當下就寫進模組 doc §0 並附來源連結**,不得只留在 task 或 commit message —— 2026-07-28 稽核發現六個模組的研究成果全散落在 repo 外,**來源 URL 差點永久遺失**。追溯補寫用 `## 0-bis`。
> ⚠️ **「附連結」不是形式要求**|2026-07-29 複核 `print-merge` 時,其只記文件編號未附 URL 的引用**被查出兩處歸屬錯誤**(把「紙張委派瀏覽器」記在 `doc/149` 名下,實際出處是 `doc/4`)。**編號無法自我驗證,錯了也看不出來**;連上去一看即知。凡引用外部文件一律附可點 URL。
> **[P0] 收尾必回填 `docs/25`**|模組 SHIPPED 時,除本表標 ✅ 外**必須**同步更新 `docs/25 功能完整對照清單` 之對應子功能列(✅ / 🟡 並註明界線)並重算覆蓋率 —— docs/25 是「仿 Ragic 進度」唯一權威,不回填則該數字失真(2026-07-28 實例:只落後四天即嚴重失真,須整份重查)。
> **狀態圖例**|✅ SHIPPED · 🚧 進行中(APPROVED / 實作)· 📝 M0 DRAFT · ⬜ 未起(規劃中)

---

## Foundation(Phase -1|基礎 / 橫切,gate R1 對外上線)

| 模組 | Sprint | 狀態 | 文件 |
|---|---|---|---|
| **訂閱計費(地基預留,非實作)** | F-8 | ✅ **SHIPPED v1.0(2026-07-28;M1–M3 + FMEA B1–B8)**|應決策方「先規劃預留,避免最後商業化有問題」。**核心主張:分開「計費自動化」(可延 —— 17 家客戶手工開帳單完全可行,docs/04 A7 已採此路)與「計費地基」(不可延)**,分界線為「**成本是否隨時間上升**」。**⚠️ 查出文件不一致**:docs/04 A7 說 **Phase 2**、docs/13 說 **Phase 4**、docs/23 說 **R4** —— 排期差兩階段 → OQ-SB-1(建議統一為 docs/04 原文之觸發條件「客戶數 > 10 家」,而非硬編階段)。**程式碼現況**:entitlement / 用量記錄 / 租戶生命週期**三者全缺**,但 `QuotaService` 單一檢查點 · `tenants.parent_tenant_id`(多廠區)· `TenantGuard`(停權攔截點)已是可用的縫。**不可延三項**:**用量歷史(晚做就永久失去,唯一不可回溯者)** · 生命週期狀態(要在 `TenantGuard` **每請求熱路徑**攔)· entitlement 檢查點(成本隨模組數**線性成長**)。**預留為零行為變化**:`tenants` 三個 nullable 欄 + append-only `tenant_usage_daily` + **P0 一律放行**的 `EntitlementService`。**FMEA 要點**:計費須 **fail-open**,不可與 authz 的 deny-by-default 混用(B2);停權採唯讀而非封鎖,否則客戶拿不回自己的資料(OQ-SB-5)。**落地**:`tenants` 三欄 + `tenant_usage_daily`(RLS;**app 車道只授 SELECT** —— append-only 由**權限**保證不靠自律,整合測以 `SET ROLE weyver_app` 斷言 UPDATE/DELETE 皆 permission denied)+ `EntitlementService`(全放行)+ `QuotaService` 四層 fallback + `TenantGuard` 停權唯讀(**GET 不查 DB**,不為每個讀取加 round-trip)+ `UsageService` 每日快照(席位 / MAU / 表單數 / 儲存位元組,advisory lock + 冪等可補算)。**零行為變化**(全租戶 active / plan NULL)。**實作期修正**:`records` 指標宣告未採集 → 移除並說明(逐張動態表 count 成本不成比例);`UsageModule` 自 @Global 拆出(相依 DDL_KNEX,否則污染所有測試模組圖)。**殘留 B6**:停權唯讀已保證,但 **docs/04 I「匯出所有資料」⬜ 未起 → 停權功能啟用前必須先具備匯出**,否則承諾是空的。**未做**:金流 / 帳單 / 發票 / 催繳 / 方案內容 / 管理端 UI / 停權自動觸發。api 360 綠(+21)。migration 0017。commit `1eaeb1f`(M1)/ `6af1de4`(M2)。≈0.16 mo | [foundation/subscription-billing.md](foundation/subscription-billing.md) |
| Monorepo + NestJS + PG 骨架 | F-1 | ✅ 隨 form-engine-core M2 落地(`apps/api`)| —(見 R1/form-engine-core §9-bis)|
| **Auth + 租戶 context + 使用者身分**(Better Auth + Argon2id)| F-2 | ✅ **SHIPPED v1.0**(M0–M5:引擎+DI · 對映表+IdentityService · AuthGuard session→tenant + 剝 client header + TenantGuard 環境分派 + 跨租戶隔離 e2e · `/api/auth/*` handler + 前端登入/註冊/登出 + 受保護 layout〔強制登入僅 prod〕· rateLimit/安全標頭/throttler 硬化 · auth.spec 固化 · FMEA P0 全緩解;§6-bis 登入分層+治理;org→tenant 走 afterCreateOrganization hook;**dev header→真實認證,R1 上 prod 硬前提達成**。三層 RBAC=P0-4;SSO/MFA=後續)| [foundation/auth.md](foundation/auth.md) |
| UI shell + design tokens + deploy | F-3 | ✅ 前端 v2.1(`packages/ui` + `/app`)| —(見 docs/14)|
| **二步驟驗證(MFA / TOTP)** | F-4 | ✅ **SHIPPED v1.0**(M0–M4:twoFactor plugin + 4 整合測 · 帳號設定啟用/停用 UI〔QR + backup codes〕· 登入二步 challenge〔/login/2fa,TOTP + 備用碼〕· verify 端點 rateLimit · mfa.spec 固化 · FMEA P0 全緩解;承 F-2;scope out email/SMS OTP · passkey · org 強制 · trustDevice)| [foundation/mfa.md](foundation/mfa.md) |
| **平台可靠性工程(冪等 / 配額 / metadata RLS 兜底 / 清理 job)** | F-6 | ✅ **SHIPPED v1.0(2026-07-28;M1–M5 + FMEA L1–L7)**|**不是新功能,是收斂跨模組 P1 殘留**:form-engine-core R7(mutation 無 idempotency key,AGENTS ⚙️ [P0] 鐵則缺口)· C5(建表 DDL DoS 無 per-tenant 配額)· T4(metadata 表已有 RLS FORCE 但 Drizzle 走特權連線 → superuser 繞過,現為 app 層單防線;F-2 已 SHIPPED 故治本條件成立)· C2(孤兒 pending form 清理)· R8(app 車道 statement_timeout)· file-storage S6(孤兒檔實體回收)· actions-approval(簽核完成與 onComplete 非同一 tx)。**明確排除並附理由**:outbox / 欄位級加密 / feature flag / circuit breaker(無外部相依,不為不存在的需求建設施)。**收斂之殘留全清**(core R7/C5/T4/C2/R8 · file-storage S6 · actions-approval)。關鍵實作:`TenantDb.withTenant` 為租戶 metadata 唯一入口(不曝露裸 db,漏設 GUC 於編譯期即不可能);簽核改「先副作用後定案」+ 冪等 key(跨車道無法同一 tx,新序嚴格優於原設計);清理 job 交易範圍 advisory lock。api 305 + e2e 25 全綠(20 新測)。人月出自 docs/04 v2.4 已編列之 I 平台可靠性工程 8 人月之 P0 子集。migration 0015/0016。**後續批次**:outbox / 不變量對帳 job / 欄位級加密 / feature flag / circuit breaker。≈0.36 mo | [foundation/reliability.md](foundation/reliability.md) |
| **影像處理(EXIF 剝除 / 縮圖 / HEIC)** | F-7 | ✅ **SHIPPED v1.0(2026-07-28;M1–M4 + FMEA P1–P10)**|收斂 image-signature-fields S3(無縮圖)/ S4(EXIF 隱私)/ HEIC 不支援 + file-storage 縮圖殘留,**三處殘留全清**。**設計由實測與競品證據推翻三項原前提**:(a) HEIC **不是加相依就能解** —— sharp 0.34.5 預建版無 HEVC 解碼器、維護者表態永不內含,且**專利不可解**(HEVC 池明文對雲端服務按用戶計費)→ 改走**前端轉檔**(`accept` 排除 HEIC 觸發 iOS Safari 自動轉 JPEG);(b) 縮圖**不需背景 job**(24MP→240px 僅 28ms)→ 同步做,省掉整套佇列與狀態機;(c) **重編碼剝 EXIF 是錯的** —— 世代性失真且 q95 反而**大 14%** → 改採 **JPEG marker 層無損切段**(APP1/APP13),已驗證壓縮資料位元組完全相同,兼顧 Airtable「不改原檔」精神與消除 GPS(**Partiful 2025-10 GPS 外洩事故**為警示先例)。另實測 1.6MB 檔案可宣告 149.8MP、sharp 預設 pixel 上限擋不住 → 顯式 `limitInputPixels = 50MP`。落地:`ImageProcessor`(無損切段 → orientation>1 才 `.rotate()` 重編碼 → 320px webp 縮圖**永不放大**,承 Ragic)+ `{key}.thumb.webp` 衍生 key(零 migration)+ `?variant=thumb`(**取不到縮圖回原檔,前端永不破圖**,省掉 Teable 式重生機制)+ 孤兒回收涵蓋縮圖。**實作期兩發現**:假 PNG fixture 觸發 500 → 補齊設計中已列但未實作的 422 `IMAGE_UNREADABLE`;`Content-Length` 誤用 `file_object.size`(已含縮圖之配額)致下載截斷 → 改取 `storage.stat()`。api 9 單元 + 4 整合 + web 3 e2e 綠(41 e2e 全綠)。commit `76ff412`(後端)/ `eec7509`(修正)/ `40140f3`(前端)。**殘留**:P9 iOS 自動轉檔前提**僅社群證據、桌面 harness 無法驗**(降級路徑已備:錯誤訊息明確指引)/ P6 `sharp.concurrency`·`MALLOC_ARENA_MAX` 與 P8 `supportedArchitectures` 歸部署硬化 / P7 既有影像不回溯 / OQ-IP-8 WASM 轉檔 P1。≈0.17 mo | [foundation/image-processing.md](foundation/image-processing.md) |
| **檔案儲存基礎設施(上傳/下載/附件欄)** | F-5 | ✅ **SHIPPED v1.0(2026-07-27;M1–M5 + FMEA S1–S11)**|**解三模組共同硬阻塞**(OQ-FTP-6 image·signature·attachment 上傳 / OQ-PM-1 合併列印範本上傳·PDF 寫回·浮水印)。P0:`StorageDriver` 抽象(local dev/on-prem + S3-compatible 涵蓋 R2·S3·GCS·MinIO,承 docs/11 §3.6 R2 決策 + §16 避 lock-in)+ 伺服器代理上傳(**magic bytes 驗型別**/大小/白名單/**生成檔名**)+ API 代理下載(表單 view + **欄位 hidden 檢查**、`Content-Disposition: attachment`+nosniff)+ `file_object`(0014,RLS 車道)+ 兩階段綁定(pending→bound)+ 租戶配額 + attachment 欄完成(欄型契約不動)。docs/22 上傳鐵則逐條對照。**已解鎖**:attachment 欄可上傳/下載/移除(STUB 移除),OQ-FTP-6 之 image·signature 與 OQ-PM-1 之範本上傳阻塞已除,由各模組續作。api 283 + web 45 + e2e 25 全綠(`file-storage.spec` 3 測)。commit 346f67d(M1)/ bdd8c88(M2)/ ee066d5(M3)/ 2a78956(M4 前端)。**v1.1(同日)**:清 P1 殘留 S7(記錄刪除後附件不可讀)+ S10(開機 schema 檢核)。**P1 殘留**:病毒掃描 ClamAV / presigned 直傳直下 / 縮圖 Sharp / image·signature 欄 / 範本上傳 / 孤兒實體回收 job(歸平台可靠性工程)。migration 0014。≈0.37 mo | [foundation/file-storage.md](foundation/file-storage.md) |
| **框架升版(NestJS 11 + Fastify 5)** | F-9 | ✅ **SHIPPED v1.0(2026-07-29;M1→M4,OQ-FU-1..7 全採建議)**|**程式碼零改動**完成 NestJS 10→11 + Fastify 4→5(4 升 1 移 1 加);`pnpm audit` **47→25**、**fastify 鏈 16→0**、critical 歸零。實走驗證 `/api/auth/*` 萬用字元路由、multipart 上傳/下載串流、錯誤信封無 `FST_ERR` 外洩。**🔴 實測推翻 M0 的頭號風險預測**:cron ×3 在 `@nestjs/schedule@6.1.3` + NestJS 11.1.28 下**不成立**(orchestrator 為靜態 provider 且以物件屬性賦值註冊,同名覆蓋不累積)——M1 改動保留但價值定位誠實下修為「消除對框架去重行為的隱性依賴」。釐清 **`fastify` 必須釘死與 adapter 同版**(11.1.28 釘 5.10.0),宣告 `^` 範圍會在上游發 5.11 時裂成兩份、重演 F-2 期型別重複。**殘留**:prod 需設 Better Auth `trustedProxies`(否則速率限制退化為全域共用桶)/ SIGTERM 時序未實測 / `fast-uri`·`find-my-way` overrides 待上游跟進後移除。以下為 M0 研究紀錄 ——|承 #102 部署前置。**盤查推翻原始範圍認知**:`@nestjs/platform-fastify` 把 `fastify` 當**直接相依且釘死**(10.x→4.28.1 / 11.x→5.2.1)→「升 Fastify 5」實質等於「升整個 NestJS 到 11」;這也回頭解釋 F-2 期「型別重複」踩點為何必須釘 4.28.1。**§0.1 更正 file-storage 的 CVE 歸屬錯誤**:CVE-2026-33806 不影響 4.28.1(其範圍為 5.3.2–5.8.4),真正命中的是 **CVE-2026-25223**(`< 5.7.2`,high)與 **CVE-2026-3635**;結論不變但論據更換,並補判準「引用 CVE 須連同版本範圍一起記」。**§0.3 最痛的 breaking change 暴露為零** —— path-to-regexp v8 只打 Express,本專案無 middleware / 無 setGlobalPrefix / 無 enableCors。**§4.1 揪出最大風險**:`ScheduleModule.forRoot()` 被呼叫 **3 次**,Nest 10 以 deep-hash 去重故現況正常,**Nest 11 改用物件參考 → 每個 cron 跑三次**(每分鐘通知派送 = 重複寄信、每日用量統計 = 計費數字錯),而 **561 個測試結構上抓不到**(單元測試不跑 cron)。ghostfolio 真實升級案的維護者原話:「Automated tests don't cover much in this case」。`pnpm audit` 現況 50 條,其中 **16 條在本模組射程** | [foundation/framework-upgrade.md](foundation/framework-upgrade.md) |

---

## 🟢 R1 · 完整 Ragic 平台(既有客戶遷移 land)

| 模組 | Sprint | 狀態 | 文件 |
|---|---|---|---|
| **動態權限(記錄範圍 + 指派)** | E-1 | ✅ **SHIPPED v1.0(2026-07-29)**|「業務只看自己客戶」已可用:角色 × 表單 × 動作可設 `own` 範圍,`member` 欄勾「指派即授權」即資料驅動指派。**強制點在 `AS RESTRICTIVE` RLS policy**(隨建表 provision,漏注入也不外洩)+ `assignees bigint[]`/GIN + 存取預覽面板。**實作期揪出兩個 P0**:(a) member 的數值 actor id 在填單送出邊界被字串分支**靜默丟掉**;(b) **`APP_DATABASE_URL` 未設時 app 車道回落到 migration 特權角色 → RLS 全面不執法**(租戶隔離一起失效),已補 prod fail-fast + 開機自檢 + dev `x-dev-real-authz` 逃生口。**殘留**:D3 lookup 顯示值不受來源表範圍管(併入 #113)。以下為 M0 研究紀錄 ——|docs/25 覆蓋率彙總後之下一順位:「業務只看自己客戶」是 Ragic 客戶普遍在用的能力,而 **Weyver 目前完全沒有**(表單可見 = 該表所有記錄可見;既有 owner 短路是**表單層**非記錄層)。**§0.1 推翻本模組原本的假設**:docs/04 記為「依欄位值判斷」暗示 Salesforce 式規則引擎,但 **Ragic 根本沒有條件規則引擎** —— 而是「**有序存取層級**(問卷式 / 佈告欄式等,內建『自己新增及被指派的』述詞)+ **member 欄位值驅動的指派**(欄位上一個勾選,且明載仍受表單級層級所限)」兩機制組合。**§0.2 企業級對照**:Salesforce criteria-based **上限 50 條且禁 lookup/公式/衍生欄位**、預計算 `__Share` 表非同步重算(>200 萬列需開 Defer);Odoo `ir.rule` 查詢時注入且 **global AND / group OR**(加第一條 group rule 反而收緊);**Airtable 完全無此能力**(Interface 過濾**非安全邊界**);**Notion 只綁 Person 型屬性** —— 與 Ragic 獨立收斂到同一受限模型。**§0.4**:RLS policy 內子查詢**每列執行一次**、非 LEAKPROOF 破壞 pushdown → **維持只做租戶兜底,動態述詞走應用層注入**。**§0.5 Krebs/Varonis 揭露之 Salesforce Community 外洩**根因為「規則正確但管理員理解錯」且產品無法預覽 → **預覽模擬器列 P0**。**v0.2 三路補研究(含本機 PG 16.13 / 30 萬列實測)推翻 v0.1 核心架構**:(1) **強制點由「應用層注入 WHERE」翻為 `AS RESTRICTIVE` RLS policy** —— 實測兩者**執行計畫完全相同**(BitmapOr + GIN,0.169 vs 0.16 ms)**零效能代價**,但 RESTRICTIVE **語意恆為 AND**,使用者篩選的 OR **語法上不可能逃出**,且**應用層漏注入也不外洩**;實測反例:少一層括號即 **103 倍外洩**(309 列 vs 3 列)。v0.1 據以否決 RLS 的推論對複雜 policy 成立、對簡單述詞不成立,已標 SUPERSEDED。(2) **指派存成固定系統欄 `assignees bigint[]` + GIN**:實測 0.16 ms vs junction + `OR EXISTS` 的 **265 ms**(planner 產生 hashed SubPlan 退化成掃全租戶);jsonb 計畫相同但多一層轉換。更關鍵是它讓**所有動態表共用同一份靜態 policy**(建表 provision 時一次寫入,消除「新表忘了套」),規則變更是**資料變更而非 DDL**。(3) **預覽採唯讀試算不做 impersonation**(「Login as user」觀測性不足易濫用,Varonis);承業界三分類 effective access / simulator / report-only 分階段。**⚠️ 授權警訊**:**NocoDB 與 Directus 2026 起已非 OSS**、Baserow `enterprise/` 專有、Teable `apps/*` AGPL → 一律只讀公開文件不看實作。**另記一個 pre-existing bug**:`record.service.ts:425-434` keyset 分頁在非 id 排序時會跳列/重複(已對碼確認,另立小項)。**相依**:`member` 欄前端未完成(與通知模組撞同一殘留)。≈0.28 mo | [R1/dynamic-permissions.md](R1/dynamic-permissions.md) |
| **通知系統(訂閱 / 提醒 / 通道)** | H-1 | ✅ **SHIPPED v1.0(2026-07-28;M1–M5 + FMEA N1–N17)**|**解當前最大功能斷裂** —— 簽核流程已 SHIPPED 卻無任何機制告知下一關的人。**緣由**:docs/25 v1.5 覆蓋率彙總指出 H 段僅 12%,且**簽核已 SHIPPED 卻無機制告知下一關簽核人** —— 做完的功能因缺另一功能而不可用,為當前最大功能斷裂。**§0 Ragic 證據**:通知三層結構(全域開關 / 逐表單四開關 / **事件×通道矩陣**)全由終端使用者自設;「跟我相關」官方定義 = 新增 + 指派 + 回應;共通篩選訂閱有**進入 / 離開**兩種觸發;**批次匯入與大量修改刻意不通知**(風暴防護,明載);提醒為宣告式(日期欄 + N 天前後 + **收件人來自欄位值** + 每日排程 + 同天同收件人**預設合併**)。**競品分歧**:Airtable 把通知做成 automation 的 action 且 **Editors 只能檢視設定** → 與本專案自助化命門衝突,故採 Ragic 訂閱模型。**§0.3 實查出改 scope 的相依**:「跟我相關」三要素中 Weyver 僅 `createdBy` 成立 —— **member 欄型無前端渲染、註解功能完全不存在** → OQ-NT-2。P0 = 站內通知中心 + Email + 三層訂閱設定 + 簽核接通 + 風暴防護;**提醒 / LINE 等通道 / 註解 明確排除並附理由**(提醒需 cron 地基,C 段整項未起)。**v0.2 補 §4 外部通道設計**(v0.1 僅一行「通道抽象」不足以決策),並**推翻 v0.1 自身判斷**:原估「加 LINE +0.1 mo 小批」錯誤 —— 成本在**綁定流程 + inbound webhook + 租戶憑證**而非送訊息 API;結論(P0 不做 LINE)不變但理由改為「**它大到應自成模組**」而非不重要。Ragic 綁定流程:加官方帳號好友 → LINE 內傳 `/validate|信箱` → **寄驗證信** → 15 分鐘內點連結完成(**驗證刻意繞回 Email 防冒用**)。**Weyver 無計費載體(訂閱計費 Phase 2)→ 只能走租戶自帶憑證**,確認 docs/04「通道連接設定 UI 1 人月」為 LINE 硬前提。**多租戶陷阱**:LINE userId 為 **Provider 作用域** ⇒ 綁定鍵必須含 `tenant_id`,否則跨租戶洩漏。§4.5 立五項抽象約束(收件人存 user_id 非位址 / 綁定獨立 / 憑證屬租戶 / 內容結構化 / **inbound 為一等公民**)。**v0.3 補 §4.6 個人 vs 群組**(決策方 review 揪出 v0.2 缺漏 —— **docs/04 §H 早已明載「個人 1:1 + 群組」且註明「台灣 SMB 日常 LINE 群組通知」**,v0.2 只設計個人綁定):兩者**非同一功能的兩種位址而是兩種功能**(收件人 / 訂閱者 / 權限模型 / 退訂 / 審計語意五面皆異)→ (a) 群組屬「事件廣播」不屬「通知訂閱」,設定入口在表單·租戶層,歸 docs/04 通知路由引擎;(b) **OQ-NT-9 對群組升級為不可協商**(群組廣播無任何權限模型可依靠);(c) **§4.5 第 1 條修正為多型 recipient** —— v0.2 原寫 `recipient_user_id` **是錯的**,群組通知沒有收件使用者。新增 OQ-NT-13/14 + FMEA N12(群組廣播洩漏 P0)· N13。**UI 裁定**:LINE 欄 P0 不顯示(無 driver 亦無連接頁 → 死控件)· 全部通知只做面板 50 則 · 設定放個人設定 · **簽核逾期豁免總開關但須明示**。**v0.4 §0.4「站在巨人的肩膀上」四路研究,推翻自身四項決定**:(1) **訂閱模型改單一有序 enum** —— GitHub/GitLab/Discourse/Zulip/Notion/Slack/Teams/Linear **無一例外**,且查不到任何系統從 enum 退回獨立開關(Zulip 反由二元→四檔);Ragic 4 開關 = 16 組含「別人建的通知、自己建的不通知」等無意義組合;**「與我相關」在成熟系統是自動訂閱地板非開關**;(2) **通知與寄送必須分兩張表** —— Discourse/GitLab/Novu 三家皆分,生命週期 / 寫入模式(dead tuple)/ 扇出 / 保留策略四者衝突;PostgreSQL-only 仍正確(pilot < 50 msg/s 遠低於實測 800 jobs/s 死亡螺旋);(3) **同記錄去抖動必做**(Jira 3min idle / 10min max + 關鍵事件 bypass),否則連續編輯 10 次 = 10 封信;跨記錄 digest 才可不做;(4) **自架 SMTP 在雲主機基本不可行**(IP 段被封 / port 25 封鎖 / 低量養不熱 IP)→ relay 共用 pool,**與 OSS-only 不衝突**(買 IP 信譽非軟體授權)。**OQ-NT-9 理由改寫**:真論據為「**欄位級權限使業界主流的收件人過濾失效**」(Jira/GitLab 無欄位級權限),佐以 CVE-2019-11544 / CVE-2021-39119 / CONFSERVER-52560。**🔴 FMEA N14(P0,已對碼驗證)**:`titleOf()` 取 `fields[0]` = 使用者自建任意首欄 → **通知標題本身即可能洩漏金額 / 身分證號**。N17:**PgBouncer tx mode 下 LISTEN/NOTIFY 不可用 + RLS FORCE 擋 worker 跨租戶掃描**,M1 第一件事確認。**落地**:四表(notification / delivery / pref / setting,皆 RLS FORCE)· 有序 enum 層級 + **沿用既有分類資源軸繼承**(不新建基礎設施)· 顯式 emit 風暴防護 · `safeTitle()` 型別上不接受欄位值 · 站內鈴鐺(Discourse 式部分索引算未讀)· Email 通道(relay / **抑制清單 P0** / 退避重試 / **同記錄去抖動** / `References` threading)· 三軸設定頁。**實作期揪出五個缺陷**:指名收件人被角色過濾而**丟掉無角色的送簽者** · `scope_id` 為 NULL 使唯一索引失效致**偏好每次修改都新增一列** · 應用時鐘與 DB 時鐘不一致使**去抖動永不到期** · 通道偏好誤做成整批共用 · `engineFetch` 無 body 的 POST 仍宣告 content-type 致 500(共用層,順修)。**殘留**:N8 送達率部署設定(SPF/DKIM/DMARC/VERP bounce 管線,**pilot 前必做,否則通知等於沒送出**)· N9 保留策略 · 提醒(待 cron 地基)· LINE 等通道(自成模組)· 註解 @提及。api 391 綠 / e2e 44 綠。migration 0018–0020。UI 稿 [notification-flow.html](../mockups/notification-flow.html)。≈0.33 mo | [R1/notifications.md](R1/notifications.md) |
| **匯入既有表單(upsert + 撤銷)** | #106 | ✅ **SHIPPED v1.0-M3(2026-07-29)**|**§4.2 決策表與 §4.3 schema 已全數補完**:命中多筆改為擋(原本 `Map.set` 直接覆蓋 = 文件明文否決的 Airtable 式靜默全部更新;**unique 約束擋不住**,因比對用的是正規化後的值)· 正規化命中警告 · 大量影響二次確認 · clear 打字確認**後端也驗**(前端有對話框但沒送後端,API 可繞過)· 撤銷保留期 30 天 · Idempotency-Key。**第三次「整合測綠、瀏覽器才爆」**:`approval_instance` 走 app 車道但該表刻意無 grant → 改走特權車道(反向注入 ActionsRepository 會造成模組循環)。以下為 M2 ——|四政策 upsert / 決策表 / plan 是 dry-run / commit 帶 planHash 防「看的是 A 檔送的是 B 檔」/ 撤銷為補償批次(before-after diff + per-field compare-and-set)。**M2 補**:未知選項 `create` 真正生效(原本 schema 收但不實作 → 使用者選了會整批失敗)· 合併儲存格填滿並回報(舊 Excel 單頭欄必然合併,原本靜默變空白)· 錯誤列 CSV 下載含注入防護 · **撤銷 UI**(原本有 revert 端點但沒有清單 → 使用者無從得知 batchId,功能等於不存在)。**子表語意維持 untouched**,append/replace 待真實客戶檔案樣本再設計。以下為 M0 研究紀錄 ——|**R1「既有客戶遷移」定位的硬破口**:Ragic 官方的匯入主入口是「既有 sheet → Tools → Import Data From File」,遷移後客戶**每天在做的是匯入既有表**,而本平台只做了「Excel → 建**新**表單」。**§0 研究推翻兩個原本判斷**:(a) 最會靜默毀資料的**不是**「檔案中沒有的欄位」—— NetSuite / Shopify / Airtable / Baserow **一致保留未映射欄位**;真正的戰場是**「有映射但儲存格空白」**,而業界在此分裂:Salesforce / NetSuite **預設保留**(安全),**Airtable 預設覆蓋、Shopify 覆蓋且無開關**(官方逐字「overwritten as blank」)。(b) 原本的 `import_batch_id` + soft delete 撤銷設計有 **5 個實質缺口**,最大兩個是「**更新型變更無 before-image 撤不回來**」(而遷移場景絕大多數是更新)與「**batch_id 掛在記錄上會被第二批覆蓋**」→ 改為側表 `import_batch_rows` 存 before/after **diff** + per-field **compare-and-set** 偵測「匯入後又被人改過」。**其他關鍵發現**:業界**無一家做原子交易**(Salesforce/NetSuite/Odoo/Shopify 全部分提交)→ 護欄是 dry-run + 可撤銷,不是 rollback;**檔案內 key 重複會讓 PG `ON CONFLICT` 直接爆錯**(cardinality violation),應用層必須先擋;**Airtable 擴充在既有命中多筆時「全部更新」且靜默**(官方逐字),**絕不採用**;**key 必須有 unique 約束**且由 DB 強制(NocoDB #3438 正是真實表架構下未強制而讓主鍵重複);**業界無一家做 no-op 偵測**(值沒變仍寫入,污染 `updated_at` 與稽核)→ 低成本差異化。**競爭切入點**:🔴 **Ragic 有 revert 但 revert 完資料是不一致的** —— 官方明載還原**無法復原且不會重新觸發**連結與載入 / 公式重算 / workflow。**Excel 本身是資料破壞源**:基因名事件 30.9% 論文受影響且五年未解;英國 PHE **15,841 筆確診因 XLS 列上限被靜默丟棄**致接觸者未匡列 → preflight 主動偵測前導零流失 / 科學記號 / 日期誤轉是業界共同破口上的差異化。**OQ-IMP-6 為推翻既有 OQ-GEI-3=A 之提案**(前端解析 → 後端;Airtable 的 25,000 列上限正是前端解析的代價,SheetJS 官方亦建議大檔在伺服器處理)。**前端解析的三處靜默吃錯資料已先修**(e068c6a:寫死首工作表 / 寫死第一列當標題 / 推斷只取前 50 列)| [R1/import-to-existing-form.md](R1/import-to-existing-form.md) |
| **表單引擎動態 schema 核心** | P0-1 | ✅ **SHIPPED v1.0**(2026-07-19;M0–M7,59 tests,FMEA P0 全清)| [R1/form-engine-core.md](R1/form-engine-core.md) |
| **表單設計器 + 填單 接引擎 API** | P0-1·UI | ✅ **SHIPPED v1.0**(2026-07-19;M0–M5,Playwright golden path 固化;**Gate P0-1 全數達成**)| [R1/form-designer-ui.md](R1/form-designer-ui.md) |
| Grid 主檢視 + Excel 建表 onboarding | P0-2 | ✅ **SHIPPED v1.0**(2026-07-19;M0–M4;Glide 網格改格 + xlsx 推斷建表 bulk 灌資料;e2e 固化 + FMEA P0 全清)| [R1/grid-and-excel-import.md](R1/grid-and-excel-import.md) |
| 公式引擎 + Link&Load | P0-3 | ✅ **SHIPPED v1.0**(2026-07-19;M0–M6;fork Teable MIT parser + Tarjan SCC 依賴圖 + Link&Load + Rollup N+1 + 讀時算注入 + 設計器啟用 + 即時預覽 + e2e 固化;66 tests;FMEA P0 全清)| [R1/formula-and-linkload.md](R1/formula-and-linkload.md) |
| **三層權限(授權層)** | P0-4a | ✅ **SHIPPED(M1–M7 + FMEA + 管理 UI)**|表單級 **動作集**(view/create/edit/delete/approve/export/design,M7 由 4 級升級,Guard 執法)+ 欄位級遮罩/寫白名單 + **role tree 部門繼承**〔OQ-1=C〕+ owner→admin 對映 + 權限管理 API + **管理 UI `/app/settings/permissions`**(角色樹+動作矩陣+欄位可見性,瀏覽器驗證往返);deny-by-default;縱深(RLS 仍為跨租戶最後防線);後端 163 tests 綠、§12 FMEA P0 全清。記錄級延 P1-I | [R1/authz.md](R1/authz.md) |
| **↳ 資源軸繼承(分類授權 + owner + 敏感旗標)** | P0-4a·uplift | ✅ **SHIPPED v1.0(2026-07-24;M1–M4 + FMEA)**|解逐表列舉維護膨脹(`O(表單)→O(分類)`):既有 `form_permissions` 重定位為**覆寫層**,其下補**分類授權層** `category_permissions` + owner 短路(`form_def.created_by`,**得資料動作 design 除外**)+ 敏感旗標(不吃繼承,admin-only)+ 租戶預設 profile(Salesforce OWD 式)+ 無權表三態(readable/鎖定 stub/敏感隱藏)。migration 0008 純加法惰性零回歸;分層解析 owner→覆寫→分類繼承→預設;管理 UI(FormMatrix 分類分組 + ResourceSettings + 工作台鎖定 stub);後端 40+ tests + 6 web e2e 綠(`permissions.spec` 固化)。**向上設計研究**(Notion/Salesforce/Drive/Purview,§10-bis)錨定 OQ-ARI-1..8。commit `4c9b76b`(後端)| [R1/authz-resource-inheritance.md](R1/authz-resource-inheritance.md) |
| **↳ 記錄工作台收斂(app-shell + 集合視圖 → Object Page)** | R1·workbench-uplift | ✅ **SHIPPED v1.0(2026-07-28;M1–M4 + FMEA E1–E3/U1–U4)**|**A0/A1 已由 workspace-ia 與 views-list 交付**(v0.5 對照程式碼查證重整;OQ-RWB-1 nested 路由被 views-list 之 nuqs mode/rid 取代)。本輪補完 A2–A5:`/api/users/lookup`(只回同租戶 role_members 之 {id,name},不回 email)+ `/records/:id/relations` 反向關聯(**來源改由 `field_def.options->>targetFormId` 推導** —— `relation_def` 僅於 registerRelation 時寫入會漏)+ 狀態章(首個 singleSelect;tone 只認明確設定之語意色,不臆測)+ 金額彙總 + 真實使用者名 + 關聯 rail 正反雙向 + **inline 檢視↔編輯**(不再跳設計器)+ 清單列 status/money enrich。api 311 + web 45 + e2e 28 全綠(`record-workbench.spec` 3 測)。commit 03e3175(後端)+ fb2104a(前端)。殘留:U3 狀態欄治本(OQ-3=B 設計器指定)/ E3 反向關聯單一 UNION / 選項顏色設定 UI(field-types-parity P1)|承 form-designer-ui;現況 Object Page 已誠實建好 ~70%(master-detail + 錨點 + label/value + 子表 rollup + 稽核)。補 R1 缺口:**A0 app-shell 密度**(全域 status bar + 首頁卡牆改工作面,回應「頁面單薄」;M1 先做)+ **① 集合視圖**(browse 網格)+ 狀態章 / 金額彙總 / 關聯 rail(正+反向,需小後端)/ user 名 / inline 編輯;OQ-6=C 單域 rail+status bar;R2/工作流(GL/簽核/批號)不放。UI 依 `docs/mockups/weyver-integrated-list-to-object.html` | [R1/record-workbench-ui.md](R1/record-workbench-ui.md) |
| **工作區 IA(分類目錄首頁 + app-shell)** | R1·UP-1 | ✅ **SHIPPED v1.0(2026-07-25;M1–M5 + FMEA W1–W6)**|docs/27 §6 順序 1(OQ-UP 全裁定後首發):首頁卡牆改**分類目錄**(復用 form_categories,三態可見性,空分類隱藏)+ 左 icon rail / 底部 status bar 信任訊號(OQ-RWB-6=C 落地)+ ⌘K command palette 導航搜尋(client-side 零洩漏)+ Object Page 動作列(複製/刪除/友善列印 + print CSS);**誠實不上**通知鈴鐺(無事件源)/工作項目(獨立模組)/右欄佔位。小後端:`GET /api/categories`(非 admin,tenant-scoped)+ forms DTO +categoryId/updatedAt。api 200 + web 7 e2e(`workspace.spec` 固化)綠。commit c15913a(後端)+ ec7504e(前端)| [R1/workspace-ia.md](R1/workspace-ia.md) |
| **視圖系統 + 集合(browse)視圖** | R1·UP-2 | ✅ **SHIPPED v1.0(2026-07-25;M1–M5 + FMEA V1–V12)**|docs/27 §6 順序 2(承 workspace-ia):落地 D2「Ragic 語意、Airtable 骨架」—— 每表恆有**集合 Glide 網格視圖**(套 view 選欄/篩選/排序、inline 編輯、前導「檢視」欄下鑽)+ 與 Object Page **雙模式**(nuqs mode/rid,列表為進表預設)+ `view_def`(0009,authz Tier-1 車道非 RLS)**儲存檢視三態**(個人/共通/預設,admin-gating + 預設唯一)+ facet 篩選(型別感知 operator、單層 AND\|OR)+ 多鍵排序 + 快速搜尋(textual ILIKE)+ 批次刪除 + client-side 匯出。records query 擴充單層 combinator(OR 包 group 不洩 tenant/deleted_at 邊界)+ 快速搜尋。**OQ-VL-2 修正 docs/27 §3**:forcedFilter 非 view 屬性 → 移出歸 authz 軸(Airtable 明文 view filter 非安全邊界 / Teable Authority Matrix)。api 220 + web 12 e2e(views.spec)綠。commit f986477(後端)+ 3300561/9ccda81/9c7cdba(前端)+ ad2a4c2(spec)。殘留 P1:伺服器端大量匯出 / autoNumber·formula 篩選 / `/me` capabilities | [R1/views-list.md](R1/views-list.md) |
| **分組 / 看板 / 行事曆檢視** | F-1 | ✅ **SHIPPED v1.0(2026-07-30;M1→M5,OQ-VG-1..9 全採建議)**|**核心決斷成立**:分組作為排序變形,keyset 完整保留、不必退回 offset。Kanban 完整共用分組地基(stack = 單欄 group-by 的一階特例),Calendar 走獨立區間路徑。拖曳走既有 PATCH → 天然吃到樂觀鎖/欄位級/RLS/簽核鎖四層,優於業界普遍的 LWW;失敗給三類具名訊息(Jira 靜默彈回是反面教材)。FMEA G1–G10 全緩解,G1(小計洩漏)/ G2(隱藏欄分組)/ G6(時區差一天)已反向驗證。**🔴 實走揪出既有 P0**: 未跟上 #105 的選項物件化 → **填單單選下拉、篩選、看板分欄全部空清單**,型別為 unknown 故靜默通過所有測試。api 583 + web 87 + e2e 3 全綠。以下為 M0 研究紀錄 ——|承 docs/25 §226 優先序(通知 H-1 / 動態權限 E-1 已 SHIPPED,本批為下一項:「客戶第一週就會撞到通知與報表」)。**兩路深度研究,多條結論取自競品原始碼**(Baserow / NocoDB / Teable 皆 OSS)。**核心決斷:分組是「排序的變形」而非聚合查詢** → group key 前置進排序鍵、複合 cursor 擴充,**keyset 分頁完整保留**(AG Grid 官方明載 infinite row model 不支援 grouping —— 前提是把分組理解成「先聚合再展開」;改成排序變形即無此限制)。**Kanban 的 stack = 單欄 group-by 的一階特例,可完整共用地基;Calendar 不可共用**(是區間重疊查詢,一筆佔多格,而 group-by 假設一筆屬一組)。**§0.3 聚合 × 記錄級權限**:PG 官方明載 policy 先於 user query 的 conditions/functions 求值 → `COUNT`/`SUM` **天然只算通過 RLS 的列**,唯一洩漏路徑是特權連線 —— 而**本 session 已三度踩到該類**(RLS 假綠 / `action_audit` 缺 grant / `approval_instance` 走錯車道);**Ragic 官方自承「報表快照以系統管理員權限產生,可能包含檢視者無權存取的資料」是現成反面教材**。拖曳走既有 `PATCH record` 不開後門,天然吃到樂觀鎖/欄位級/RLS/簽核鎖四層,優於業界普遍的 LWW;失敗須具名三種訊息(Jira 拖曳彈回卻無提示是有紀錄的反面教材)。時區以 RFC 5545 為錨:`date` 欄無時區、`dateTime` 依租戶時區分桶(Airtable 依瀏覽器時區導致「差一天」為經典抱怨)。≈0.42 mo | [R1/views-group-kanban-calendar.md](R1/views-group-kanban-calendar.md) |
| **2D 表單設計器(form-designer-ui uplift)** | R1·UP-3 | ✅ **SHIPPED v1.0(2026-07-25;M1–M5 + FMEA F1–F10)**|docs/27 §6 順序 3(承 views-list):落地 D1「2D 格線畫布=填單畫面」—— 既有線性設計模式 uplift 成 Excel 式 **2D 格線畫布**(row/col/span 擺位、拖曳、合併)+ **靜態敘述/圖片元素** + **表單分段**(列範圍)+ **欄位設定核心**(預設值 17 變數 / 唯讀 / 隱藏 / placeholder / 說明)+ **設計草稿模型**(批次 Save + Ctrl+Z)。**核心洞見**:版面皆 layout metadata、與資料正交 → `form_def.layout` JSONB(migration 0010),form-engine-core DDL/DML 鏈**不動**(低風險)。OQ-FD2-2 定調:layout 走草稿、結構性 DDL 維持即時(Ragic 排除清單);靜態=layout 元素非 field_def(OQ-3);分段=列範圍(OQ-4);畫布 CSS grid + dnd-kit(OQ-5)。create-time 預設值變數($DATE/$USERID 等)。api 226 + web 15 e2e(designer.spec,拖曳分步 mouse)綠。commit e073659(後端)+ cd7dc3a/4a2ee56/2445a5f(前端)+ a473a6a(spec)。殘留 P1:填單頁 Markdown sanitize / 格式 mask·民國年 / 條件式格式 / 全延遲結構性變更集。≈0.47 mo(四模組最大)| [R1/form-designer-2d.md](R1/form-designer-2d.md) |
| **欄位型別 parity(form-engine-core 增量)** | R1·UP-4 | ✅ **SHIPPED v1.1(2026-07-29 快照帶入補完)**|**帶入欄的 live/snapshot 顯式化**:`syncMode` 預設 live(既有零遷移)、snapshot 走物理欄 + lazy backfill、**重整先給 diff 再套用且逐筆稽核**(Ragic 對應功能是無差別覆蓋、無 diff、無記錄)、設計器文案不出現術語、帶入三層權限閘。**瀏覽器實走抓到整合測遮住的缺陷**:`action_audit` 對 app 車道缺 grant(測試當時走 superuser 車道 → 假綠;已補 migration 0029 並把測試改走 app 車道)。以下為 v1.0 —— |v1.0(2026-07-25;M1–M5 + FMEA T1–T10)|docs/27 §6 順序 4(承 form-designer-2d):落地 §2 欄位型別 P0 —— **系統欄 4 型**(投影 audit 零儲存)+ **rollup**(複用已完整 RollupService,接讀時 field type)+ **lookup**(複用 RelationService,讀時注入)+ **link 補完**(啟用/驗證/顯示標籤/link&load/reverse)+ **autoNumber pattern**(token 文法 + date + reset,counter table)+ **選項顏色 + 連動選項**(options 加法擴充,valueSchema 不變零遷移)+ **條碼生成**(前端 OSS lib 渲染,零 DB)+ **格式遮罩**(displayMask 顯示)。核心洞見:RollupService 已完整、formula withFormulas 讀時注入為 lookup/rollup/系統欄範本、link 部分已建。**OQ-FTP-6**:image/signature/attachment 上傳依賴檔案儲存基礎設施 → 排除 P0(自成 P1)。migration 0011(autonumber_counter)。核心洞見:RollupService 已完整、formula withFormulas 讀時注入範本、系統欄投影 audit 零儲存、virtual 欄(no-op buildColumn/baseQuery 排除)。M4 設計器進階 palette + 設定編輯器。api 236 + web 17 e2e(field-types.spec)綠。commit 0972cf7/d98ffa4/7d212f0(後端)+ e469aa1/ad8c9e7(前端)+ 58e6f1d(spec)。殘留:⚠️ lookup 欄位級權限 / 顯示層 QR·顏色 chip·連動過濾 / image·signature(file-storage)/ member·rich-text·reverse-query 皆 P1。≈0.45 mo | [R1/field-types-parity.md](R1/field-types-parity.md) |
| **條件式格式(form-designer-2d P1 解鎖)** | R1·UP-3b | ✅ **SHIPPED v1.0(2026-07-28;M1–M4 + FMEA G1–G7)**|form-designer-2d 之 P1 殘留。**§0 clean-room 證據:Ragic 與 Airtable 的模型相反,不可各取一半** —— Ragic=**表單級 + 欄位級著色 + 後者覆蓋 + 自由選色**,且表單頁與列表頁分開設定;Airtable=視圖級 + 整列著色 + 前者優先。Ragic-parity-first → 整套採 Ragic(OQ-CF-1/3/5/7)。**OQ-CF-2 需裁定**:Ragic 用自由選色,與 docs/14 §0.2 才剛立的「受控色盤非自由選色」衝突(建議沿用 12 tone,誠實代價=遷移色需映射)。**OQ-CF-8 明確排除條件式行為**(顯示/隱藏/唯讀/必填):條件式隱藏**不是安全邊界**、條件式必填只在前端即為裝飾 → 另立模組。P0 複用既有 filter 模型 + 純前端求值 + 存 form_def.layout → **零 migration 零新端點**。落地:求值器 16 單元測(含「反轉順序→結果反轉」證明覆蓋序)· 設計器面板(兩面獨立/覆蓋序常駐提示/12 色盤/即時預覽)· 記錄頁標題與值著色 + Glide themeOverride(每列快取)。**色值三處皆白名單查表**(後端 enum / chipToneTextClass / grid-tone)。api 321 + web 61 + e2e 38 全綠。UI 稿 `docs/mockups/conditional-format-flow.html`。commit 20aa281 + daa0c63。殘留:G6 未壓測 / 條件式行為(另立)/ 整列著色。≈0.16 mo | [R1/conditional-format.md](R1/conditional-format.md) |
| **選項顏色設定 UI(field-types-parity P1 解鎖)** | R1·UP-4c | ✅ **SHIPPED v1.0(2026-07-28;M1–M4 + FMEA C1–C6)**|`options.colors` 後端於 field-types-parity 已建、**前端從未有入口**(死參數);record-workbench-ui 狀態章已預備讀取。**§0 clean-room 證據推翻預設前提**:**Ragic 無 per-option 顏色**(其對應功能為**條件式格式**),per-option 色為 Airtable/Teable 範式 → 本模組是**增強而非遷移 parity**;兩競品實際色盤色數**文件未載**,任何色數皆為推測。**§0.2 攤開與 docs/14「裝飾配色一票否決」之正面衝突** → OQ-OC-1 交裁定(建議 A 只開 4 語意 tone)。另 OQ-OC-4 誠實指出:若求遷移 parity,**條件式格式(form-designer-2d P1 殘留)優先度應更高**。**裁定 OQ-OC-1=B 完整色盤**(用戶明示可推翻 docs/14;**解禁嚴格限「裝飾配色」一項**,漸層/pill/陰影/大留白仍一票否決,已寫回 docs/14 §0.2)。8 組類別色 token(去飽和、避開綠/琥珀/紅)+ StatusChip 12 tone(toneClass 兼安全白名單)+ 後端 enum 收斂與 colors↔choices 交叉驗證 + ChoicesEditor(逐項/即時預覽/自動配色)。**順帶**:edit-form-panel 422→299 行;**修 C6 潛在缺陷**(呈現用設定壞掉會擋住整張表的記錄寫入)。api 321 + web 45 + e2e 34 全綠。commit 47cfa4c + c0a8af7。殘留:條件式格式(OQ-OC-4)/ 整列著色(OQ-OC-3)。≈0.13 mo | [R1/option-colors.md](R1/option-colors.md) |
| **圖片欄 + 簽名欄(field-types-parity P1 解鎖)** | R1·UP-4b | ✅ **SHIPPED v1.0(2026-07-28;M1–M4 + FMEA S1–S8)**|field-types-parity 之 OQ-FTP-6 子件,阻塞由 F-5 file-storage v1.1 解除。**§0 clean-room 競品證據**:Ragic 明載「圖片上傳」與「簽名」皆為**獨立欄型**(圖片有縮圖/多張/排列;簽名為滑鼠觸控手寫存成圖片),且**明確區分簽名(欄位)與簽核(流程)**;Airtable/Teable 統一 attachment、無簽名欄。P0 = 兩欄型(jsonb,沿用 `[{key,name}]` 契約 → 零 migration 零新端點)+ **上傳 MIME 收斂為僅影像** + 多張上傳/縮圖預覽 + canvas 簽名板(自建 Pointer Events,零相依)+ 顯示面。**明確排除並附理由**:伺服器縮圖 Sharp / EXIF 剝除 / HEIC / Ragic 之簽名背景圖與移除邊界空白 / **合規電子簽章 TWCA(docs/23 v6.1 C2 已裁定 R2)**。**順帶修既有隱性缺陷**:`toDbValue` 原硬編 attachment 才 JSON 序列化 → 改以 `dbFieldType==='jsonb'` 判定。api 317 + web 45 + e2e 31 全綠(`image-signature.spec` 3 測)。commit a057008(後端)+ 0247c68(前端)。殘留:EXIF 剝除 / 伺服器縮圖 / HEIC。≈0.17 mo | [R1/image-signature-fields.md](R1/image-signature-fields.md) |
| **自訂按鈕 + 簽核流程(workflow UX)** | R1·後續-1 | ✅ **SHIPPED v1.0(2026-07-25;M1–M5 + FMEA A1–A10)**|docs/27 §6 後續-1(承四大 P0 模組):落地 §4 P1「按鈕與簽核=workflow UX 面」—— **自訂按鈕動作框架**(updateSelf 更新本表 / pushTo 資料拋轉 / openUrl;確定性 allowlist + 權限 gate + 冪等 + audit,docs/22 不變量)+ **簽核 state machine**(階層順序多步 + ZEN 金額路由 + 人核准 gate + 記錄鎖 + 簽核完自動執行)。**核心洞見:簽核=DB 狀態機(pending step 由 approve 推進)不需 DBOS durable execution;ZEN 只算路由決策**(OQ-AA-1/4)。裝 GoRules ZEN(MIT in-process);DBOS/並簽/Email·SMS 動作/留言@提及 → P1。migration 0012(button_def/action_audit/approval_def/instance/step_log)。記錄鎖用全域 interceptor(guard 早於 TenantGuard 拿不到租戶)。api 250 + web 19 e2e(actions-approval.spec)綠。commit d346451/6c352aa(後端)+ a4d48cd/ead550c(前端)+ 56adb24(spec)。殘留:⚠️ 完成狀態與 onComplete 非同一 tx(冪等兜底)/ 空角色送簽檢查 / 並簽 / Email·SMS 動作 / 留言@提及 皆 P1。≈0.45 mo | [R1/actions-approval.md](R1/actions-approval.md) |
| **標籤/QR 產生器 + 列印增強** | R1·後續-2 | ✅ **SHIPPED v1.0(2026-07-27;M1–M5 + FMEA P1–P10)**|docs/27 §6 後續-2(§4 P1/P2)。**範圍洞見(Ragic 證據)**:列印是三件事 —— ①標籤 maker **完全 in-app 設定**(選欄+樣式+標籤尺寸+平舖/一頁一標籤+數量參照欄,doc/40)、②友善列印**紙張/邊界/方向委派瀏覽器**(doc/149)、③合併列印/客製報表才需**上傳** .xlsx/.docx(Carbone,doc/42·138)。→ **P0 取 ①②(零新依賴、零 infra):barcode 欄 QR 渲染(複用已裝 qrcode.react)+ `label_def` 標籤產生器 + A4 平舖列印頁 + 批次 + `layout.print` 頁首頁尾/換頁**;**③範本上傳合併歸 P1,待 file-storage**(與 OQ-FTP-6 同一阻塞)。migration 0013。**M2 補 field-types-parity 之條碼顯示層殘留**(barcode 欄實際渲染 QR)。api 258 + web 22 e2e(print-merge.spec)綠。commit 4620642(後端)+ 7b3f138/3778afb/58b3539/de4d46d(前端)+ 674deac(spec)。殘留 P1:頁首頁尾未每頁重複(需 @page margin box 或伺服器端 PDF)/ 合併列印範本上傳(待 file-storage)/ 伺服器端 PDF 密碼·浮水印 / Code128。≈0.34 mo | [R1/print-merge.md](R1/print-merge.md) |
| 通知(通訊平台 LINE/Slack/…)+ 事件推送偏好 | P0-4b | ⬜(拆自 P0-4,與授權無耦合)| 待建 |
| Ops(觀測 / 健康檢查 / 部署硬化)| P0-4c | ⬜(拆自 P0-4)| 待建 |
| 每表單自動 API + Public Form + webhook | P0-5 | ⬜(zod-openapi;M6 deviation 於此落地)| 待建 |
| Ragic A–I 補齊(BI/報表/樞紐/mobile PWA/電子簽章基本/i18n/系統管理）| P1-I | ⬜ | 待建 |

---

## 🔵 R2 · ERP 核心 + 計算層(補「算」取代 ERP)

| 模組 | Sprint | 狀態 | 文件 |
|---|---|---|---|
| **⭐ 語意計算綁定層(自由表單 ↔ 算,自助化)** | P0-6(命門地基）| ✅ **M0 APPROVED — OQ-CBL-1..8 全採建議**(R2 design-ahead;M1–M7 待 R2 計算層啟動;與 J/K/L/N 共用「接法」)| [R2/calc-binding-layer.md](R2/calc-binding-layer.md) |
| 財會 GL / AP / AR core | P0-6 | ⬜ | 待建 |
| 進銷 K + 生產 L basics | P0-7 | ⬜ | 待建 |
| 台灣電子發票 + 批號追蹤 | P0-8 | ⬜ | 待建 |
| 計算層(過帳期結 / 成本 / 估值 / MRP / FX;docs/18)| P0-6~ | ⬜ | 待建 |
| Q 多 ERP 對帳 bridge | P1-A | ⬜ | 待建 |
| ERP 進階(J/K/L+N/P)+ HR 完整 + 合規電子簽章 TWCA | P1-B~F | ⬜ | 待建 |

---

## 🟣 R3 · MES 現場 + ISO 品保(完整 MVP)

| 模組 | Sprint | 狀態 | 文件 |
|---|---|---|---|
| MES 現場 UI + Cloud/Edge Gateway + OEE/Andon | P0-9(T)+P1-G | ⬜ | 待建 |
| ISO 通用工具(文管/稽核/NCR/CAPA/客訴/供應商評鑑)| P0-9(U)+P1-H | ⬜ | 待建 |

---

## 🟠 R4 · 對標 + AI 智慧營運(持續演進)

| 模組 | Sprint | 狀態 | 文件 |
|---|---|---|---|
| 財會 / 生產 / 品質對標(Multi-book / MRP II / SPC / 進階 WMS 等)| Phase 4 | ⬜ | 待建 |
| AI 進階(Copilot / 智慧對帳 / 需求預測 / 異常偵測)| Phase 4 | ⬜ | 待建 |
| HR/CRM 對標 · 完整行動 App · 訂閱計費 | Phase 4 | ⬜ | 待建 |

---

## 下一步候選(依 docs/13 dependency)

> **✅ Gate P0-1 + P0-2 達成**(form-engine-core + form-designer-ui + grid-and-excel-import 皆 SHIPPED):瀏覽器可建表/加欄/填單/子表/檢視 + Excel-like 網格改格 + xlsx 推斷建表灌資料,引擎生成真實資料表 + RLS 隔離。

1. **F-2 Auth**|Better Auth + JWT + nestjs-cls;三模組對外上線硬前提(治 dev tenant header 殘留)
2. **P0-4 三層權限 + 通知 + Ops**|三層權限(表單/欄位/記錄)+ 通訊平台通知 + 監控
3. **P0-5 每表單自動 API + Public Form + webhook**;**P1-I Ragic A–I 補齊**(BI/報表/樞紐/mobile PWA/i18n)
