# [F-8] 訂閱計費(**地基預留**,非實作)

> ✅ **狀態:SHIPPED v1.0(2026-07-28)** — M1–M3 完成,**零行為變化**(所有租戶皆 `active` / `plan_code` NULL,與加欄前完全相同)。api 360 綠(+21)。
> **本模組刻意不含**|金流 / 帳單 / 發票 / 催繳 / 自助升降級 —— 待 docs/04 A7 之營運觸發條件(付費客戶 > 10 家 或 手工開帳單月耗 > 8 小時)。
> **裁定摘要**|1=C 觸發條件非硬編階段 · 2=A 地基現在做 · 3=A seat 語意(另記 MAU 不計費)· 4=A 外部使用者不佔 seat · 5=A 停權唯讀 · 6=A 能力碼粒度 · 7=A 每日快照 · 8=A 只立結構不填方案內容。
> **範圍再確認(決策方 2026-07-28)**|「**先把功能預留,日後再補實際支付流程**」—— 本模組不含金流 / 帳單 / 發票 / 催繳。
> **上游**|docs/05 §2 定價模型(三段式)· docs/04 A7(訂閱計費 Phase 2、MVP 期手工對帳)· docs/13 §Phase 4 · docs/23 R4 · docs/11 §16(部署成本)
> **緣由**|決策方要求「先規劃預留,避免最後商業化會有問題」。**本模組的主張是:計費自動化可以延,但計費地基不能延** —— 理由見 §1。

---

> **研究豁免**|本模組的核心裁定是「**分開計費自動化與計費地基**」,而分界線是
> 「**成本是否隨時間上升**」——那是第一原理推導,不是「別家怎麼做」的問題。
> 三個不可延項(用量歷史 / 生命週期狀態 / entitlement 檢查點)之所以不可延,
> 理由分別是「**晚做就永久失去,唯一不可回溯者**」「要在每請求熱路徑攔」
> 「成本隨模組數線性成長」——三條都由本專案自身的架構決定,查 Stripe 或
> Airtable 的計費頁不會讓任何一條更成立或更不成立。
>
> 🔴 **刻意不補外部連結。** 本模組的 §0 是**對自家文件與程式碼的走查**
> (docs/04 A7 / docs/13 / docs/23 對「何時做計費」三處互相矛盾;
> 程式碼三個要件全缺但兩個縫已半成形),那本身就是可回查的一手證據。
> 硬湊十個競品連結只會是**引用表演** —— 而表演會讓整個研究指標失去意義。
>
> ⚠️ **這條豁免會列進 `_research-index.md` §D 供稽核。** 要質疑它,直接看上面的理由。

## 0. 現況走查(先講三件查證到的事)

### 0.1 ⚠️ 文件對「何時做計費」自己不一致

| 文件 | 位置 | 說法 |
|---|---|---|
| **docs/04** | A7 假設 | 「訂閱計費 **Phase 2** 才做(2026-07-16 決策)—— MVP 期用**手工/綠界對帳**」「首批客戶 > 10 家後自動化」 |
| **docs/13** | §Phase 4 清單 | 訂閱計費列於 **Phase 4**(+390 人月那一批) |
| **docs/23** | R4「行動 / 企業」 | 訂閱計費列於 **R4** |

**這不是文字瑕疵,是排期差了兩個階段。** 依 docs/23 solo 務實時程,R2 ≈ 4 年、R4 更遠 —— 若採 R4,意味著**手工開帳單要撐好幾年**。那未必是錯的決定(17 家客戶手工完全可行),但它**必須是被明確選擇的**,而不是三份文件各說各話的結果。→ **OQ-SB-1**

### 0.2 定價模型對系統的要求(docs/05 §2)

| 方案 | 月費 | 內容 |
|---|---|---|
| Starter | NT$10,000 | 表單引擎 + ERP core + **≤10 users** |
| **Pro** | NT$28,000 | + 生產/MES + ISO + 品保 + **≤25 users** |
| Enterprise | NT$50,000+ | + **多廠區** + AI 加值 + **不限 user** |
| 加購 user | NT$600/user/月 | 超出方案 seat |
| Onboarding | NT$50–200K 一次性 | 資料遷移 + 導入 + 教育訓練 |

**拆出三個系統需求**:
1. **模組包 entitlement** —— 方案決定「哪些模組可用」(MES / ISO / AI 是 Pro 以上)
2. **seat 上限 + 超額計數** —— 需要「目前有幾個計費使用者」這個數字,且需**歷史**(某月超額幾人)
3. **多廠區** —— Enterprise 專屬;`tenants.parent_tenant_id` **已存在**(F-2 預留),這個縫已經在了

### 0.3 程式碼現況:三個要件全缺,但兩個縫已半成形

| 要件 | 現況 | 查核 |
|---|---|---|
| entitlement / plan | ❌ **完全不存在** | 全庫無 `plan` / `entitlement` / `feature flag`;`tenants` 只有 9 欄,**每個租戶拿到完全相同的功能面** |
| 用量記錄 | ❌ **完全不存在** | 全庫無 `usage` / `metering` / `seat` 計數 |
| 租戶生命週期 | ❌ **完全不存在** | `tenants` 無 `status`;無 trial / 停權 / 取消概念 |
| 資源配額 | 🟡 **半成品的縫** | F-6 已有 `tenants.maxForms/maxFieldsPerForm/maxRecordsPerForm`(nullable = 用系統預設)+ `QuotaService` 單一檢查點 —— **這正是 plan 應該去設定的東西**,只差「誰決定這些值」 |
| 多租戶層級 | 🟡 **已預留** | `tenants.parent_tenant_id` 已存在(F-2 為 AUTH-8 場景 B 預留),多廠區集團計費可用 |
| 單一攔截點 | ✅ **已存在** | `TenantGuard` 是每個請求解析租戶的唯一入口 → 停權檢查的自然位置 |

---

## 1. 核心主張:**分開「計費自動化」與「計費地基」**

這是本模組唯一重要的判斷,其餘都是它的推論。

### 1.1 可以延後的(**現在不要做**)

金流串接(綠界 / 藍新 / Stripe)· 帳單產生 · 發票開立 · 催繳 dunning · 自助升降級 · proration 換算 · 折扣碼 · 試用轉換漏斗分析。

**理由**|這些**全都是自足的後加模組**,不侵入既有程式碼,且 17 家客戶手工開帳單完全可行(docs/04 A7 已明確採此路)。現在做等於為不存在的規模建設施。

### 1.2 **不能**延後的(理由不同,要分開講)

| 要件 | 為什麼不能延 |
|---|---|
| **① 用量歷史** | **過去無法重建。** 沒有逐日記錄,日後(a) 無法驗證 docs/05 的定價假設是否成立、(b) 客戶爭議「我上個月沒那麼多人」無從查證、(c) 無法知道超額。這是**唯一一種「晚做就永久失去」的資料**,其餘都只是工。 |
| **② 租戶生命週期狀態** | 停權 / 試用到期要在 **`TenantGuard`** 攔 —— 那是**每一個請求**都會過的熱路徑。現在加一個 nullable 欄 + 一次查詢是零成本;日後在已上線系統的熱路徑動刀是另一回事。 |
| **③ entitlement 單一檢查點** | 若模組持續在**沒有任何 entitlement 縫**的前提下累積,日後要加方案分級,就得回頭逐模組改 —— **成本隨模組數線性成長**,而模組數正在快速增加。現在立一個 `EntitlementService`(P0 一律回 `true`)成本近乎零,但把呼叫點的位置固定下來了。 |

> **要點**|①②③ 的共同性質是「**成本會隨時間上升**」;§1.1 那些的成本**不隨時間變化**。這就是分界線。

---

## 2. 具體預留(最小改動,零行為變化)

### 2.1 `tenants` 加三欄(全 nullable / 有預設 → 零遷移風險)

| 欄 | 型別 | 用途 |
|---|---|---|
| `status` | text default `'active'` | `trial` / `active` / `past_due` / `suspended` / `cancelled` |
| `plan_code` | text nullable | `starter` / `pro` / `enterprise`;NULL = 不受方案管(現況) |
| `trial_ends_at` | timestamptz nullable | 試用到期 |

### 2.2 `tenant_usage_daily`(append-only)

以「日 × 租戶 × 指標」為粒度累積:計費使用者數 · 表單數 · 記錄數 · 儲存位元組 · API 呼叫數 · (日後)AI token。

**設計要點**|**append-only 不可改**(承 AGENTS「傳票不可變」精神 —— 用量是計費憑據);由每日 job 寫入(復用 F-6 `cleanup.service` 的 advisory lock 模式);**保留期長於任何帳務爭議窗口**。

### 2.3 `EntitlementService` —— 單一檢查點,**P0 一律放行**

```
canUse(tenantId, capability): Promise<boolean>   // P0: return true
seatLimit(tenantId): Promise<number | null>      // P0: null(不限)
```

**關鍵在於呼叫點就位,不在於它現在會不會擋。** 現在把「這個能力屬於哪個方案」這件事**有地方可寫**,日後填表即可。

### 2.4 `QuotaService` 接上 plan

現行 `quotaFor()` 已是 `tenants` 欄 → env 預設 的兩層 fallback。改為三層:**`tenants` 覆寫 → plan 預設 → env 預設**。這是既有程式碼的自然延伸,不是新設施。

### 2.5 `TenantGuard` 加停權檢查

`status ∈ {suspended, cancelled}` → 拒絕(唯讀例外見 OQ-SB-5)。P0 因所有租戶都是 `active`,行為完全不變。

---

## 3. scope 切分

| 里程碑 | 內容 | 估算 |
|---|---|---|
| **M1 地基** | `tenants` 三欄 + `tenant_usage_daily` + `EntitlementService`(全放行)+ `QuotaService` 接 plan + `TenantGuard` 停權檢查 + 測試 | 0.08 mo |
| **M2 用量採集** | 每日 job 寫入用量 + 計費使用者定義 + 管理端查詢 | 0.06 mo |
| **M3 收尾** | spec + FMEA + doc v1.0 + MODULES + **docs/25 回填** | 0.02 mo |

**合計 ≈ 0.16 mo。** 對照 docs/04「訂閱計費 6 人月(對標)」—— 本模組**刻意只做其中不可延的部分**,金流 / 帳單 / 發票留在 Phase 2 或 R4(OQ-SB-1)。

---

## 4. 台灣在地考量(日後實作時的已知約束,現在只記錄)

- **自家開發票**|Weyver 賣給台灣公司必須開**電子發票**(B2B 三聯式)。注意:docs/04 模組 **O 是做給客戶用的 ERP 電子發票**,與「Weyver 自己對客戶開票」是**不同的租戶脈絡**,不要假設可直接複用。
- **金流**|綠界 / 藍新為台灣 SMB 常見;docs/04 A7 已點名綠界。國際卡務(Stripe)對台灣 B2B 年繳非必要。
- **年繳為主**|docs/05 的 ACV 以**年**計(NT$120K–600K+),不是月訂閱心智 —— 帳務週期設計應以年約 + 期中加購 seat 為主,而非 SaaS 常見的月循環。
- **Onboarding 一次性費**|NT$50–200K,屬**專案收入非訂閱**,帳務上要分開(影響 ARR 計算的正確性)。

---

## 10. 開放問題(OQ-SB-N)— ✅ 已裁定 2026-07-28(全採建議)

| # | 議題 | 選項 | 建議 |
|---|---|---|---|
| **OQ-SB-1** ⭐ | 計費**自動化**排在哪一階段(文件現有三種說法) | A. **維持 docs/04 A7:Phase 2**,並修正 docs/13 / docs/23<br>B. 改採 docs/13/23 的 **R4**,並修正 docs/04<br>C. 不定,依「客戶數 > 10 家」觸發 | **C,但要寫成明確觸發條件** — docs/04 A7 原文其實就是 C(「首批客戶 > 10 家後自動化」),只是後續文件把它硬編成階段。**建議把觸發條件列為正式門檻**(客戶數 > 10 **或** 手工開帳單月耗 > 8 小時),三份文件統一指向它。理由:計費自動化的驅動力是**營運痛感**不是開發階段 |
| **OQ-SB-2** ⭐ | 本模組(地基)何時做 | A. **現在做**(≈0.16 mo)<br>B. 與計費自動化同批 | **A** — 這正是提問的原點。§1.2 三項成本隨時間上升,其中**用量歷史晚做就永久失去**。0.16 mo 買一個不可逆風險的保險 |
| **OQ-SB-3** | 「計費使用者」怎麼定義 | A. **有角色指派且未停用的 `users`**<br>B. 當月有登入者(MAU)<br>C. 所有 `users` 列 | **A** — 對齊 docs/05「≤10 / ≤25 users」之 seat 語意(買的是**席位**不是活躍度);B 會讓帳單隨使用波動、客戶難預算;C 會把已停用帳號算進去。**但要記錄 B 的數字**作為觀察(不計費),日後若要改模型才有歷史 |
| **OQ-SB-4** | 外部使用者 / 訪客是否佔 seat | A. **不佔**(承 Ragic:外部使用者不佔 seat)<br>B. 佔 | **A** — docs/04 E 段「外部使用者(客戶/供應商填單帳號,**不佔 seat**)」已明列為 Ragic parity。此處只是與計費一致化。**注意**:該功能目前 ⬜ 未起,但定義先立好,免得日後補做時計費語意才吵 |
| **OQ-SB-5** | 停權時的行為 | A. **唯讀**(可登入可看可匯出,不可寫)<br>B. 完全封鎖<br>C. 僅橫幅警告不限制 | **A** — B 會讓客戶連自己的資料都拿不出來,對 ERP 級系統是**不可接受的商業風險**(且可能有法律爭議:資料是客戶的)。A 保留取回資料的路徑,同時停止價值累積。**與 docs/04 I「備份/匯出所有資料」相依** —— 該項目前 ⬜ |
| **OQ-SB-6** | entitlement 的粒度 | A. **能力碼**(`mes.execution` / `ai.assistant`)<br>B. 模組碼(A–U)<br>C. 逐功能 | **A** — B 太粗(AI 加值跨多模組)、C 太細(維護成本高於價值)。能力碼可由模組自行宣告,與 docs/04 的模組劃分**解耦** —— 因為方案分級(Starter/Pro/Enterprise)切的是**客戶感知的價值包**,不是我們的程式碼模組邊界 |
| **OQ-SB-7** | 用量記錄粒度 | A. **每日快照**<br>B. 每次事件即時累加<br>C. 每月一次 | **A** — B 在寫入熱路徑加負擔且需去重;C 粒度太粗,無法回答「哪一天開始超額」。每日快照由背景 job 產生,不碰請求路徑 |
| **OQ-SB-8** | 是否現在就填方案內容(哪個能力屬哪個方案) | A. **不填**,只立結構,`plan_code` 一律 NULL<br>B. 現在就依 docs/05 填三個方案 | **A** — docs/05 明載其定價「**是模型不是斷言**」,且 §10 尚有待定假設。現在把方案內容硬編進系統,等於把未定案的商業決策固化成程式碼。結構先立,內容待商業決策 |

---

## 12. 失效場景反思(FMEA)— ✅ M3 確認(2026-07-28)

| # | 場景 | 緩解 | Sev | 狀態 |
|---|---|---|---|---|
| B1 | **停權邏輯誤傷正常租戶** → 全客戶無法使用 | `status` 預設 `'active'`;`isReadOnlyStatus()` 為**白名單式**(只有 `suspended`/`cancelled` 回 true);單元測逐一斷言 `trial`/`past_due`/未知值/空字串/`null`/`undefined` **全部放行** | **P0** | ✅ 已緩解 |
| B2 | entitlement 誤擋已付費客戶功能 | `canUse()` 一律回 `true`;檔頭以 ⚠️ 明載 **fail-open,不可與 authz 的 deny-by-default 混用**;測試以「根本不存在的能力碼」斷言放行;`planFor()` 查無租戶亦回 `active` | **P0** | ✅ 已緩解 |
| B3 | 用量記錄跨租戶錯置 → 帳單算到別人頭上 | `tenant_usage_daily` 帶 `tenant_id` + RLS FORCE + policy;整合測以兩租戶斷言互不汙染(A 2 席 / B 1 席、A 1 表 / B 0 表) | **P0** | ✅ 已緩解 |
| B4 | 用量 job 漏跑一天 → 該日永久空缺 | 唯一鍵 `(tenant_id, day, metric)` + `onConflict().merge()` → 冪等;`run(day)` 可補算任意日期;整合測斷言「重跑不產生重複列」與「補算不影響其他日」 | P1 | ✅ 已緩解 |
| B5 | 計費使用者定義變更 → 歷史數字語意不一致 | 存**指標碼**而非固定欄位;檔頭明載「新定義用新碼並存(如 `billable_users_v2`),舊碼原樣保留」 | P1 | ✅ 已緩解 |
| B6 | 停權後客戶無法取回資料 → 商業 / 法律爭議 | `TenantGuard` 只擋寫入方法,**GET/HEAD/OPTIONS 一律放行**(且不查 DB,不為每個讀取加 round-trip);測試斷言 suspended 租戶 GET 仍 200 | P1 | ⚠️ **部分**|唯讀路徑已保證,但 **docs/04 I「匯出所有資料」仍 ⬜ 未起** → **停權功能實際啟用前必須先具備匯出**,否則「可取回資料」的承諾是空的 |
| B7 | 手工開帳單期間無用量佐證 → 收費爭議 | M2 即開始每日採集,**早於**自動化計費上線 | P1 | ✅ 已緩解 |
| B8 | 用量表無限成長 / 被誤刪 | app 車道**只授 SELECT**,不給 UPDATE/DELETE —— append-only 由**權限**保證不靠自律;整合測以 `SET ROLE weyver_app` 斷言兩者皆 permission denied。量極小(百家租戶 × 4 指標 × 365 ≈ 15 萬列/年)**刻意不設清理 job** | P2 | ✅ 已緩解 |

**P0 全數緩解**;殘留 1 項(B6)且**阻塞條件明確**:停權功能不得在「匯出所有資料」具備前啟用。

### 12.1 已知未做(刻意)

- **停權沒有觸發者** —— `status` 目前只能人工改 DB。自動轉 `past_due`/`suspended` 屬計費自動化範圍(OQ-SB-1 觸發條件)。
- **方案內容為空** —— `PLAN_QUOTAS` 與 `canUse()` 皆未填(OQ-SB-8=A),待商業決策。
- **無管理端 UI** —— `UsageService.history()` 為服務層方法,尚無 HTTP 端點與畫面。手工開帳單期間可直接查 DB。

---

## 13. 變更紀錄

| 日期 | 版本 | 變更 | 作者 |
|---|---|---|---|
| 2026-07-28 | **v1.0 SHIPPED** | M1–M3 完成,**零行為變化**。**M1** `tenants` 三欄(status/plan_code/trial_ends_at)+ `tenant_usage_daily`(RLS + **app 車道只授 SELECT**,append-only 由權限保證)+ `EntitlementService`(全放行 / fail-open)+ `QuotaService` 四層 fallback + `TenantGuard` 停權唯讀檢查;**M2** `UsageService` 每日快照(席位 / MAU / 表單數 / 儲存位元組)+ advisory lock + 冪等可補算;**M3** FMEA 確認 + docs/25 回填。**實作期三個修正**:(a) `records` 指標宣告卻未採集 → 移除並說明理由(逐張動態表 count 成本不成比例、定價亦不以此計);(b) `UsageModule` 自 `@Global` BillingModule 拆出 —— `UsageService` 相依 `DDL_KNEX`,放進全域會讓每個只想測 auth 的測試模組圖被迫提供 Knex;(c) 測試以顯式 `undefined` 傳參會觸發預設參數 → 改用 `null` 當哨兵。api 360 綠(+21)。migration 0017。**未做**:金流 / 帳單 / 發票 / 催繳 / 方案內容 / 管理端 UI / 停權自動觸發 | Claude Code |
| 2026-07-28 | v0.1 | 初版 DRAFT —— 應決策方「先規劃預留,避免最後商業化有問題」。**核心主張:分開「計費自動化」(可延,手工可撐)與「計費地基」(不可延)**,分界線為「成本是否隨時間上升」。**§0.1 查出文件不一致**:docs/04 A7 說 Phase 2、docs/13 說 Phase 4、docs/23 說 R4,排期差兩階段 → OQ-SB-1。**§0.3 程式碼現況**:entitlement / 用量 / 生命週期**三者全缺**,但 `QuotaService` 單一檢查點、`tenants.parent_tenant_id`、`TenantGuard` 已是可用的縫。**§1.2 不可延三項**:用量歷史(**晚做就永久失去**)· 生命週期狀態(在 `TenantGuard` 熱路徑)· entitlement 檢查點(成本隨模組數線性成長)。**§2 預留為零行為變化**:三個 nullable 欄 + append-only 用量表 + P0 全放行的 `EntitlementService`。**§4 台灣在地**:自家開發票 ≠ 模組 O(不同租戶脈絡)· 年繳非月循環 · onboarding 費為專案收入不入 ARR。OQ-SB-1..8 待裁定;FMEA B1/B2 點出**計費須 fail-open,不可與 authz 的 deny-by-default 混用** | Claude Code |
