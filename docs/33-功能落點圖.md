# Weyver 功能落點圖(每個功能住在哪個 surface / 選單 / 面板)

> **這份文件回答的問題**|「這個功能在哪裡?」
> `docs/25` 回答「有沒有做」、`docs/24 §6` 回答「屬於哪個 surface」——
> **缺的是子功能級的落點**,而那正是 mockup 一天改六版、其中三版是被「發現功能/落點」打斷的原因。
>
> **合併兩件事**|task #158(落點圖)與 task #156(IA 重新歸位)是同一件事的兩半:
> 盤點就是落點圖,而盤點完自然看得出哪些擺錯了。
>
> 對碼盤點日期:**2026-08-03**(以 `apps/web/src/app` 實際路由與元件為準,不引用規劃文件)

---

## 1. 現況:Weyver 一共只有四個 surface

| Surface | 路由 | 裡面有什麼 |
|---|---|---|
| **工作區** | `/app` | 分類目錄首頁 · 最近使用 · ⌘K · 通知鈴鐺 · status bar |
| **設計器** | `/app/builder` | 欄位設計 / 2D 版面 / 欄位設定 / 條件式格式 / 型別轉換 · **動作按鈕** · **簽核定義** · **標籤設計** · **列印設定** · Excel 匯入建表 · 記錄面板(表單/網格/清單) |
| **表單** | `/app/forms/[formId]` | 六個檢視(列表 / 看板 / 行事曆 / 樞紐 / 圖表 / 記錄)· 篩選排序 · **匯出 Excel** · **批次刪除** · 匯入既有表單 · 記錄頁動作(編輯/複製/刪除/列印/簽核) |
| **設定中心** | `/app/settings/*` | 12 頁:公司 / 成員 / 權限 / 通知設定 / 通知通道 / **公開表單** / 整合(Webhook·API 金鑰)/ 資料匯出 / 回收桶 / 帳號安全 / 簽核代理人 / 個人設定 |

另有一條獨立列印路由:`/app/forms/[formId]/labels/[labelId]/print`。

---

## 2. 🔴 IA 錯位:三處,而且性質不同

### 2.1 per-form 的設定放在租戶設定頁,靠下拉選對象(**最該修**)

對碼查證(`apps/web/src/app/app/settings/`):

| 設定 | 實際粒度 | 現在住哪 | 問題 |
|---|---|---|---|
| **公開表單** | **per-form**(`formId` 為必要參數) | `/app/settings/public-forms` | 使用者人在表單上,想把**這張表**公開,卻要離開表單 → 進設定 → **再把同一張表單選一次** |
| **通知設定** | 有 per-form 層(`scope === "form"` + 表單下拉) | `/app/settings/notifications` | 同上。租戶層那一半放這裡是對的,**表單層那一半不是** |
| Webhook / API 金鑰 | 真·租戶級(端點 + 事件型別,不綁表單) | `/app/settings/integrations` | ✅ **放對了**,不要動 |

**這不是缺功能,是入口反了。** 兩者都已 SHIPPED、都能用,只是使用者找不到 ——
而「找不到」與「沒有」對客戶而言沒有差別。

### 2.2 表單層動作散在設計器與檢視兩邊

| 動作 | 現在住哪 | 心智上該在哪 |
|---|---|---|
| 匯出 Excel · 批次刪除 | 表單檢視工具列 | ✅ 對 |
| 匯入既有表單 | 表單檢視 | ✅ 對 |
| **標籤設計 · 列印設定** | **設計器** | ⚠️ 設計標籤是設計行為(對),但**印**是日常操作 —— 目前要印得先進設計器 |
| **動作按鈕 · 簽核定義** | **設計器** | ✅ 對(定義是設計行為),但**沒有任何地方看得到「這張表有哪些自動化」的總覽** |
| Excel 匯入**建新表單** | 設計器 | ✅ 對(那是建表不是填資料) |

### 2.3 沒有「這張表單的全部動作」這個概念

Ragic 的心智是**表單就是操作中心** —— 一個「工具」選單收六組 25+ 動作,
針對這張表的所有事都在這張表上做完。Weyver 目前**沒有這個聚合點**:
使用者得記住「匯出在檢視工具列、標籤在設計器、公開在設定中心」。

---

## 3. 對照 Ragic 工具選單:我們有什麼、在哪裡

> 來源:pilot 客戶正在用的 Ragic 生產畫面(六組 25+ 動作)。
> ⚠️ 這一欄是**落點對照**,不是 parity 評分 —— 有沒有做見 `docs/25`。

| Ragic 工具分組 | 動作 | Weyver 現況 | 落點 |
|---|---|---|---|
| **編輯** | 大量修改 | ⬜ 未起 | — |
| | 批次執行(按鈕) | 🟡 按鈕可單筆執行,無批次 | 記錄頁 |
| | 大量產生 / 大量複製 | ⬜ 未起 | — |
| | 大量刪除 | ✅ | 表單檢視工具列 |
| **匯出** | Excel | ✅(僅已載入列) | 表單檢視工具列 |
| | 完整資料匯出 | ✅ **I-1** | 設定中心(**租戶級,放對了**) |
| | 友善列印 | ✅ | 記錄頁動作列 |
| | PDF | 🟡 走瀏覽器列印 | 記錄頁 |
| | 下載附件 | 🟡 逐筆 | 記錄頁 |
| | 客製列印報表 | ⬜ 未起 | — |
| **匯入** | 從檔案匯入(既有表)| ✅ **#106** | 表單檢視 |
| | 用檔案建立新表單 | ✅ **P0-2** | 設計器 |
| | 多筆編輯上傳 | ⬜ 未起(≈ #153 網格貼上)| — |
| | 定期從網址匯入 | ⬜ 未起 | — |
| **分享** | 公開表單 | ✅ **G-2** | 🔴 **設定中心(錯位)** |
| | 嵌入 / E-mail / 簡訊 | ⬜ 未起 | — |
| **報表** | 產生報表(樞紐/圖表)| ✅ **F-2** | 表單檢視 tab |
| | 合併列印 | ✅ **後續-2** | 設計器 |
| | 發佈到網路 / 定期寄出 | ⬜ 未起 | — |
| **同步與通知** | Webhook | ✅ **G-1** | 設定中心(**租戶級,放對了**) |
| | 通知設定 | ✅ **H-1** | 🔴 **表單層那一半錯位** |
| | 行事曆 / SQL 同步 / 雲端整合 | ⬜ 未起 | — |
| | 表單填寫提醒 | ⬜ 未起(H-1 殘留)| — |

**盤點結論**|Ragic 工具選單 25 項,Weyver **已交付 11 項**(含部分),
其中 **2 項擺在錯的 surface**、**1 項(標籤列印)得先進設計器才印得到**。
真正「沒有」的是 12 項,大多是批次編輯與排程類。

---

## 4. 建議的落點原則(待裁定)

| # | 原則 | 理由 |
|---|---|---|
| **P1** | **粒度決定落點**:per-form 的東西放表單層,per-tenant 的放設定中心 | 這是唯一不會隨功能增加而失效的規則。用「這是設定所以放設定中心」會愈積愈亂 —— 公開表單與通知設定就是這樣進去的 |
| **P2** | **表單層需要一個聚合入口**(對齊 Ragic 的「工具」)| 沒有聚合點,使用者得記住每個動作各自住哪。**但不照抄六組分類** —— 我們只有 11 項,硬分六組會出現只有一項的組 |
| **P3** | **定義留在設計器,執行放到表單層** | 設計標籤 / 定義簽核 / 建按鈕是設計行為;**印標籤 / 送簽 / 按按鈕是日常操作**。目前標籤混在一起,要印得先進設計器 |
| **P4** | 🔴 **搬動入口不得放寬權限** | 公開表單目前受設定中心的 admin gate 保護;搬到表單層之後,**必須改由該表單的 `design` 動作權把關**,否則任何能看到表單的人都看得到公開設定入口 |

---

## 5. 開放問題(OQ-IA-N)— ✅ **已裁定 2026-08-03(全採建議)**

| # | 議題 | 選項 | 建議 |
|---|---|---|---|
| **OQ-IA-1** | 表單層聚合入口的形態 | A. **單一「工具」選單**(Ragic 形態)<br>B. 依現有檢視 tab 分散擺放<br>C. 右側抽屜 | **A** —— B 是現況,問題正出在這;C 與既有的欄位設定抽屜衝突。**但分組用我們自己的**(見 OQ-IA-2) |
| **OQ-IA-2** | 分組怎麼分 | A. 照抄 Ragic 六組<br>B. **依動作對象分三組:資料(匯入/匯出/批次)· 產出(列印/標籤/報表)· 連外(公開/通知)**<br>C. 不分組,平鋪 11 項 | **B** —— A 會出現只有一項的組(我們只有 11 項);C 在 11 項時還行,但加到 20 項就崩 |
| **OQ-IA-3** | 公開表單與通知設定要不要搬 | A. **搬到表單層工具選單,設定中心保留租戶級那一半**<br>B. 兩邊都留(雙入口)<br>C. 不搬 | **A** —— B 的雙入口會讓「改哪個才算數」變成客訴來源。搬動時**必須同步改權限閘門**(P4) |
| **OQ-IA-4** | 標籤列印的入口 | A. **設計留設計器,列印入口移到表單層工具選單**<br>B. 全部留設計器 | **A** —— 印標籤是倉管每天做的事,不該要求他進設計器(而設計器對他多半也沒權限) |
| **OQ-IA-5** | 這批要不要一次做完 | A. 先做**入口聚合 + 兩項搬遷**,批次編輯類另案<br>B. 連同缺的 12 項一起排 | **A** —— 缺的 12 項是新功能(各自要 M0),與 IA 錯位是兩件事。**混在一起會讓「已經有但找不到」繼續被壓在新功能後面** |

---

## 6. 為什麼這件事值得先做

已交付卻找不到的有 **3 項**(公開表單 / 通知設定 / 標籤列印),
它們的工程成本**已經付掉了**,現在只差入口。
相對地,缺的 12 項每一項都要重新排人月。

**投報率的差距不在同一個量級。**


---

## 7. 實作紀錄(2026-08-03,第一階段)

**已交付**|表單層「工具」聚合入口(`form-tools-menu.tsx`),三組:資料 / 產出 / 連外。
匯入資料自標題列的裸露按鈕收進選單;標籤列印入口從設計器移出(依表單既有標籤動態列出,
沒有標籤時**整組隱藏**而不是顯示空標題);公開表單與通知設定改為**帶著 `?form=` 深連**,
兩個設定頁均已能預選 —— 「離開表單後還要把同一張表再選一次」已消除。
e2e `form-tools.spec` 4 條固化,其中兩條專釘深連要帶表單(退化時畫面看起來完全正常)。

**⚠️ 第二階段未做,誠實標注**|公開表單與通知設定的**面板本身**仍住在設定中心。
兩者都與其他東西混在同一頁(公開表單頁還包含待審收件匣、通知頁還包含租戶級偏好),
乾淨拆成可在表單層渲染的元件是另一段重構。
**現況不是雙入口**(設定只有一份,表單層是捷徑),但「不必離開表單」尚未達成。

**🔴 一個必須記著的權限缺口**|`canDesign` 目前恆為 true。
前端**沒有能力來源** —— `/me capabilities` 是 views-list 的 P1 殘留,
同一個檔案裡的 `isAdmin` 也是寫死 true(既有)。
**權限由後端執法**(`public-form-admin.controller` 檢查 `permissions.isAdmin`),
故此處放行**不會放寬權限**,只會讓無權者按下去拿到 403 —— 安全但體驗差。
`/me` 一旦落地,改 `form-workspace.tsx` 那一行即可(刻意集中於單一處)。
